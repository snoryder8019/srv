$2/m to pla, $12/mo per private server, with as many games cold saved to their server as memeory allows only one running at a time per 12.mo.
$80 lifetime play

currentylwe have playcontrols for admins. The play button will host alot more power. user persmission: brodcaster when hit play will spin them as a meeting host too add them to the broadcast modal on the users home page and on the landign page. users can jump onto the chat

when a user has access to spin any of our games as a private server, we need to auto spin a linode and provisssion the smallest shared linode to host games servers. theses should be able to be strong enough to run at least 3-5 desiscated game servers for the user. 

our deedicated servers need some running analytics that socket oi the front end in a global digest and threejs imagery. 
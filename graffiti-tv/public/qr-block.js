/**
 * GFTV QR Block — TV display only
 * Renders a QR + info card directly into a container div.
 * No modals. No popups. No lightboxes. Static layout element.
 *
 * initQRBlock(containerId, opts)
 *
 * opts.label:   'offer' | 'events' | 'signup' | 'menu'
 * opts.title:   quick string shown above — 'ACT NOW',

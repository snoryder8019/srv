<%- include('../partials/head', { title: '404 — Triple-Twenty' }) %>

<div style="display:flex; flex-direction:column; align-items:center; justify-content:center; min-height:60vh; text-align:center; padding:32px;">
  <div style="font-size:4rem; margin-bottom:16px;">🎯</div>
  <h1 style="font-size:2rem; font-weight:800; color:var(--primary); margin-bottom:8px;">Missed.</h1>
  <p style="color:var(--text2); margin-bottom:24px;">That page doesn't exist.</p>
  <a href="/" class="btn btn-primary btn-lg">← Back to Lobby</a>
</div>

<%- include('../partials/foot') %>

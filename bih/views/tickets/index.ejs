<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tickets - bih</title>
  <link rel="stylesheet" href="/css/theme.css">
  <link rel="stylesheet" href="/css/chat.css">
  <link rel="stylesheet" href="/css/sidebar.css">
  <link rel="stylesheet" href="/css/call.css">
  <style>
    .ticket-list { margin-top: 16px; }
    .ticket-row {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 10px 12px;
      border: 1px solid var(--border);
      margin-bottom: 6px;
      transition: background 0.2s;
    }
    .ticket-row:hover { background: var(--bg-surface); }
    .ticket-row a { border-bottom: none; flex: 1; }
    .ticket-status {
      font-size: 11px;
      padding: 2px 8px;
      border: 1px solid;
      text-transform: uppercase;
      letter-spacing: 1px;
      white-space: nowrap;
    }
    .status-open { color: var(--green); border-color: var(--green-dim); }
    .status-in-progress { color: #ffaa00; border-color: #aa7700; }
    .status-closed { color: var(--text-dim); border-color: var(--border); }
    .ticket-meta { color: var(--text-dim); font-size: 12px; white-space: nowrap; }
    .ticket-subject { color: var(--green); }
    .ticket-submitter { color: var(--text-dim); font-size: 12px; }
    .nav-links { margin: 12px 0; }
    .nav-links a { margin-right: 16px; }
    .empty { color: var(--text-dim); margin-top: 16px; }
  </style>
</head>
<body>
  <h1>Tickets</h1>
  <div class="nav-links">
    <a href="/tickets/new">+ New Ticket</a>
    <a href="/dashboard">Dashboard</a>
    <a href="/">Home</a>
  </div>
  <% if (staff) { %>
    <p style="color: var(--text-dim);">[STAFF VIEW — showing all tickets]</p>
  <% } %>
  <div class="ticket-list">
    <% if (tickets.length === 0) { %>
      <p class="empty">No tickets found.</p>
    <% } %>
    <% tickets.forEach(t => { %>
      <div class="ticket-row">
        <span class="ticket-status status-<%= t.status %>"><%= t.status %></span>
        <a href="/tickets/<%= t._id %>">
          <span class="ticket-subject"><%= t.subject %></span>
          <% if (staff && t.user) { %>
            <span class="ticket-submitter"> — <%= t.user.displayName || t.user.email %></span>
          <% } %>
        </a>
        <span class="ticket-meta"><%= new Date(t.createdAt).toLocaleDateString() %></span>
      </div>
    <% }) %>
  </div>
  <%- include('../partials/sidebar') %>
  <%- include('../partials/chat') %>
  <%- include('../partials/call') %>
</body>
</html>

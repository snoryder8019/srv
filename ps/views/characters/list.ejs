<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <%- include('../partials/header', { user: user }) %>

  <main class="container">
    <h1><%= title %></h1>

    <a href="/characters/create" class="btn btn-primary">Create New Character</a>

    <div class="characters-grid">
      <% if (characters.length === 0) { %>
        <p>You don't have any characters yet. Create one to get started!</p>
      <% } else { %>
        <% characters.forEach(character => { %>
          <div class="character-card">
            <h3><%= character.name %></h3>
            <p>Species: <%= character.species %></p>
            <p>Level: <%= character.level %></p>
            <p>Class: <%= character.primaryClass %></p>
            <a href="/characters/<%= character._id %>" class="btn btn-small">View Details</a>
          </div>
        <% }) %>
      <% } %>
    </div>
  </main>

  <%- include('../partials/footer') %>
</body>
</html>

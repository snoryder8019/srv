<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <link rel="stylesheet" href="/stylesheets/asset-builder.css">
</head>
<body>
  <%- include('../partials/header', { user: user, character: character }) %>

  <div class="container" style="max-width: 1400px; margin-top: 2rem;">
    <div class="page-header">
      <h1>My Assets</h1>
      <p>Manage your assets and review community suggestions</p>
    </div>

    <div id="alertContainer" style="margin: 1rem 0;"></div>

    <div id="myAssetsContainer">
      <p style="text-align: center; padding: 3rem; color: #888;">Loading your assets...</p>
    </div>
  </div>

  <%- include('../partials/footer') %>

  <!-- Pass user data to JavaScript -->
  <script>
    <% if (user) { %>
      window.USER_ID = "<%= user._id %>";
      window.USERNAME = "<%= user.username %>";
    <% } else { %>
      window.USER_ID = null;
      window.USERNAME = null;
    <% } %>
  </script>

  <script src="/javascripts/my-assets.js"></script>
</body>
</html>

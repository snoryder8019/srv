<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <link rel="stylesheet" href="/stylesheets/landing.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/styles/atom-one-dark.min.css">
  <script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/highlight.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
  <style>
    body {
      background: #0a0e27;
      color: #e4e4e7;
      min-height: 100vh;
    }

    .docs-container {
      max-width: 1400px;
      margin: 0 auto;
      padding: 2rem;
      display: grid;
      grid-template-columns: 300px 1fr;
      gap: 2rem;
      min-height: calc(100vh - 4rem);
    }

    /* Sidebar */
    .docs-sidebar {
      position: sticky;
      top: 2rem;
      height: fit-content;
      max-height: calc(100vh - 4rem);
      overflow-y: auto;
      background: rgba(15, 23, 42, 0.6);
      border: 1px solid rgba(138, 79, 255, 0.3);
      border-radius: 12px;
      padding: 1.5rem;
      backdrop-filter: blur(10px);
    }

    .docs-sidebar h2 {
      font-family: 'Orbitron', sans-serif;
      font-size: 1.2rem;
      color: #8a4fff;
      margin-bottom: 1rem;
      padding-bottom: 0.5rem;
      border-bottom: 1px solid rgba(138, 79, 255, 0.3);
    }

    .docs-category {
      margin-bottom: 1.5rem;
    }

    .docs-category h3 {
      font-size: 0.9rem;
      color: #00ff9f;
      margin-bottom: 0.75rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .docs-category-count {
      font-size: 0.75rem;
      color: #94a3b8;
      margin-left: 0.5rem;
    }

    .docs-list {
      list-style: none;
      padding-left: 0;
    }

    .docs-list li {
      margin-bottom: 0.5rem;
    }

    .docs-list a {
      display: block;
      padding: 0.5rem 0.75rem;
      color: #cbd5e1;
      text-decoration: none;
      border-radius: 6px;
      transition: all 0.2s;
      font-size: 0.85rem;
    }

    .docs-list a:hover {
      background: rgba(138, 79, 255, 0.2);
      color: #8a4fff;
      transform: translateX(4px);
    }

    .docs-list a.active {
      background: rgba(138, 79, 255, 0.3);
      color: #8a4fff;
      border-left: 3px solid #8a4fff;
    }

    /* Content Area */
    .docs-content {
      background: rgba(15, 23, 42, 0.4);
      border: 1px solid rgba(138, 79, 255, 0.2);
      border-radius: 12px;
      padding: 2rem;
      backdrop-filter: blur(10px);
      min-height: 500px;
    }

    .docs-header {
      margin-bottom: 2rem;
      padding-bottom: 1rem;
      border-bottom: 2px solid rgba(138, 79, 255, 0.3);
    }

    .docs-header h1 {
      font-family: 'Orbitron', sans-serif;
      font-size: 2.5rem;
      background: linear-gradient(135deg, #8a4fff 0%, #00ff9f 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      margin-bottom: 0.5rem;
    }

    .docs-meta {
      display: flex;
      gap: 1rem;
      font-size: 0.85rem;
      color: #94a3b8;
    }

    .docs-meta-item {
      display: flex;
      align-items: center;
      gap: 0.25rem;
    }

    /* Markdown Content Styling */
    .docs-body {
      line-height: 1.8;
      font-size: 1rem;
    }

    .docs-body h1,
    .docs-body h2,
    .docs-body h3,
    .docs-body h4,
    .docs-body h5,
    .docs-body h6 {
      font-family: 'Orbitron', sans-serif;
      color: #8a4fff;
      margin-top: 2rem;
      margin-bottom: 1rem;
    }

    .docs-body h1 { font-size: 2rem; }
    .docs-body h2 { font-size: 1.75rem; }
    .docs-body h3 { font-size: 1.5rem; }
    .docs-body h4 { font-size: 1.25rem; }

    .docs-body p {
      margin-bottom: 1rem;
      color: #cbd5e1;
    }

    .docs-body a {
      color: #00ff9f;
      text-decoration: none;
      border-bottom: 1px solid rgba(0, 255, 159, 0.3);
      transition: all 0.2s;
    }

    .docs-body a:hover {
      border-bottom-color: #00ff9f;
      color: #00ffaa;
    }

    .docs-body ul,
    .docs-body ol {
      margin-bottom: 1rem;
      padding-left: 2rem;
      color: #cbd5e1;
    }

    .docs-body li {
      margin-bottom: 0.5rem;
    }

    .docs-body code {
      background: rgba(0, 0, 0, 0.5);
      padding: 0.2rem 0.4rem;
      border-radius: 4px;
      font-family: 'Courier New', monospace;
      font-size: 0.9em;
      color: #00ff9f;
    }

    .docs-body pre {
      background: rgba(0, 0, 0, 0.7);
      border: 1px solid rgba(138, 79, 255, 0.3);
      border-radius: 8px;
      padding: 1rem;
      overflow-x: auto;
      margin-bottom: 1rem;
    }

    .docs-body pre code {
      background: none;
      padding: 0;
      color: inherit;
    }

    .docs-body blockquote {
      border-left: 4px solid #8a4fff;
      padding-left: 1rem;
      margin: 1rem 0;
      color: #94a3b8;
      font-style: italic;
    }

    .docs-body table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 1rem;
    }

    .docs-body th,
    .docs-body td {
      border: 1px solid rgba(138, 79, 255, 0.3);
      padding: 0.75rem;
      text-align: left;
    }

    .docs-body th {
      background: rgba(138, 79, 255, 0.2);
      color: #8a4fff;
      font-weight: 600;
    }

    .docs-body hr {
      border: none;
      border-top: 1px solid rgba(138, 79, 255, 0.3);
      margin: 2rem 0;
    }

    /* Welcome Screen */
    .docs-welcome {
      text-align: center;
      padding: 4rem 2rem;
    }

    .docs-welcome h1 {
      font-family: 'Orbitron', sans-serif;
      font-size: 3rem;
      background: linear-gradient(135deg, #8a4fff 0%, #00ff9f 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      margin-bottom: 1rem;
    }

    .docs-welcome p {
      font-size: 1.2rem;
      color: #94a3b8;
      margin-bottom: 2rem;
    }

    .docs-stats {
      display: flex;
      justify-content: center;
      gap: 2rem;
      margin-top: 2rem;
    }

    .docs-stat {
      text-align: center;
    }

    .docs-stat-value {
      font-size: 2rem;
      font-weight: bold;
      color: #8a4fff;
      font-family: 'Orbitron', sans-serif;
    }

    .docs-stat-label {
      font-size: 0.9rem;
      color: #94a3b8;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    /* Back Button */
    .back-button {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.5rem 1rem;
      background: rgba(138, 79, 255, 0.2);
      border: 1px solid rgba(138, 79, 255, 0.3);
      color: #8a4fff;
      text-decoration: none;
      border-radius: 6px;
      font-size: 0.9rem;
      transition: all 0.2s;
      margin-bottom: 1rem;
    }

    .back-button:hover {
      background: rgba(138, 79, 255, 0.3);
      transform: translateX(-4px);
    }

    /* Mobile Menu Toggle for Sidebar */
    .sidebar-toggle {
      display: none;
      position: fixed;
      bottom: 20px;
      right: 20px;
      z-index: 1000;
      background: rgba(138, 79, 255, 0.9);
      border: 1px solid rgba(138, 79, 255, 0.5);
      color: white;
      padding: 12px 18px;
      border-radius: 50px;
      cursor: pointer;
      font-size: 0.9rem;
      font-weight: 600;
      box-shadow: 0 4px 15px rgba(138, 79, 255, 0.4);
      transition: all 0.3s;
      backdrop-filter: blur(10px);
    }

    .sidebar-toggle:hover {
      background: rgba(138, 79, 255, 1);
      transform: scale(1.05);
      box-shadow: 0 6px 20px rgba(138, 79, 255, 0.6);
    }

    .sidebar-toggle::before {
      content: '📚 ';
      margin-right: 5px;
    }

    /* Responsive Tablet */
    @media (max-width: 1024px) {
      .docs-container {
        grid-template-columns: 250px 1fr;
        gap: 1.5rem;
        padding: 1.5rem;
      }

      .docs-sidebar {
        padding: 1.25rem;
      }

      .docs-content {
        padding: 1.5rem;
      }

      .docs-header h1 {
        font-size: 2rem;
      }

      .docs-welcome h1 {
        font-size: 2.5rem;
      }
    }

    /* Responsive Mobile */
    @media (max-width: 768px) {
      .docs-container {
        grid-template-columns: 1fr;
        padding: 1rem;
        gap: 0;
        min-height: auto;
      }

      .sidebar-toggle {
        display: block;
      }

      .docs-sidebar {
        position: fixed;
        left: 0;
        top: 0;
        bottom: 0;
        width: 280px;
        max-width: 80vw;
        max-height: 100vh;
        padding: 1.5rem;
        transform: translateX(-100%);
        transition: transform 0.3s ease;
        z-index: 999;
        overflow-y: auto;
        box-shadow: 5px 0 20px rgba(0, 0, 0, 0.5);
      }

      .docs-sidebar.mobile-open {
        transform: translateX(0);
      }

      .sidebar-close {
        display: block;
        position: absolute;
        top: 1rem;
        right: 1rem;
        background: rgba(255, 107, 107, 0.2);
        border: 1px solid rgba(255, 107, 107, 0.4);
        color: #ff6b6b;
        width: 32px;
        height: 32px;
        border-radius: 50%;
        cursor: pointer;
        font-size: 1.2rem;
        line-height: 1;
        transition: all 0.2s;
      }

      .sidebar-close:hover {
        background: rgba(255, 107, 107, 0.3);
        transform: scale(1.1);
      }

      .docs-sidebar h2 {
        margin-top: 2.5rem;
      }

      .docs-content {
        padding: 1rem;
        min-height: auto;
      }

      .docs-header {
        margin-bottom: 1.5rem;
      }

      .docs-header h1 {
        font-size: 1.75rem;
      }

      .docs-meta {
        flex-direction: column;
        gap: 0.5rem;
        font-size: 0.8rem;
      }

      .docs-welcome h1 {
        font-size: 2rem;
      }

      .docs-welcome p {
        font-size: 1rem;
      }

      .docs-welcome {
        padding: 2rem 1rem;
      }

      .docs-stats {
        flex-direction: column;
        gap: 1rem;
      }

      .docs-body {
        font-size: 0.95rem;
      }

      .docs-body h1 { font-size: 1.5rem; }
      .docs-body h2 { font-size: 1.35rem; }
      .docs-body h3 { font-size: 1.2rem; }
      .docs-body h4 { font-size: 1.1rem; }

      .docs-body pre {
        overflow-x: auto;
        font-size: 0.85rem;
      }

      .docs-body table {
        display: block;
        overflow-x: auto;
        white-space: nowrap;
      }

      .back-button {
        font-size: 0.85rem;
        padding: 0.5rem 0.75rem;
      }
    }

    /* Small Mobile Devices */
    @media (max-width: 480px) {
      .docs-container {
        padding: 0.5rem;
      }

      .docs-sidebar {
        width: 100%;
        max-width: 100vw;
        padding: 1rem;
      }

      .docs-content {
        padding: 0.75rem;
        border-radius: 8px;
      }

      .docs-header h1 {
        font-size: 1.5rem;
        word-break: break-word;
      }

      .docs-welcome h1 {
        font-size: 1.5rem;
      }

      .docs-welcome p {
        font-size: 0.9rem;
      }

      .docs-body {
        font-size: 0.9rem;
      }

      .docs-body h1 { font-size: 1.3rem; }
      .docs-body h2 { font-size: 1.2rem; }
      .docs-body h3 { font-size: 1.1rem; }
      .docs-body h4 { font-size: 1rem; }

      .docs-body pre {
        padding: 0.75rem;
        font-size: 0.75rem;
      }

      .docs-body code {
        font-size: 0.85em;
        padding: 0.15rem 0.35rem;
      }

      .docs-stat-value {
        font-size: 1.5rem;
      }

      .docs-stat-label {
        font-size: 0.8rem;
      }

      .sidebar-toggle {
        bottom: 15px;
        right: 15px;
        padding: 10px 15px;
        font-size: 0.85rem;
      }

      .back-button {
        font-size: 0.8rem;
        padding: 0.4rem 0.6rem;
      }
    }

    /* Sidebar Overlay */
    .sidebar-overlay {
      display: none;
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: rgba(0, 0, 0, 0.7);
      z-index: 998;
      backdrop-filter: blur(2px);
    }

    .sidebar-overlay.active {
      display: block;
    }

    @media (min-width: 769px) {
      .sidebar-close {
        display: none;
      }
    }

    /* Scrollbar Styling */
    .docs-sidebar::-webkit-scrollbar {
      width: 6px;
    }

    .docs-sidebar::-webkit-scrollbar-track {
      background: rgba(0, 0, 0, 0.3);
      border-radius: 3px;
    }

    .docs-sidebar::-webkit-scrollbar-thumb {
      background: rgba(138, 79, 255, 0.5);
      border-radius: 3px;
    }

    .docs-sidebar::-webkit-scrollbar-thumb:hover {
      background: rgba(138, 79, 255, 0.7);
    }
  </style>
</head>
<body class="landing-page">
  <%- include('../partials/header', { user: user }) %>

  <!-- Mobile Sidebar Toggle -->
  <button class="sidebar-toggle" onclick="toggleMobileSidebar()">Menu</button>

  <!-- Sidebar Overlay -->
  <div class="sidebar-overlay" onclick="closeMobileSidebar()"></div>

  <div class="docs-container">
    <!-- Sidebar Navigation -->
    <aside class="docs-sidebar" id="docsSidebar">
      <button class="sidebar-close" onclick="closeMobileSidebar()">✕</button>
      <h2>Documentation</h2>

      <% if (docsTree && docsTree.categories) { %>
        <% docsTree.categories.forEach(category => { %>
          <div class="docs-category">
            <h3>
              <%= category.name %>
              <span class="docs-category-count">(<%= category.count %>)</span>
            </h3>
            <ul class="docs-list">
              <% category.docs.forEach(doc => { %>
                <li>
                  <a href="/help/documentation?doc=<%= doc.fileName %>"
                     class="<%= docMeta && docMeta.fileName === doc.fileName ? 'active' : '' %>">
                    <%= doc.title %>
                  </a>
                </li>
              <% }); %>
            </ul>
          </div>
        <% }); %>
      <% } else { %>
        <p style="color: #94a3b8; font-size: 0.9rem; text-align: center;">
          Loading documentation index...
        </p>
      <% } %>
    </aside>

    <!-- Main Content -->
    <main class="docs-content">
      <a href="/menu" class="back-button">
        ← Back to Menu
      </a>

      <% if (docContent) { %>
        <!-- Specific Document View -->
        <div class="docs-header">
          <h1><%= docMeta.title %></h1>
          <% if (docsTree) { %>
            <div class="docs-meta">
              <span class="docs-meta-item">
                📄 Markdown Document
              </span>
              <span class="docs-meta-item">
                🕒 Last updated: <%= new Date(docsTree.generated).toLocaleDateString() %>
              </span>
            </div>
          <% } %>
        </div>
        <div class="docs-body" id="docContent"></div>
        <script>
          // Render markdown content
          const markdownContent = `<%= docContent.replace(/`/g, '\\`').replace(/\$/g, '\\$') %>`;
          const htmlContent = marked.parse(markdownContent);
          document.getElementById('docContent').innerHTML = htmlContent;

          // Highlight code blocks
          document.querySelectorAll('pre code').forEach((block) => {
            hljs.highlightBlock(block);
          });
        </script>
      <% } else { %>
        <!-- Welcome Screen -->
        <div class="docs-welcome">
          <h1>📚 Documentation Hub</h1>
          <p>
            Welcome to the Stringborn Universe documentation center.<br>
            Select a document from the sidebar to get started.
          </p>

          <% if (docsTree) { %>
            <div class="docs-stats">
              <div class="docs-stat">
                <div class="docs-stat-value"><%= docsTree.totalFiles %></div>
                <div class="docs-stat-label">Documents</div>
              </div>
              <div class="docs-stat">
                <div class="docs-stat-value"><%= docsTree.categories.length %></div>
                <div class="docs-stat-label">Categories</div>
              </div>
              <div class="docs-stat">
                <div class="docs-stat-value">
                  <%= new Date(docsTree.generated).toLocaleDateString() %>
                </div>
                <div class="docs-stat-label">Last Updated</div>
              </div>
            </div>
          <% } %>

          <div style="margin-top: 3rem; text-align: left; max-width: 600px; margin-left: auto; margin-right: auto;">
            <h3 style="color: #00ff9f; margin-bottom: 1rem;">Quick Start</h3>
            <ul style="color: #cbd5e1; line-height: 2;">
              <li><strong>New to the project?</strong> Start with <a href="/help/documentation?doc=PROJECT_OVERVIEW">Project Overview</a></li>
              <li><strong>Testing the game?</strong> Check out <a href="/help/documentation?doc=TESTER_QUICK_REFERENCE">Tester Quick Reference</a></li>
              <li><strong>Building assets?</strong> Read <a href="/help/documentation?doc=ASSET_BUILDER_COMPLETE">Asset Builder Guide</a></li>
              <li><strong>Exploring the universe?</strong> See <a href="/help/documentation?doc=GALACTIC_MAP_COMPLETE">Galactic Map Guide</a></li>
            </ul>
          </div>
        </div>
      <% } %>
    </main>
  </div>

  <%- include('../partials/footer') %>

  <script>
    // Mobile sidebar toggle functionality
    function toggleMobileSidebar() {
      const sidebar = document.getElementById('docsSidebar');
      const overlay = document.querySelector('.sidebar-overlay');

      sidebar.classList.toggle('mobile-open');
      overlay.classList.toggle('active');

      // Prevent body scroll when sidebar is open
      if (sidebar.classList.contains('mobile-open')) {
        document.body.style.overflow = 'hidden';
      } else {
        document.body.style.overflow = '';
      }
    }

    function closeMobileSidebar() {
      const sidebar = document.getElementById('docsSidebar');
      const overlay = document.querySelector('.sidebar-overlay');

      sidebar.classList.remove('mobile-open');
      overlay.classList.remove('active');
      document.body.style.overflow = '';
    }

    // Close sidebar when clicking a documentation link on mobile
    document.addEventListener('DOMContentLoaded', () => {
      const docLinks = document.querySelectorAll('.docs-list a');
      docLinks.forEach(link => {
        link.addEventListener('click', () => {
          if (window.innerWidth <= 768) {
            closeMobileSidebar();
          }
        });
      });
    });

    // Close sidebar on window resize if no longer mobile
    window.addEventListener('resize', () => {
      if (window.innerWidth > 768) {
        closeMobileSidebar();
      }
    });

    // Documentation Modal for Mobile
    const docModal = {
      overlay: null,
      container: null,
      contentArea: null,

      init() {
        // Create modal elements
        this.createModal();

        // Only intercept links on mobile devices
        if (window.innerWidth <= 768) {
          this.interceptDocLinks();
        }

        // Re-check on resize
        window.addEventListener('resize', () => {
          if (window.innerWidth <= 768) {
            this.interceptDocLinks();
          }
        });
      },

      createModal() {
        // Create overlay
        this.overlay = document.createElement('div');
        this.overlay.className = 'doc-modal-overlay';
        this.overlay.style.cssText = `
          display: none;
          position: fixed;
          top: 0;
          left: 0;
          right: 0;
          bottom: 0;
          background: rgba(0, 0, 0, 0.9);
          backdrop-filter: blur(8px);
          z-index: 9999;
          overflow-y: auto;
          -webkit-overflow-scrolling: touch;
          opacity: 0;
          transition: opacity 0.3s ease;
        `;

        // Create container
        this.container = document.createElement('div');
        this.container.className = 'doc-modal-container';
        this.container.style.cssText = `
          position: relative;
          min-height: 100vh;
          padding: 1rem;
          display: flex;
          flex-direction: column;
        `;

        // Create header
        const header = document.createElement('div');
        header.style.cssText = `
          display: flex;
          justify-content: space-between;
          align-items: center;
          padding: 1rem;
          background: rgba(15, 23, 42, 0.8);
          border: 1px solid rgba(138, 79, 255, 0.3);
          border-radius: 8px;
          margin-bottom: 1rem;
        `;

        const title = document.createElement('h2');
        title.className = 'doc-modal-title';
        title.style.cssText = `
          font-family: 'Orbitron', sans-serif;
          font-size: 1.25rem;
          color: #8a4fff;
          margin: 0;
          flex: 1;
          word-break: break-word;
        `;

        const closeBtn = document.createElement('button');
        closeBtn.innerHTML = '×';
        closeBtn.style.cssText = `
          background: transparent;
          border: none;
          color: #888;
          font-size: 2.5rem;
          line-height: 1;
          cursor: pointer;
          padding: 0;
          width: 40px;
          height: 40px;
          display: flex;
          align-items: center;
          justify-content: center;
          transition: all 0.2s;
          border-radius: 4px;
          flex-shrink: 0;
        `;
        closeBtn.onclick = () => this.close();
        closeBtn.onmouseover = () => {
          closeBtn.style.color = '#00ff9f';
          closeBtn.style.background = 'rgba(0, 255, 159, 0.1)';
          closeBtn.style.transform = 'rotate(90deg)';
        };
        closeBtn.onmouseout = () => {
          closeBtn.style.color = '#888';
          closeBtn.style.background = 'transparent';
          closeBtn.style.transform = 'rotate(0deg)';
        };

        header.appendChild(title);
        header.appendChild(closeBtn);

        // Create content area
        this.contentArea = document.createElement('div');
        this.contentArea.className = 'doc-modal-content';
        this.contentArea.style.cssText = `
          background: rgba(15, 23, 42, 0.6);
          border: 1px solid rgba(138, 79, 255, 0.2);
          border-radius: 8px;
          padding: 1.5rem;
          flex: 1;
          overflow-y: auto;
        `;

        // Assemble modal
        this.container.appendChild(header);
        this.container.appendChild(this.contentArea);
        this.overlay.appendChild(this.container);
        document.body.appendChild(this.overlay);

        // Close on overlay click (but not on content click)
        this.overlay.addEventListener('click', (e) => {
          if (e.target === this.overlay) {
            this.close();
          }
        });
      },

      interceptDocLinks() {
        // Find all documentation links
        const docLinks = document.querySelectorAll('.docs-list a, .docs-body a[href^="/help/documentation"]');

        docLinks.forEach(link => {
          // Remove existing listeners if any
          link.replaceWith(link.cloneNode(true));
        });

        // Re-select after cloning
        const freshLinks = document.querySelectorAll('.docs-list a, .docs-body a[href^="/help/documentation"]');

        freshLinks.forEach(link => {
          link.addEventListener('click', (e) => {
            if (window.innerWidth <= 768) {
              e.preventDefault();
              const url = new URL(link.href);
              const docParam = url.searchParams.get('doc');
              if (docParam) {
                this.loadDoc(docParam);
                closeMobileSidebar(); // Close sidebar when opening modal
              }
            }
          });
        });
      },

      async loadDoc(docName) {
        try {
          // Show modal with loading state
          this.open();
          this.setTitle('Loading...');
          this.contentArea.innerHTML = '<p style="color: #94a3b8; text-align: center; padding: 2rem;">Loading document...</p>';

          // Fetch the document
          const response = await fetch(`/help/documentation?doc=${docName}`);
          const html = await response.text();

          // Parse the HTML to extract the document content
          const parser = new DOMParser();
          const doc = parser.parseFromString(html, 'text/html');

          // Get the title
          const titleElement = doc.querySelector('.docs-header h1');
          const title = titleElement ? titleElement.textContent : docName.replace(/_/g, ' ');

          // Get the markdown content
          const docBody = doc.querySelector('.docs-body');

          if (docBody) {
            this.setTitle(title);
            this.contentArea.innerHTML = docBody.innerHTML;

            // Re-highlight code blocks
            this.contentArea.querySelectorAll('pre code').forEach((block) => {
              if (typeof hljs !== 'undefined') {
                hljs.highlightBlock(block);
              }
            });

            // Apply documentation body styles
            this.contentArea.style.cssText += `
              line-height: 1.8;
              font-size: 0.95rem;
            `;

            // Style the content (inherit from docs-body)
            this.contentArea.classList.add('docs-body');

            // Intercept any links within the loaded content
            setTimeout(() => this.interceptDocLinks(), 100);
          } else {
            throw new Error('Could not parse document content');
          }

        } catch (error) {
          console.error('Error loading document:', error);
          this.setTitle('Error');
          this.contentArea.innerHTML = `
            <div style="color: #ff6b6b; text-align: center; padding: 2rem;">
              <p>Failed to load document.</p>
              <p style="font-size: 0.9rem; color: #94a3b8; margin-top: 1rem;">${error.message}</p>
            </div>
          `;
        }
      },

      setTitle(title) {
        const titleElement = this.container.querySelector('.doc-modal-title');
        if (titleElement) {
          titleElement.textContent = title;
        }
      },

      open() {
        this.overlay.style.display = 'block';
        document.body.style.overflow = 'hidden';
        // Animate in
        setTimeout(() => {
          this.overlay.style.opacity = '1';
        }, 10);
      },

      close() {
        this.overlay.style.opacity = '0';
        setTimeout(() => {
          this.overlay.style.display = 'none';
          document.body.style.overflow = '';
        }, 300);
      }
    };

    // Initialize modal on page load
    docModal.init();
  </script>
</body>
</html>

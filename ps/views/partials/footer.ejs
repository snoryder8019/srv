<footer>
  <div class="footer-content">
    <p>&copy; 2025 Stringborn Universe v0.1 | Created by Violet</p>
    <p>A community-driven sci-fi universe powered by Three.js</p>
  </div>
</footer>

<script>
  // Global logout function
  function logout() {
    fetch('/auth/logout', {
      method: 'POST',
      credentials: 'same-origin'
    })
    .then(res => {
      if (res.ok) {
        window.location.href = '/';
      } else {
        console.error('Logout failed');
        // Fallback: redirect anyway
        window.location.href = '/';
      }
    })
    .catch(err => {
      console.error('Logout error:', err);
      // Fallback: redirect anyway
      window.location.href = '/';
    });
  }
</script>

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <style>
    .welcome-container {
      max-width: 800px;
      margin: 4rem auto;
      padding: 2rem;
      text-align: center;
    }

    .welcome-container h1 {
      font-size: 3rem;
      margin-bottom: 1rem;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .welcome-container p {
      font-size: 1.2rem;
      line-height: 1.8;
      margin: 1.5rem 0;
      opacity: 0.9;
    }

    .welcome-features {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 2rem;
      margin: 3rem 0;
      text-align: left;
    }

    .feature-item {
      padding: 1.5rem;
      background: rgba(255, 255, 255, 0.05);
      border-radius: 8px;
      border-left: 3px solid #667eea;
    }

    .feature-item h3 {
      margin: 0 0 0.5rem 0;
      font-size: 1.1rem;
    }

    .feature-item p {
      margin: 0;
      font-size: 0.95rem;
      opacity: 0.8;
    }

    .continue-btn {
      margin-top: 3rem;
      padding: 1rem 3rem;
      font-size: 1.2rem;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: transform 0.2s, box-shadow 0.2s;
    }

    .continue-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 30px rgba(102, 126, 234, 0.4);
    }

    .continue-btn:active {
      transform: translateY(0);
    }
  </style>
</head>
<body>
  <div class="welcome-container">
    <h1>Welcome to Stringborn Universe</h1>

    <p>
      You've entered a vast, player-driven cosmos where your choices shape the galaxy.
      Explore distant worlds, create legendary assets, and forge your destiny among the stars.
    </p>

    <div class="welcome-features">
      <div class="feature-item">
        <h3>Explore the Universe</h3>
        <p>Navigate through procedurally generated zones, planets, and space stations.</p>
      </div>

      <div class="feature-item">
        <h3>Create & Build</h3>
        <p>Design custom assets, ships, and structures that become part of the universe.</p>
      </div>

      <div class="feature-item">
        <h3>Shape the Story</h3>
        <p>Your actions influence the galactic narrative and territorial control.</p>
      </div>

      <div class="feature-item">
        <h3>Community Driven</h3>
        <p>Vote on new content and participate in democratic governance.</p>
      </div>
    </div>

    <p style="font-size: 1rem; opacity: 0.7;">
      Ready to begin your journey?
    </p>

    <button class="continue-btn" onclick="completeWelcome()">
      Continue to Introduction
    </button>
  </div>

  <script>
    async function completeWelcome() {
      try {
        const response = await fetch('/complete-welcome', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json'
          }
        });

        const data = await response.json();

        if (response.ok) {
          window.location.href = '/intro';
        } else {
          alert('Failed to complete welcome. Please try again.');
        }
      } catch (err) {
        console.error('Error completing welcome:', err);
        alert('An error occurred. Please try again.');
      }
    }
  </script>
</body>
</html>

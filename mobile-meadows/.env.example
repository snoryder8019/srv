# ── Server ──
PORT=
NODE_ENV=development
SESSION_SECRET=

# ── MongoDB ──
MONGO_URI=
# Collections are prefixed with mm. (e.g. mm.users, mm.bookings)

# ── Google OAuth (Passport) ──
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=

# ── Admin ──
ADMIN_EMAIL=snoryder8019@gmail.com

# ── Email (Nodemailer SMTP) ──
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=

# ── Site / Brand ──
SITE_NAME=Mobile Meadows
SITE_TAGLINE=Mobile RV & Motorhome Repair — Branson, MO
SITE_URL=

# ── Future: SMS (Twilio or similar) ──
# SMS_ACCOUNT_SID=
# SMS_AUTH_TOKEN=
# SMS_FROM_NUMBER=

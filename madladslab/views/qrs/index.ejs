<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - MadLadsLab</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      padding: 20px;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
    }

    .header {
      background: white;
      padding: 30px;
      border-radius: 12px;
      margin-bottom: 30px;
      box-shadow: 0 10px 40px rgba(0,0,0,0.1);
    }

    .header h1 {
      color: #333;
      margin-bottom: 10px;
    }

    .header p {
      color: #666;
      font-size: 14px;
    }

    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
      margin-bottom: 30px;
    }

    .stat-card {
      background: white;
      padding: 25px;
      border-radius: 12px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.08);
    }

    .stat-card h3 {
      color: #999;
      font-size: 14px;
      font-weight: 500;
      text-transform: uppercase;
      margin-bottom: 10px;
    }

    .stat-card .value {
      font-size: 36px;
      font-weight: 700;
      color: #667eea;
    }

    .actions {
      display: flex;
      gap: 15px;
      margin-bottom: 30px;
    }

    .btn {
      padding: 12px 24px;
      border: none;
      border-radius: 8px;
      font-size: 14px;
      font-weight: 600;
      cursor: pointer;
      text-decoration: none;
      display: inline-block;
      transition: all 0.3s;
    }

    .btn-primary {
      background: #667eea;
      color: white;
    }

    .btn-primary:hover {
      background: #5568d3;
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
    }

    .btn-secondary {
      background: white;
      color: #667eea;
      border: 2px solid #667eea;
    }

    .btn-secondary:hover {
      background: #667eea;
      color: white;
    }

    .qr-list {
      background: white;
      border-radius: 12px;
      box-shadow: 0 10px 40px rgba(0,0,0,0.1);
      overflow: hidden;
    }

    .qr-list-header {
      padding: 20px 30px;
      background: #f8f9fa;
      border-bottom: 1px solid #e9ecef;
    }

    .qr-list-header h2 {
      color: #333;
      font-size: 20px;
    }

    .qr-item {
      padding: 20px 30px;
      border-bottom: 1px solid #e9ecef;
      display: flex;
      align-items: center;
      justify-content: space-between;
      transition: background 0.2s;
    }

    .qr-item:hover {
      background: #f8f9fa;
    }

    .qr-item:last-child {
      border-bottom: none;
    }

    .qr-info {
      flex: 1;
    }

    .qr-name {
      font-size: 16px;
      font-weight: 600;
      color: #333;
      margin-bottom: 5px;
    }

    .qr-meta {
      font-size: 13px;
      color: #999;
    }

    .qr-stats {
      display: flex;
      gap: 30px;
      margin-right: 30px;
    }

    .qr-stat {
      text-align: center;
    }

    .qr-stat-value {
      font-size: 20px;
      font-weight: 700;
      color: #667eea;
    }

    .qr-stat-label {
      font-size: 12px;
      color: #999;
      text-transform: uppercase;
    }

    .qr-actions {
      display: flex;
      gap: 10px;
    }

    .btn-small {
      padding: 8px 16px;
      font-size: 13px;
    }

    .status-badge {
      display: inline-block;
      padding: 4px 12px;
      border-radius: 12px;
      font-size: 12px;
      font-weight: 600;
      text-transform: uppercase;
    }

    .status-active {
      background: #d4edda;
      color: #155724;
    }

    .status-paused {
      background: #fff3cd;
      color: #856404;
    }

    .status-archived {
      background: #f8d7da;
      color: #721c24;
    }

    .empty-state {
      padding: 60px 30px;
      text-align: center;
      color: #999;
    }

    .empty-state svg {
      width: 120px;
      height: 120px;
      margin-bottom: 20px;
      opacity: 0.3;
    }

    .empty-state h3 {
      font-size: 20px;
      margin-bottom: 10px;
    }

    .empty-state p {
      margin-bottom: 20px;
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Header -->
    <div class="header">
      <h1>QR Code Manager</h1>
      <p>Create, manage, and track your QR codes</p>
    </div>

    <!-- Stats -->
    <div class="stats-grid">
      <div class="stat-card">
        <h3>Total QR Codes</h3>
        <div class="value"><%= stats.total %></div>
      </div>
      <div class="stat-card">
        <h3>Active</h3>
        <div class="value"><%= stats.active %></div>
      </div>
      <div class="stat-card">
        <h3>Total Scans</h3>
        <div class="value"><%= stats.totalScans %></div>
      </div>
    </div>

    <!-- Actions -->
    <div class="actions">
      <a href="/qrs/create" class="btn btn-primary">+ Create QR Code</a>
      <a href="/qrs/analytics" class="btn btn-secondary">View Analytics</a>
    </div>

    <!-- QR Code List -->
    <div class="qr-list">
      <div class="qr-list-header">
        <h2>Your QR Codes</h2>
      </div>

      <% if (qrCodes.length === 0) { %>
        <div class="empty-state">
          <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v1m6 11h2m-6 0h-2v4m0-11v3m0 0h.01M12 12h4.01M16 20h4M4 12h4m12 0h.01M5 8h2a1 1 0 001-1V5a1 1 0 00-1-1H5a1 1 0 00-1 1v2a1 1 0 001 1zm12 0h2a1 1 0 001-1V5a1 1 0 00-1-1h-2a1 1 0 00-1 1v2a1 1 0 001 1zM5 20h2a1 1 0 001-1v-2a1 1 0 00-1-1H5a1 1 0 00-1 1v2a1 1 0 001 1z" />
          </svg>
          <h3>No QR codes yet</h3>
          <p>Create your first QR code to get started</p>
          <a href="/qrs/create" class="btn btn-primary">Create Your First QR Code</a>
        </div>
      <% } else { %>
        <% qrCodes.forEach(qr => { %>
          <div class="qr-item">
            <div class="qr-info">
              <div class="qr-name"><%= qr.name %></div>
              <div class="qr-meta">
                <span class="status-badge status-<%= qr.status %>"><%= qr.status %></span>
                <% if (qr.isDynamic) { %>
                  <span style="margin-left: 10px; color: #667eea;">🔗 Dynamic</span>
                <% } %>
                <span style="margin-left: 10px;"><%= qr.category %></span>
              </div>
            </div>

            <div class="qr-stats">
              <div class="qr-stat">
                <div class="qr-stat-value"><%= (qr.stats && qr.stats.totalScans) || 0 %></div>
                <div class="qr-stat-label">Scans</div>
              </div>
            </div>

            <div class="qr-actions">
              <a href="/qrs/<%= qr._id %>" class="btn btn-secondary btn-small">View</a>
              <a href="/api/v1/qrs/<%= qr._id %>/image?format=png" download class="btn btn-secondary btn-small">Download</a>
            </div>
          </div>
        <% }) %>
      <% } %>
    </div>

    <%- include('../mainContent/adminNav.ejs') %>
  </div>
</body>
</html>

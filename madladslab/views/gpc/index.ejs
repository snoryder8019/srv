<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
      min-height: 100vh;
      padding: 2rem 1rem;
    }

    .container {
      max-width: 800px;
      margin: 0 auto;
    }

    .header {
      text-align: center;
      color: white;
      margin-bottom: 3rem;
    }

    .header h1 {
      font-size: 2.5rem;
      margin-bottom: 0.5rem;
    }

    .header p {
      font-size: 1.2rem;
      opacity: 0.9;
    }

    .card {
      background: white;
      border-radius: 20px;
      padding: 2.5rem;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
      margin-bottom: 2rem;
    }

    .wallet-section {
      margin-bottom: 2rem;
    }

    .wallet-section h2 {
      color: #333;
      margin-bottom: 1rem;
      font-size: 1.5rem;
    }

    .balance-display {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 2rem;
      border-radius: 16px;
      text-align: center;
      margin-bottom: 2rem;
    }

    .balance-display .amount {
      font-size: 3rem;
      font-weight: 700;
      margin-bottom: 0.5rem;
    }

    .balance-display .value {
      font-size: 1.2rem;
      opacity: 0.9;
    }

    .form-group {
      margin-bottom: 1.5rem;
    }

    .form-group label {
      display: block;
      color: #374151;
      font-weight: 600;
      margin-bottom: 0.5rem;
    }

    .form-group input {
      width: 100%;
      padding: 1rem;
      border: 2px solid #e5e7eb;
      border-radius: 12px;
      font-size: 1rem;
      font-family: monospace;
      transition: border-color 0.3s;
    }

    .form-group input:focus {
      outline: none;
      border-color: #667eea;
    }

    .form-group .hint {
      font-size: 0.85rem;
      color: #6b7280;
      margin-top: 0.5rem;
    }

    .btn {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      border: none;
      padding: 1rem 2rem;
      border-radius: 12px;
      cursor: pointer;
      font-size: 1rem;
      font-weight: 600;
      width: 100%;
      transition: transform 0.2s, box-shadow 0.2s;
    }

    .btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 20px rgba(102, 126, 234, 0.4);
    }

    .btn-secondary {
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
      margin-top: 1rem;
    }

    .btn-secondary:hover {
      box-shadow: 0 8px 20px rgba(16, 185, 129, 0.4);
    }

    .current-address {
      background: #f9fafb;
      padding: 1rem;
      border-radius: 8px;
      word-break: break-all;
      font-family: monospace;
      font-size: 0.9rem;
      color: #374151;
      margin-bottom: 1rem;
    }

    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
      gap: 1rem;
      margin-bottom: 2rem;
    }

    .stat-box {
      background: #f9fafb;
      padding: 1.5rem;
      border-radius: 12px;
      text-align: center;
    }

    .stat-box .label {
      font-size: 0.85rem;
      color: #6b7280;
      margin-bottom: 0.5rem;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .stat-box .value {
      font-size: 1.5rem;
      font-weight: 700;
      color: #333;
    }

    .alert {
      padding: 1rem;
      border-radius: 12px;
      margin-bottom: 1rem;
      font-weight: 600;
    }

    .alert.success {
      background: #d1fae5;
      color: #065f46;
      border: 2px solid #10b981;
    }

    .alert.error {
      background: #fee2e2;
      color: #991b1b;
      border: 2px solid #ef4444;
    }

    .quick-actions {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 1rem;
      margin-top: 2rem;
    }

    .quick-actions a {
      text-decoration: none;
    }

    .login-prompt {
      text-align: center;
      color: white;
    }

    .login-prompt h2 {
      font-size: 2rem;
      margin-bottom: 1rem;
    }

    .login-prompt .btn {
      max-width: 300px;
      margin: 2rem auto 0;
    }

    .admin-link {
      text-align: center;
      margin-top: 1rem;
    }

    .admin-link a {
      color: white;
      text-decoration: none;
      padding: 0.75rem 1.5rem;
      background: rgba(255, 255, 255, 0.2);
      border-radius: 8px;
      display: inline-block;
      backdrop-filter: blur(10px);
    }

    @media (max-width: 768px) {
      .header h1 {
        font-size: 2rem;
      }

      .balance-display .amount {
        font-size: 2rem;
      }

      .quick-actions {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>🍝 Graffiti Pasta Coin</h1>
      <p>Your Cryptocurrency Rewards Wallet</p>
    </div>

    <% if (user) { %>
      <div class="card">
        <% if (balance) { %>
          <div class="balance-display">
            <div class="amount"><%= balance.wallet.balance.toFixed(2) %> GPC</div>
            <div class="value">≈ $<%= balance.dollarValue.toFixed(2) %> USD</div>
          </div>

          <div class="stats-grid">
            <div class="stat-box">
              <div class="label">Total Earned</div>
              <div class="value"><%= balance.wallet.totalEarned.toFixed(2) %></div>
            </div>
            <div class="stat-box">
              <div class="label">Spent</div>
              <div class="value">$<%= balance.wallet.totalPurchases.toFixed(2) %></div>
            </div>
            <div class="stat-box">
              <div class="label">Redeemed</div>
              <div class="value"><%= balance.wallet.totalRedeemed.toFixed(2) %></div>
            </div>
            <div class="stat-box">
              <div class="label">Coin Value</div>
              <div class="value">$<%= balance.coinValue.toFixed(4) %></div>
            </div>
          </div>
        <% } %>

        <div class="wallet-section">
          <h2>Your Wallet Address</h2>
          <% if (wallet && wallet.address) { %>
            <div class="current-address">
              <strong>Internal Address:</strong><br>
              <%= wallet.address %>
            </div>
          <% } %>

          <div id="alertContainer"></div>

          <form id="walletForm">
            <div class="form-group">
              <label for="receiveAddress">External Wallet Address (Optional)</label>
              <input
                type="text"
                id="receiveAddress"
                placeholder="0x... or your wallet address"
                value="<%= wallet && wallet.externalWalletAddress ? wallet.externalWalletAddress : '' %>"
              >
              <div class="hint">
                Link your MetaMask or other crypto wallet to receive GPC on-chain (coming soon)
              </div>
            </div>
            <button type="submit" class="btn">
              <%= wallet && wallet.externalWalletAddress ? 'Update' : 'Link' %> External Wallet
            </button>
          </form>
        </div>

        <div class="quick-actions">
          <a href="/gpc/scan">
            <button class="btn btn-secondary">📱 Scan QR Code</button>
          </a>
          <a href="/gpc/wallet">
            <button class="btn btn-secondary">📜 View History</button>
          </a>
        </div>

        <% if (user.isAustins) { %>
          <div class="admin-link">
            <a href="/gpc/admin">⚙️ Admin Dashboard</a>
          </div>
        <% } %>
      </div>

    <% } else { %>
      <div class="card login-prompt">
        <h2>Welcome to GPC!</h2>
        <p>Earn cryptocurrency rewards with every purchase at Graffiti Pasta locations.</p>
        <p style="margin-top: 1rem;">Sign in to start earning GPC today.</p>
        <a href="/auth">
          <button class="btn">Get Started</button>
        </a>
      </div>
    <% } %>
  </div>

  <% if (user) { %>
  <script>
    function showAlert(message, type = 'success') {
      const alertContainer = document.getElementById('alertContainer');
      const alert = document.createElement('div');
      alert.className = `alert ${type}`;
      alert.textContent = message;
      alertContainer.appendChild(alert);

      setTimeout(() => {
        alert.remove();
      }, 5000);
    }

    document.getElementById('walletForm').addEventListener('submit', function(e) {
      e.preventDefault();

      const externalAddress = document.getElementById('receiveAddress').value.trim();

      if (!externalAddress) {
        showAlert('Please enter a wallet address', 'error');
        return;
      }

      fetch('/gpc/api/wallet/link', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json'
        },
        body: JSON.stringify({ externalAddress })
      })
        .then(res => res.json())
        .then(data => {
          if (data.success) {
            showAlert('External wallet linked successfully!', 'success');
          } else {
            showAlert(data.error || 'Failed to link wallet', 'error');
          }
        })
        .catch(err => {
          console.error('Error linking wallet:', err);
          showAlert('Error linking wallet', 'error');
        });
    });
  </script>
  <% } %>

  <%-include('../mainContent/adminNav.ejs')%>
</body>
</html>

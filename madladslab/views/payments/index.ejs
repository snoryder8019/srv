<!DOCTYPE html>
<html>
<head>
    <link rel="icon" type="image/png" href="/images/beaker_logo_no_stash.png">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
    <meta property="og:title" content="Payment Options - madLadsLab" />
    <title><%= pageTitle %> - madLadsLab</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: #0a0a0a;
            color: #e0e0e0;
            line-height: 1.6;
            min-height: 100vh;
        }

        .payment-header {
            text-align: center;
            padding: 3rem 2rem;
            background: linear-gradient(180deg, #1a1a1a 0%, #0a0a0a 100%);
            border-bottom: 2px solid #2a2a2a;
        }

        .payment-header h1 {
            font-size: 2.5rem;
            color: #ffffff;
            margin-bottom: 0.5rem;
        }

        .payment-header p {
            color: #888;
            font-size: 1.1rem;
            max-width: 600px;
            margin: 0 auto;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }

        .section {
            margin-bottom: 3rem;
        }

        .section-header {
            display: flex;
            align-items: center;
            gap: 1rem;
            margin-bottom: 1.5rem;
            padding-bottom: 1rem;
            border-bottom: 2px solid #2a2a2a;
        }

        .section-header h2 {
            font-size: 1.8rem;
            color: #fff;
        }

        .section-badge {
            background: linear-gradient(135deg, #00ff00, #00cc00);
            color: #000;
            padding: 0.25rem 0.75rem;
            border-radius: 12px;
            font-size: 0.85rem;
            font-weight: 600;
        }

        .section-badge.crypto {
            background: linear-gradient(135deg, #f59e0b, #d97706);
        }

        .featured-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }

        .payment-methods-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 1rem;
        }

        .payment-card {
            background: #1a1a1a;
            border: 2px solid #2a2a2a;
            border-radius: 12px;
            padding: 1.5rem;
            transition: all 0.3s;
            cursor: pointer;
            text-decoration: none;
            color: inherit;
            display: block;
        }

        .payment-card:hover {
            border-color: #00ff00;
            transform: translateY(-5px);
            box-shadow: 0 8px 24px rgba(0, 255, 0, 0.2);
        }

        .payment-card.featured {
            background: linear-gradient(135deg, #1a1a1a, #2a2a2a);
            border-color: #00ff00;
            box-shadow: 0 4px 12px rgba(0, 255, 0, 0.1);
        }

        .payment-card.crypto:hover {
            border-color: #f59e0b;
            box-shadow: 0 8px 24px rgba(245, 158, 11, 0.2);
        }

        .payment-card.crypto.featured {
            border-color: #f59e0b;
            box-shadow: 0 4px 12px rgba(245, 158, 11, 0.1);
        }

        .payment-icon {
            font-size: 3rem;
            margin-bottom: 1rem;
            text-align: center;
        }

        .payment-name {
            font-size: 1.3rem;
            font-weight: 600;
            color: #fff;
            margin-bottom: 0.5rem;
            text-align: center;
        }

        .payment-description {
            color: #888;
            font-size: 0.95rem;
            text-align: center;
            margin-bottom: 1rem;
        }

        .payment-meta {
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
            padding-top: 1rem;
            border-top: 1px solid #2a2a2a;
        }

        .payment-meta-item {
            display: flex;
            justify-content: space-between;
            font-size: 0.85rem;
        }

        .payment-meta-label {
            color: #666;
        }

        .payment-meta-value {
            color: #00ff00;
            font-weight: 500;
        }

        .payment-card.crypto .payment-meta-value {
            color: #f59e0b;
        }

        .featured-badge {
            display: inline-block;
            background: linear-gradient(135deg, #00ff00, #00cc00);
            color: #000;
            padding: 0.25rem 0.75rem;
            border-radius: 12px;
            font-size: 0.75rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-top: 0.5rem;
        }

        .payment-card.crypto .featured-badge {
            background: linear-gradient(135deg, #f59e0b, #d97706);
        }

        .info-box {
            background: rgba(0, 170, 255, 0.1);
            border: 1px solid rgba(0, 170, 255, 0.3);
            border-radius: 8px;
            padding: 1rem;
            color: #00aaff;
            margin-bottom: 2rem;
        }

        .info-box h3 {
            margin-bottom: 0.5rem;
            font-size: 1.1rem;
        }

        .info-box p {
            font-size: 0.9rem;
            line-height: 1.5;
        }

        .cta-button {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            background: linear-gradient(135deg, #00ff00, #00cc00);
            color: #000;
            padding: 0.75rem 1.5rem;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s;
            border: none;
            cursor: pointer;
            font-size: 1rem;
            margin-top: 0.5rem;
            width: 100%;
            justify-content: center;
        }

        .cta-button:hover {
            background: linear-gradient(135deg, #00cc00, #00aa00);
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(0, 255, 0, 0.3);
        }

        .cta-button.crypto {
            background: linear-gradient(135deg, #f59e0b, #d97706);
        }

        .cta-button.crypto:hover {
            background: linear-gradient(135deg, #d97706, #b45309);
            box-shadow: 0 4px 12px rgba(245, 158, 11, 0.3);
        }

        .divider {
            height: 2px;
            background: linear-gradient(90deg, transparent, #2a2a2a, transparent);
            margin: 3rem 0;
        }

        @media (max-width: 768px) {
            .payment-header h1 {
                font-size: 2rem;
            }

            .container {
                padding: 1rem;
            }

            .featured-grid,
            .payment-methods-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="payment-header">
        <h1>💳 Payment Options</h1>
        <p>Choose your preferred payment method. We accept both traditional and cryptocurrency payments.</p>
    </div>

    <div class="container">
        <!-- Featured Fiat Payments -->
        <% if (featuredFiat.length > 0) { %>
        <section class="section">
            <div class="section-header">
                <h2>Featured Payment Methods</h2>
                <span class="section-badge">Recommended</span>
            </div>

            <div class="featured-grid">
                <% featuredFiat.forEach(method => { %>
                <a href="<%= method.link %>" class="payment-card featured" target="_blank" rel="noopener noreferrer">
                    <div class="payment-icon"><%= method.icon %></div>
                    <div class="payment-name"><%= method.name %></div>
                    <div class="payment-description"><%= method.description %></div>
                    <div class="payment-meta">
                        <% if (method.fees) { %>
                        <div class="payment-meta-item">
                            <span class="payment-meta-label">Fees:</span>
                            <span class="payment-meta-value"><%= method.fees %></span>
                        </div>
                        <% } %>
                    </div>
                    <button class="cta-button">Pay Now →</button>
                </a>
                <% }); %>
            </div>
        </section>
        <% } %>

        <!-- All Fiat Methods -->
        <% if (fiatMethods.length > featuredFiat.length) { %>
        <section class="section">
            <div class="section-header">
                <h2>More Payment Options</h2>
            </div>

            <div class="payment-methods-grid">
                <% fiatMethods.filter(m => !m.featured).forEach(method => { %>
                <% if (method.link && method.link !== '#') { %>
                <a href="<%= method.link %>" class="payment-card" target="_blank" rel="noopener noreferrer">
                <% } else { %>
                <div class="payment-card">
                <% } %>
                    <div class="payment-icon"><%= method.icon %></div>
                    <div class="payment-name"><%= method.name %></div>
                    <div class="payment-description"><%= method.description %></div>
                    <div class="payment-meta">
                        <% if (method.fees) { %>
                        <div class="payment-meta-item">
                            <span class="payment-meta-label">Fees:</span>
                            <span class="payment-meta-value"><%= method.fees %></span>
                        </div>
                        <% } %>
                        <% if (method.info) { %>
                        <div class="payment-meta-item" style="flex-direction: column; align-items: flex-start;">
                            <span class="payment-meta-label">Info:</span>
                            <span class="payment-meta-value"><%= method.info %></span>
                        </div>
                        <% } %>
                    </div>
                    <% if (method.link && method.link !== '#') { %>
                    <button class="cta-button">Pay Now →</button>
                    <% } %>
                <% if (method.link && method.link !== '#') { %>
                </a>
                <% } else { %>
                </div>
                <% } %>
                <% }); %>
            </div>
        </section>
        <% } %>

        <div class="divider"></div>

        <!-- Featured Crypto -->
        <% if (featuredCrypto.length > 0) { %>
        <section class="section">
            <div class="section-header">
                <h2>Cryptocurrency Payments</h2>
                <span class="section-badge crypto">Crypto</span>
            </div>

            <div class="info-box">
                <h3>🔐 Secure Crypto Payments</h3>
                <p>Send cryptocurrency directly to our wallet addresses. Transactions are verified on the blockchain and typically confirm within minutes.</p>
            </div>

            <div class="featured-grid">
                <% featuredCrypto.forEach(method => { %>
                <a href="/payments/method/crypto/<%= method.id %>" class="payment-card crypto featured">
                    <div class="payment-icon"><%= method.icon %></div>
                    <div class="payment-name"><%= method.name %></div>
                    <div class="payment-description"><%= method.description %></div>
                    <div class="payment-meta">
                        <div class="payment-meta-item">
                            <span class="payment-meta-label">Symbol:</span>
                            <span class="payment-meta-value"><%= method.symbol %></span>
                        </div>
                        <div class="payment-meta-item">
                            <span class="payment-meta-label">Network:</span>
                            <span class="payment-meta-value"><%= method.network %></span>
                        </div>
                    </div>
                    <button class="cta-button crypto">View Address →</button>
                </a>
                <% }); %>
            </div>
        </section>
        <% } %>

        <!-- All Crypto Methods -->
        <% if (cryptoMethods.length > featuredCrypto.length) { %>
        <section class="section">
            <div class="section-header">
                <h2>More Cryptocurrencies</h2>
            </div>

            <div class="payment-methods-grid">
                <% cryptoMethods.filter(m => !m.featured).forEach(method => { %>
                <a href="/payments/method/crypto/<%= method.id %>" class="payment-card crypto">
                    <div class="payment-icon"><%= method.icon %></div>
                    <div class="payment-name"><%= method.name %></div>
                    <div class="payment-description"><%= method.description %></div>
                    <div class="payment-meta">
                        <div class="payment-meta-item">
                            <span class="payment-meta-label">Symbol:</span>
                            <span class="payment-meta-value"><%= method.symbol %></span>
                        </div>
                        <div class="payment-meta-item">
                            <span class="payment-meta-label">Network:</span>
                            <span class="payment-meta-value"><%= method.network %></span>
                        </div>
                    </div>
                    <button class="cta-button crypto">View Address →</button>
                </a>
                <% }); %>
            </div>
        </section>
        <% } %>
    </div>

    <%-include('../mainContent/adminNav.ejs')%>
</body>
</html>

#!/usr/bin/env node
import app from '../app.js';
import { config } from '../config/config.js';

const port = config.PORT;
app.listen(port, '127.0.0.1', () => {
  console.log(`[media-hasher] running on http://127.0.0.1:${port}  (${config.NODE_ENV})`);
});

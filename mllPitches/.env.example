# Left Field — environment

# Server
PORT=3608
NODE_ENV=production

# Session signing — generate a long random string and commit to memory, not git
SESSION_SECRET=replace-me-with-a-long-random-string

# Google OAuth (admin login)
# 1. Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client ID (Web)
# 2. Authorized redirect URI: https://pitch.madladslab.com/auth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=https://pitch.madladslab.com/auth/google/callback

# Only this email can log into /admin
ADMIN_EMAIL=snoryder8019@gmail.com

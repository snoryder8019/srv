<%- include('../partials/head', { title }) %>
</div><!-- close .layout from head.ejs -->

<style>
/* ── Editor split layout ── */
.layout { display: none !important; }
.editor-layout { display: flex; height: 100vh; position: fixed; inset: 0; z-index: 60; }
.editor-panel { width: 340px; flex-shrink: 0; overflow-y: auto; background: var(--surface); border-right: 1px solid var(--border); padding: 0; }
.editor-preview { flex: 1; min-width: 320px; background: var(--ivory); display: flex; flex-direction: column; }
.editor-preview iframe { flex: 1; border: none; width: 100%; }

/* ── Accordion sections ── */
.editor-section { border-bottom: 1px solid var(--border); }
.editor-section-toggle {
  display: flex; align-items: center; justify-content: space-between;
  padding: 14px 20px; cursor: pointer; font-size: 0.72rem; font-weight: 600;
  letter-spacing: 0.14em; text-transform: uppercase; color: var(--slate);
  background: none; border: none; width: 100%; text-align: left;
  font-family: var(--font-body); transition: color 0.15s;
}
.editor-section-toggle:hover { color: var(--on-surface); }
.editor-section-body { display: none; padding: 0 20px 20px; }
.editor-section.open .editor-section-body { display: block; }
.editor-section.open .editor-section-toggle .arrow { transform: rotate(90deg); }
.arrow { transition: transform 0.2s; font-size: 0.6rem; }

/* ── Compact form controls ── */
.ep-field { margin-bottom: 10px; }
.ep-field label { display: block; font-size: 0.68rem; font-weight: 600; letter-spacing: 0.08em; text-transform: uppercase; color: var(--slate); margin-bottom: 4px; }
.ep-field input[type=text], .ep-field input[type=number], .ep-field textarea, .ep-field select {
  width: 100%; padding: 7px 8px; border: 1px solid var(--border); border-radius: 2px;
  font-family: var(--font-body); font-size: 0.78rem; color: var(--on-surface); background: var(--ivory);
  box-sizing: border-box;
}
.ep-field textarea { resize: vertical; min-height: 52px; }
.ep-field select { appearance: auto; -webkit-appearance: auto; }
.ep-hint { font-size: 0.62rem; color: var(--slate); margin-top: 2px; }
.ep-row { display: grid; grid-template-columns: 1fr 1fr; gap: 8px; }
.ep-row-3 { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 8px; }

/* ── Color picker compact ── */
.color-row { display: flex; gap: 6px; align-items: center; }
.color-row input[type=color] { width: 36px; height: 30px; flex-shrink: 0; padding: 1px; border: 1px solid var(--border); border-radius: 2px; cursor: pointer; }
.color-row input[type=text] { flex: 1; min-width: 0; padding: 6px 8px; font-size: 0.75rem; border: 1px solid var(--border); border-radius: 2px; color: var(--slate); background: var(--ivory); }

/* ── Swatch preview ── */
.swatch-row { display: flex; gap: 4px; flex-wrap: wrap; margin-top: 10px; }
.swatch { width: 40px; height: 26px; border-radius: 2px; flex-shrink: 0; }

/* ── Visibility grid ── */
.vis-grid-compact { display: grid; grid-template-columns: 1fr 1fr; gap: 6px; }
.vis-item { display: flex; align-items: center; gap: 6px; padding: 6px 8px; background: var(--ivory); border: 1px solid var(--border); border-radius: 2px; cursor: pointer; font-size: 0.72rem; color: var(--on-surface); }
.vis-item input { width: 14px; height: 14px; accent-color: var(--navy); flex-shrink: 0; }

/* ── Theme cards ── */
.theme-card { border: 1px solid var(--border); border-radius: 2px; overflow: hidden; background: var(--surface); margin-bottom: 8px; }
.theme-card-swatches { display: flex; height: 20px; }
.theme-card-swatches div { flex: 1; }
.theme-card-body { padding: 8px 10px; }
.theme-card-name { font-size: 0.75rem; font-weight: 600; color: var(--on-surface); }
.theme-card-meta { font-size: 0.6rem; color: var(--slate); margin-bottom: 6px; }
.theme-card-actions { display: flex; gap: 4px; }

/* ── Preview toolbar ── */
.preview-bar { padding: 8px 16px; background: var(--surface); border-bottom: 1px solid var(--border); display: flex; gap: 8px; align-items: center; }
.preview-bar .btn { padding: 4px 10px; font-size: 0.65rem; }

/* ── Success/error banners ── */
.save-banner { padding: 10px 20px; font-size: 0.78rem; font-weight: 500; }
.save-banner.success { background: rgba(21,128,61,0.08); color: var(--success, #15803D); border-bottom: 1px solid rgba(21,128,61,0.2); }
.save-banner.error { background: rgba(185,28,28,0.08); color: #B91C1C; border-bottom: 1px solid rgba(185,28,28,0.2); }

/* ── Agent btn ── */
.agent-btn {
  display: inline-flex; align-items: center; gap: 5px; padding: 4px 10px; border-radius: 2px;
  background: linear-gradient(135deg, #6B3FA0, #8B5FC0); color: var(--gold-light);
  border: 1px solid color-mix(in srgb, var(--gold) 30%, transparent);
  font-family: var(--font-body); font-size: 0.65rem; font-weight: 600;
  letter-spacing: 0.1em; text-transform: uppercase; cursor: pointer;
}
.agent-btn:hover { background: linear-gradient(135deg, #8B5FC0, #6B3FA0); }

/* ── Agent chat panel (matches blog/pages pattern) ── */
#agentPanel { position: fixed; top: 0; right: -440px; width: 420px; height: 100vh; height: 100dvh; background: var(--surface); border-left: 1px solid var(--border); box-shadow: -8px 0 40px rgba(15,27,48,0.12); z-index: 500; display: flex; flex-direction: column; transition: right 0.3s cubic-bezier(0.4,0,0.2,1); }
#agentPanel.open { right: 0; }
#agentOverlay { display: none; position: fixed; inset: 0; background: rgba(15,27,48,0.15); z-index: 499; }
#agentOverlay.active { display: block; }
.ap-header { padding: 18px 20px; border-bottom: 1px solid var(--border); background: linear-gradient(135deg, var(--navy-deep), var(--navy)); display: flex; align-items: center; justify-content: space-between; flex-shrink: 0; }
.ap-title { font-family: var(--font-heading); font-size: 1.2rem; font-weight: 400; color: var(--gold-light); display: flex; align-items: center; gap: 10px; }
.ap-dot { width: 8px; height: 8px; border-radius: 50%; background: #4ADE80; animation: apPulse 2s infinite; }
@keyframes apPulse { 0%,100%{opacity:1;} 50%{opacity:0.4;} }
.ap-close { background: none; border: none; color: rgba(253,252,250,0.5); font-size: 1.3rem; cursor: pointer; padding: 4px; transition: color 0.15s; }
.ap-close:hover { color: var(--gold-light); }
.ap-sub { padding: 8px 20px; background: rgba(201,168,72,0.07); border-bottom: 1px solid rgba(201,168,72,0.15); flex-shrink: 0; font-size: 0.7rem; color: var(--slate); letter-spacing: 0.04em; display: flex; align-items: center; gap: 6px; }
.ap-sub-dot { width: 5px; height: 5px; border-radius: 50%; background: #4ADE80; }
.ap-msgs { flex: 1; overflow-y: auto; -webkit-overflow-scrolling: touch; padding: 16px 20px; display: flex; flex-direction: column; gap: 12px; }
.ap-msg { display: flex; flex-direction: column; gap: 4px; }
.ap-msg.user { align-items: flex-end; }
.ap-msg.assistant { align-items: flex-start; }
.ap-bubble { max-width: 88%; padding: 10px 14px; border-radius: 4px; font-size: 0.84rem; line-height: 1.5; font-weight: 300; }
.ap-msg.user .ap-bubble { background: var(--navy); color: var(--gold-light); border-radius: 4px 4px 1px 4px; }
.ap-msg.assistant .ap-bubble { background: var(--ivory); color: var(--on-surface); border: 1px solid var(--border); border-radius: 4px 4px 4px 1px; }
.ap-typing { display: flex; gap: 4px; padding: 10px 14px; background: var(--ivory); border: 1px solid var(--border); border-radius: 4px 4px 4px 1px; align-self: flex-start; }
.ap-typing-dot { width: 7px; height: 7px; border-radius: 50%; background: var(--slate); animation: apBounce 1.2s infinite; }
.ap-typing-dot:nth-child(2) { animation-delay: 0.2s; }
.ap-typing-dot:nth-child(3) { animation-delay: 0.4s; }
@keyframes apBounce { 0%,60%,100%{transform:translateY(0);opacity:0.4;} 30%{transform:translateY(-5px);opacity:1;} }
.ap-fill-notice { font-size: 0.68rem; color: #15803D; font-weight: 600; letter-spacing: 0.06em; text-transform: uppercase; margin-top: 4px; }
.ap-search-notice { font-size: 0.68rem; color: var(--slate); font-style: italic; }
.ap-footer { padding: 14px 20px; padding-bottom: max(14px, env(safe-area-inset-bottom, 14px)); border-top: 1px solid var(--border); flex-shrink: 0; }
.ap-quick { display: flex; gap: 6px; margin-bottom: 10px; overflow-x: auto; -webkit-overflow-scrolling: touch; scrollbar-width: none; flex-wrap: nowrap; padding-bottom: 2px; }
.ap-quick::-webkit-scrollbar { display: none; }
.ap-quick-btn { font-size: 0.67rem; padding: 5px 10px; border-radius: 2px; border: 1px solid var(--border); background: var(--surface); color: var(--slate); cursor: pointer; font-family: var(--font-body); font-weight: 500; letter-spacing: 0.04em; transition: all 0.15s; white-space: nowrap; flex-shrink: 0; }
.ap-quick-btn:hover { border-color: var(--navy-mid); color: var(--navy); }
.ap-input-row { display: flex; gap: 8px; }
.ap-input { flex: 1; padding: 10px 14px; border: 1px solid var(--border); border-radius: 2px; font-family: var(--font-body); font-size: 0.88rem; font-weight: 300; color: var(--on-surface); outline: none; resize: none; transition: border-color 0.15s; min-height: 42px; max-height: 120px; }
.ap-input:focus { border-color: var(--navy); }
.ap-send { padding: 10px 16px; background: var(--navy); color: var(--gold-light); border: none; border-radius: 2px; cursor: pointer; font-size: 0.9rem; transition: background 0.15s; flex-shrink: 0; align-self: flex-end; }
.ap-send:hover { background: var(--navy-deep); }
.ap-send:disabled { opacity: 0.4; cursor: not-allowed; }
.ai-filled { background: rgba(21,128,61,0.07) !important; border-color: rgba(21,128,61,0.45) !important; transition: background 0.4s, border-color 0.4s; }

/* ── Brand image compact ── */
.brand-img-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 8px; }
.brand-img-slot { border: 1px solid var(--border); border-radius: 2px; overflow: hidden; }
.brand-img-slot img { width: 100%; height: 60px; object-fit: contain; background: var(--ivory); }
.brand-img-slot .lbl { padding: 4px 6px; font-size: 0.62rem; color: var(--slate); }

/* ── Repeater (add/remove rows in lists) ── */
.dp-repeater { display:flex; flex-direction:column; gap:10px; }
.dp-rep-item { padding:10px; border:1px solid var(--border); border-radius:3px; background:color-mix(in srgb, var(--ivory) 60%, white); position:relative; }
.dp-rep-head { display:flex; align-items:center; justify-content:space-between; margin-bottom:8px; font-size:0.62rem; font-weight:700; letter-spacing:0.14em; text-transform:uppercase; color:var(--slate); }
.dp-rep-actions { display:flex; gap:4px; }
.dp-rep-actions button { background:none; border:1px solid var(--border); width:24px; height:24px; border-radius:2px; cursor:pointer; font-size:0.78rem; color:var(--slate); padding:0; }
.dp-rep-actions button:hover:not(:disabled) { background:var(--ivory); color:var(--on-surface); }
.dp-rep-actions button:disabled { opacity:0.3; cursor:not-allowed; }
.dp-rep-remove { background:none; border:1px solid var(--border); width:24px; height:24px; border-radius:2px; cursor:pointer; font-size:0.85rem; color:#8B1C1C; padding:0; }
.dp-rep-remove:hover { background:rgba(139,28,28,0.08); }
.dp-rep-add { padding:7px 14px; font-size:0.65rem; font-weight:600; letter-spacing:0.1em; text-transform:uppercase; background:var(--ivory); color:var(--navy); border:1px dashed var(--navy-mid); border-radius:2px; cursor:pointer; transition:background 0.15s; align-self:flex-start; font-family:var(--font-body); }
.dp-rep-add:hover { background:color-mix(in srgb, var(--navy) 6%, white); }

/* ── Tab nav (top of left panel) ── */
.dp-tabs-wrap { position:sticky; top:0; z-index:5; display:flex; align-items:stretch; background:var(--surface); border-bottom:1px solid var(--border); }
.dp-tabs { display:flex; flex:1; min-width:0; overflow-x:auto; scrollbar-width:none; scroll-behavior:smooth; }
.dp-tabs::-webkit-scrollbar { display:none; }
/* Edge fades — signal there's more to scroll toward. Toggled by JS via wrap classes. */
.dp-tabs-wrap::before, .dp-tabs-wrap::after { content:""; position:absolute; top:0; bottom:0; width:28px; pointer-events:none; z-index:2; opacity:0; transition:opacity 0.15s; }
.dp-tabs-wrap::before { left:0; background:linear-gradient(to right, var(--surface), transparent); }
.dp-tabs-wrap::after  { right:0; background:linear-gradient(to left, var(--surface), transparent); }
.dp-tabs-wrap.dp-scroll-l::before { opacity:1; }
.dp-tabs-wrap.dp-scroll-r::after  { opacity:1; }
/* Scroll-arrow buttons — hidden unless that direction can scroll */
.dp-tab-nav { display:none; align-items:center; justify-content:center; width:26px; flex-shrink:0; background:var(--surface); border:none; color:var(--slate); cursor:pointer; font-size:1rem; line-height:1; z-index:3; transition:color 0.15s; }
.dp-tab-nav:hover { color:var(--navy); }
.dp-tabs-wrap.dp-scroll-l .dp-tab-nav-l { display:flex; }
.dp-tabs-wrap.dp-scroll-r .dp-tab-nav-r { display:flex; }
.dp-tab { flex:1; min-width:fit-content; padding:10px 12px; font-size:0.62rem; font-weight:700; letter-spacing:0.12em; text-transform:uppercase; color:var(--slate); background:none; border:none; border-bottom:2px solid transparent; cursor:pointer; transition:color 0.15s, border-color 0.15s; font-family:var(--font-body); white-space:nowrap; }
.dp-tab:hover { color:var(--on-surface); }
.dp-tab.active { color:var(--navy); border-bottom-color:var(--gold); }
.dp-tab .ico { display:inline-block; margin-right:5px; font-size:0.78rem; }
/* hide accordion sections not matching the active tab */
.editor-section[data-tab]:not(.dp-show) { display:none; }

/* ── Mobile: no gate — real responsive layout ── */
.mobile-gate { display: none; }

/* ════════════════════════════════════════════════════════
   RESPONSIVE — tablet / phone (≤820px)
   Strategy: unfix the editor, stack editor-panel above
   editor-preview, make preview collapsible via a toggle.
   ════════════════════════════════════════════════════════ */
@media (max-width: 820px) {
  /* Unfix the editor shell so it becomes normal flow */
  .editor-layout {
    position: static !important;
    display: flex !important;
    flex-direction: column !important;
    height: auto !important;
    min-height: 100dvh;
  }

  /* Left panel: full-width, natural height */
  .editor-panel {
    width: 100% !important;
    flex-shrink: unset !important;
    border-right: none !important;
    border-bottom: 1px solid var(--border);
    overflow-y: visible !important;
    /* stick the top header + tab strip when scrolling the panel */
  }

  /* Right (preview) panel: full-width below, collapsible */
  .editor-preview {
    width: 100% !important;
    min-width: 0 !important;
    flex: none !important;
    /* height is controlled by .dp-preview-open toggle */
    height: 0;
    overflow: hidden;
    transition: height 0.3s ease;
  }
  .editor-preview.dp-preview-open {
    height: 480px;
  }
  .editor-preview iframe {
    width: 100% !important;
    height: 100% !important;
  }

  /* Preview toggle bar — visible on mobile only */
  .dp-preview-toggle {
    display: flex !important;
  }

  /* Preview bar: wrap buttons, smaller text */
  .preview-bar {
    flex-wrap: wrap;
    gap: 6px;
    padding: 8px 12px;
  }
  .preview-bar .btn {
    font-size: 0.6rem;
    padding: 5px 8px;
  }
  .preview-bar span[style*="auto"] {
    display: none; /* hide "Live Preview" label to save space */
  }

  /* Tab nav: horizontal scroll (already scrollable, just ensure shrink) */
  .dp-tabs { overflow-x: auto; }
  .dp-tab { min-width: 52px; padding: 10px 8px; font-size: 0.58rem; }
  .dp-tab .ico { margin-right: 3px; font-size: 0.72rem; }

  /* Two-column ep-rows collapse to single column */
  .ep-row   { grid-template-columns: 1fr !important; }
  .ep-row-3 { grid-template-columns: 1fr 1fr !important; }

  /* Color row: keep swatch + text side by side, but allow text to shrink */
  .color-row input[type=text] { min-width: 0; font-size: 0.72rem; }

  /* Swatch preview — smaller swatches, allow wrap */
  .swatch-row { gap: 4px; }
  .swatch     { width: 32px; height: 22px; }

  /* Visibility grid — 2 col already, stays; on 520 goes 1 col */
  .vis-grid-compact { grid-template-columns: 1fr 1fr; }
  .vis-item { font-size: 0.7rem; padding: 7px 8px; min-height: 40px; }

  /* Brand image grid — 2 col, smaller thumbnails */
  .brand-img-grid { grid-template-columns: 1fr 1fr; }
  .brand-img-slot img { height: 50px; }

  /* Section body padding — reduce for mobile */
  .editor-section-body { padding: 0 14px 16px; }
  .editor-section-toggle { padding: 12px 14px; font-size: 0.68rem; }

  /* HF sub-tabs: allow wrap */
  .hf-tabs { flex-wrap: wrap !important; }
  .hf-tab  { padding: 8px 10px !important; font-size: 0.7rem !important; }

  /* Repeater items: tighter */
  .dp-rep-item { padding: 8px; }

  /* Agent panel: full-width slide-in from right, cap at viewport */
  #agentPanel {
    width: 100% !important;
    right: -100% !important;
    max-width: 420px;
  }
  #agentPanel.open { right: 0 !important; }

  /* Agent toggle: keep visible in preview bar even when preview is hidden */
  .agent-btn { font-size: 0.6rem; padding: 4px 8px; }

  /* Modal-like overlays spawned in this panel: full-width, capped height */
  /* (covers asset picker modals if they render inside the panel) */
}

/* ════════════════════════════════════════════════════════
   RESPONSIVE — phone (≤520px)
   ════════════════════════════════════════════════════════ */
@media (max-width: 520px) {
  .editor-layout { min-height: 100dvh; }

  /* Single-column everything at 520 */
  .ep-row   { grid-template-columns: 1fr !important; }
  .ep-row-3 { grid-template-columns: 1fr !important; }

  /* Visibility grid — 1 col at 520 */
  .vis-grid-compact { grid-template-columns: 1fr !important; }

  /* Footer col pieces grid — 1 col */
  .ep-row[style*="repeat(3"] { grid-template-columns: 1fr 1fr !important; }

  /* Brand image grid — 1 col at 520 */
  .brand-img-grid { grid-template-columns: 1fr !important; }

  /* Tab strip: even smaller, allow 2-line labels avoided by nowrap */
  .dp-tab { padding: 9px 7px; font-size: 0.56rem; letter-spacing: 0.06em; }

  /* Section toggle: tighter */
  .editor-section-toggle { padding: 11px 12px; font-size: 0.65rem; }
  .editor-section-body   { padding: 0 12px 14px; }

  /* Color row: swatch slightly smaller */
  .color-row input[type=color] { width: 32px; height: 28px; }

  /* Preview: taller on 520 since we need full-width */
  .editor-preview.dp-preview-open { height: 360px; }

  /* Agent panel: full viewport width */
  #agentPanel { max-width: 100% !important; }

  /* Swatch preview: smaller on 520 */
  .swatch-row { gap: 3px; }
  .swatch     { width: 28px; height: 20px; }

  /* HF sub-tabs: wrap text earlier */
  .hf-tab { padding: 7px 8px !important; font-size: 0.65rem !important; }

  /* ep-field inputs/textareas: ensure comfortable tap targets */
  .ep-field input[type=text],
  .ep-field input[type=number],
  .ep-field textarea,
  .ep-field select { font-size: 0.82rem; padding: 9px 10px; min-height: 40px; }

  /* Save All button: always visible and tappable */
  .editor-panel > form > div:first-child button[type=submit] {
    padding: 8px 16px !important;
    font-size: 0.65rem !important;
  }
}

/* ── Preview toggle bar (hidden on desktop, shown by JS on mobile) ── */
.dp-preview-toggle {
  display: none;
  align-items: center;
  justify-content: center;
  padding: 10px 16px;
  background: var(--surface);
  border-top: 1px solid var(--border);
  border-bottom: 1px solid var(--border);
  gap: 10px;
  position: sticky;
  bottom: 0;
  z-index: 10;
}
#dpPreviewBtn {
  flex: 1;
  max-width: 200px;
  padding: 9px 12px;
  font-size: 0.65rem;
  font-weight: 700;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  border: 1px solid var(--border);
  border-radius: 2px;
  background: var(--ivory);
  color: var(--navy);
  cursor: pointer;
  font-family: var(--font-body);
  transition: background 0.15s;
  min-height: 42px;
}
#dpPreviewBtn:hover { background: color-mix(in srgb, var(--navy) 6%, white); }
#dpPreviewBtn.active {
  background: var(--navy);
  color: var(--gold-light);
  border-color: var(--navy);
}
</style>

<div class="editor-layout">

<%# ═══════════ LEFT PANEL — FORM ═══════════ %>
<div class="editor-panel">
<form method="POST" action="/admin/design" id="designForm">

<div style="padding:10px 20px;border-bottom:1px solid var(--border);display:flex;align-items:center;justify-content:space-between;gap:8px;">
  <a href="/admin" style="font-size:0.72rem;color:var(--slate);text-decoration:none;">&larr; Admin</a>
  <span style="font-size:0.72rem;font-weight:600;letter-spacing:0.1em;text-transform:uppercase;color:var(--on-surface);flex:1;text-align:center;">Design & Content</span>
  <button type="submit" class="btn btn-navy" style="font-size:0.62rem;padding:6px 14px;letter-spacing:0.08em;white-space:nowrap;border-radius:2px;">Save All</button>
</div>
<% if (saved) { %><div class="save-banner success">Settings saved.</div><% } %>
<% if (error) { %><div class="save-banner error">Failed to save.</div><% } %>

<%# ─── Tab nav (consolidates Design / Sections / Templates / Brand) ─── %>
<div class="dp-tabs-wrap" id="dpTabsWrap">
  <button type="button" class="dp-tab-nav dp-tab-nav-l" aria-label="Scroll tabs left" tabindex="-1" onclick="dpScrollTabs(-1)">‹</button>
  <div class="dp-tabs" id="dpTabs">
    <button type="button" class="dp-tab" data-tab="design"    onclick="dpSwitch('design')"><span class="ico">◑</span>Design</button>
    <button type="button" class="dp-tab" data-tab="copy"      onclick="dpSwitch('copy')"><span class="ico">¶</span>Copy</button>
    <button type="button" class="dp-tab" data-tab="sections"  onclick="dpSwitch('sections')"><span class="ico">▦</span>Sections</button>
    <button type="button" class="dp-tab" data-tab="templates" onclick="dpSwitch('templates')"><span class="ico">◈</span>Templates</button>
    <button type="button" class="dp-tab" data-tab="brand"     onclick="dpSwitch('brand')"><span class="ico">⛌</span>Brand</button>
    <button type="button" class="dp-tab" data-tab="source"    onclick="dpSwitch('source')"><span class="ico">◐</span>Source</button>
  </div>
  <button type="button" class="dp-tab-nav dp-tab-nav-r" aria-label="Scroll tabs right" tabindex="-1" onclick="dpScrollTabs(1)">›</button>
</div>

<%# ─── Shared state used across multiple tabs ─── %>
<%
  const _sectionTemplates = typeof sectionTemplates !== 'undefined' ? sectionTemplates : {};
  const _validSections = (customSections || []).filter(s => s && s.type && _sectionTemplates[s.type]);
%>

<%# ─── 1. HOMEPAGE SOURCE — top-level switch ─── %>
<%
  // Two-value model: 'slab' (template-or-standard-layout) | 'custom' (bespoke EJS).
  // Legacy stored values (auto/layout/template) normalize for the checked radio.
  const _rawSrc = design.home_source || 'slab';
  const _src = (_rawSrc === 'custom' || (_rawSrc === 'auto' && hasCustomEjs)) ? 'custom' : 'slab';
  const _activeTpl = (templates || []).find(t => activeTemplateId && t._id.toString() === activeTemplateId);
  // Effective renderer — what actually displays at "/". 'custom' selected but no
  // EJS file falls back to slab rendering (the only fallback state now).
  let _effective;
  if (_src === 'custom') _effective = hasCustomEjs ? 'custom' : 'layout';
  else                   _effective = _activeTpl ? 'template' : 'layout';
  const _isFallback = (_src === 'custom' && !hasCustomEjs);
%>
<div class="editor-section open" data-tab="source">
  <button type="button" class="editor-section-toggle" onclick="toggleSection(this)">
    Homepage Source <span class="arrow">&#9654;</span>
  </button>
  <div class="editor-section-body">
    <%
      const _effLabel = _effective === 'template' ? ('Slab Template — ' + (_activeTpl ? _activeTpl.name : 'unknown'))
                      : _effective === 'custom' ? 'Custom EJS'
                      : 'Slab — Standard Layout';
      const _srcLabel = _src === 'custom' ? 'Custom' : 'Slab';
    %>
    <div style="margin-bottom:12px;padding:10px 14px;background:rgba(28,43,74,0.05);border-left:3px solid var(--navy);border-radius:2px;display:flex;justify-content:space-between;align-items:center;gap:12px;flex-wrap:wrap;">
      <div style="font-size:0.82rem;color:var(--on-surface);">
        <span style="font-size:0.62rem;font-weight:700;letter-spacing:0.14em;text-transform:uppercase;color:var(--slate);">Currently rendering at /</span><br>
        <strong style="color:var(--navy);"><%= _effLabel %></strong>
      </div>
      <div style="font-size:0.7rem;color:var(--slate);text-align:right;">
        Source setting: <strong style="color:var(--on-surface);"><%= _srcLabel %></strong>
        <% if (_isFallback) { %><br><span style="color:#8B1C1C;">(fallback — no custom EJS, rendering Slab)</span><% } %>
      </div>
    </div>
    <div class="ep-hint" style="margin-bottom:10px;">
      Pick what renders at <code style="font-size:0.7rem;background:var(--ivory);padding:1px 5px;border-radius:2px;">/</code>. The matching configuration section opens below.
    </div>

    <div style="display:flex;flex-direction:column;gap:6px;">
      <%
        const _options = [
          { val: 'slab',   title: 'Slab',   desc: _activeTpl
              ? ('Platform-rendered — currently the activated template <strong>' + _activeTpl.name + '</strong>. Deactivate it below to use the standard layout.')
              : 'Platform-rendered — the standard landing layout below (Classic, Bold, Minimal&hellip;). Activate a template to override it.' },
          { val: 'custom', title: 'Custom', desc: hasCustomEjs
              ? ('The bespoke EJS at <code>views/tenants/' + tenantSubdomain + '/home.ejs</code>')
              : 'Bespoke EJS file (not present for this tenant — falls back to Slab rendering)' },
        ];
      %>
      <% _options.forEach(o => { const checked = _src === o.val; %>
      <label class="vis-item" style="align-items:flex-start;padding:10px 12px;<%= checked ? 'border-color:var(--gold);box-shadow:0 0 0 1px var(--gold);' : '' %>">
        <input type="radio" name="home_source" value="<%= o.val %>" <%= checked ? 'checked' : '' %> style="margin-top:3px;flex-shrink:0;">
        <span style="display:flex;flex-direction:column;gap:2px;">
          <span style="font-weight:600;font-size:0.78rem;color:var(--on-surface);"><%= o.title %><% if (o.val === 'slab') { %> <span style="font-size:0.55rem;font-weight:600;letter-spacing:0.1em;color:var(--slate);">DEFAULT</span><% } %></span>
          <span style="font-size:0.68rem;color:var(--slate);line-height:1.4;font-weight:400;text-transform:none;letter-spacing:0;"><%- o.desc %></span>
        </span>
      </label>
      <% }) %>
    </div>

    <% if (_isFallback) { %>
    <div class="ep-hint" style="margin-top:10px;padding:8px 12px;background:rgba(139,28,28,0.06);border-left:3px solid #8B1C1C;border-radius:2px;color:#7A1818;">
      <strong>Fallback active:</strong> you selected <strong>Custom</strong> but no bespoke EJS exists, so <strong>Slab</strong> rendering is showing at <code style="font-size:0.7rem;background:rgba(255,255,255,0.6);padding:1px 5px;border-radius:2px;">/</code>.
      Create <code style="font-size:0.7rem;">views/tenants/<%= tenantSubdomain %>/home.ejs</code> to use a bespoke layout.
    </div>
    <% } %>
    <div class="ep-hint" style="margin-top:10px;">
      <strong>Tip:</strong> &ldquo;Custom&rdquo; uses CSS variables from this panel, so colors &amp; fonts you set here still apply to bespoke EJS files.
    </div>
  </div>
</div>

<%# ─── 1a. LAYOUT & TEMPLATE ─── %>
<div class="editor-section<%= (_src === 'layout' || _src === 'auto') ? ' open' : '' %>" data-tab="source">
  <button type="button" class="editor-section-toggle" onclick="toggleSection(this)">
    Standard Layout
    <span class="arrow">&#9654;</span>
  </button>
  <div class="editor-section-body">
    <div class="ep-field">
      <label>Landing Layout</label>
      <select name="landing_layout" id="field-landing_layout">
        <option value="classic"    <%= design.landing_layout === 'classic'    ? 'selected' : '' %>>Classic</option>
        <option value="bold"       <%= design.landing_layout === 'bold'       ? 'selected' : '' %>>Bold</option>
        <option value="minimal"    <%= design.landing_layout === 'minimal'    ? 'selected' : '' %>>Minimal</option>
        <option value="magazine"   <%= design.landing_layout === 'magazine'   ? 'selected' : '' %>>Magazine</option>
        <option value="dark"       <%= design.landing_layout === 'dark'       ? 'selected' : '' %>>Dark</option>
        <option value="startup"    <%= design.landing_layout === 'startup'    ? 'selected' : '' %>>Startup</option>
        <option value="showcase"   <%= design.landing_layout === 'showcase'   ? 'selected' : '' %>>Showcase</option>
        <option value="industrial" <%= design.landing_layout === 'industrial' ? 'selected' : '' %>>Industrial</option>
        <option value="split"      <%= design.landing_layout === 'split'      ? 'selected' : '' %>>Split</option>
        <option value="editorial"  <%= design.landing_layout === 'editorial'  ? 'selected' : '' %>>Editorial</option>
        <option value="agency"     <%= design.landing_layout === 'agency'     ? 'selected' : '' %>>Agency</option>
        <option value="clean"      <%= design.landing_layout === 'clean'      ? 'selected' : '' %>>Clean</option>
        <option value="brutalist"  <%= design.landing_layout === 'brutalist'  ? 'selected' : '' %>>Brutalist</option>
      </select>
    </div>
    <div class="ep-row">
      <div class="ep-field">
        <label>Animation</label>
        <select name="section_animation" id="field-section_animation">
          <option value="none" <%= design.section_animation === 'none' ? 'selected' : '' %>>None</option>
          <option value="fade" <%= design.section_animation === 'fade' ? 'selected' : '' %>>Fade up</option>
          <option value="slide" <%= design.section_animation === 'slide' ? 'selected' : '' %>>Slide in</option>
          <option value="zoom" <%= design.section_animation === 'zoom' ? 'selected' : '' %>>Zoom in</option>
          <option value="flip" <%= design.section_animation === 'flip' ? 'selected' : '' %>>Flip in</option>
          <option value="stagger" <%= design.section_animation === 'stagger' ? 'selected' : '' %>>Stagger</option>
          <option value="blur" <%= design.section_animation === 'blur' ? 'selected' : '' %>>Blur in</option>
        </select>
      </div>
      <div class="ep-field">
        <label style="margin-bottom:10px;">&nbsp;</label>
        <label style="font-size:0.75rem;text-transform:none;letter-spacing:0;display:flex;align-items:center;gap:6px;cursor:pointer;">
          <input type="checkbox" name="snap_enabled" id="field-snap_enabled" <%= design.snap_enabled === 'true' ? 'checked' : '' %> style="width:14px;height:14px;accent-color:var(--navy);"> Scroll snap
        </label>
      </div>
    </div>
    <div class="ep-row">
      <div class="ep-field">
        <label>Snap Strictness</label>
        <select name="snap_strictness" id="field-snap_strictness">
          <option value="proximity" <%= (design.snap_strictness || 'proximity') === 'proximity' ? 'selected' : '' %>>Proximity (gentle)</option>
          <option value="mandatory" <%= design.snap_strictness === 'mandatory' ? 'selected' : '' %>>Mandatory (hard lock)</option>
        </select>
        <div class="ep-hint">Only applies when Scroll snap is on.</div>
      </div>
    </div>
  </div>
</div>

<%# ─── 1b. TEMPLATE LIBRARY ─── %>
<div class="editor-section<%= _src === 'template' ? ' open' : '' %>" data-tab="templates">
  <button type="button" class="editor-section-toggle" onclick="toggleSection(this)">
    Template Library
    <span class="arrow">&#9654;</span>
  </button>
  <div class="editor-section-body">
    <div class="ep-hint" style="margin-bottom:10px;">
      <% if (_activeTpl) { %>
        Currently active: <strong style="color:var(--on-surface);"><%= _activeTpl.name %></strong>.
        Active templates override the landing layout above.
      <% } else { %>
        No template active &mdash; using the landing layout above. Activate a template to swap your homepage to a fully custom layout.
      <% } %>
    </div>

    <% if (!templates || templates.length === 0) { %>
      <div style="padding:14px;border:1px dashed var(--border);border-radius:2px;text-align:center;">
        <div style="font-size:0.78rem;color:var(--slate);margin-bottom:8px;">No templates yet.</div>
        <a href="/admin/templates/new" class="btn btn-navy" style="font-size:0.65rem;padding:5px 12px;">+ Build Template</a>
        <a href="/admin/template-store" class="btn btn-ghost" style="font-size:0.65rem;padding:5px 12px;margin-left:4px;">Browse Store</a>
      </div>
    <% } else { %>
      <div style="display:flex;flex-direction:column;gap:6px;">
        <% templates.forEach(tpl => {
             const isActive = activeTemplateId && tpl._id.toString() === activeTemplateId;
             const blocksCount = (tpl.blocks || []).length;
             const cat = tpl.category || 'landing';
        %>
          <div class="theme-card" style="<%= isActive ? 'border-color:var(--gold);box-shadow:0 0 0 1px var(--gold);' : '' %>">
            <div class="theme-card-body" style="display:flex;align-items:center;gap:8px;">
              <div style="flex:1;min-width:0;">
                <div class="theme-card-name" style="white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">
                  <%= tpl.name %>
                  <% if (isActive) { %><span style="font-size:0.55rem;font-weight:700;letter-spacing:0.12em;background:var(--gold);color:var(--navy-deep);padding:1px 5px;border-radius:2px;margin-left:4px;vertical-align:middle;">ACTIVE</span><% } %>
                </div>
                <div class="theme-card-meta">
                  <%= cat %> &middot; <%= blocksCount %> block<%= blocksCount === 1 ? '' : 's' %>
                  <% if (tpl.source === 'store') { %> &middot; from store<% } %>
                  <% if (tpl.source === 'migration') { %> &middot; built-in<% } %>
                </div>
              </div>
              <div class="theme-card-actions" style="gap:4px;">
                <a href="/admin/templates/<%= tpl._id %>/preview" target="_blank" class="btn btn-ghost" style="font-size:0.6rem;padding:3px 8px;letter-spacing:0.06em;">Preview</a>
                <a href="/admin/templates/<%= tpl._id %>/edit" class="btn btn-ghost" style="font-size:0.6rem;padding:3px 8px;letter-spacing:0.06em;">Edit</a>
                <% if (isActive) { %>
                  <button type="submit" form="tpl-deact-<%= tpl._id %>" class="btn btn-ghost" style="font-size:0.6rem;padding:3px 8px;letter-spacing:0.06em;color:var(--gold);">Deactivate</button>
                <% } else { %>
                  <button type="submit" form="tpl-act-<%= tpl._id %>" class="btn btn-navy" style="font-size:0.6rem;padding:3px 8px;letter-spacing:0.06em;">Activate</button>
                <% } %>
              </div>
            </div>
          </div>
        <% }) %>
      </div>
      <div style="margin-top:10px;display:flex;gap:6px;">
        <a href="/admin/templates" class="btn btn-ghost" style="font-size:0.62rem;padding:5px 10px;letter-spacing:0.08em;">Manage All</a>
        <a href="/admin/templates/new" class="btn btn-ghost" style="font-size:0.62rem;padding:5px 10px;letter-spacing:0.08em;">+ New</a>
        <a href="/admin/template-store" class="btn btn-ghost" style="font-size:0.62rem;padding:5px 10px;letter-spacing:0.08em;">Store</a>
      </div>
    <% } %>
  </div>
</div>

<%# ─── 1c. CUSTOM LAYOUT (only when relevant) ─── %>
<% if (hasCustomEjs || _src === 'custom') { %>
<div class="editor-section<%= _src === 'custom' ? ' open' : '' %>" data-tab="source">
  <button type="button" class="editor-section-toggle" onclick="toggleSection(this)">
    Custom Layout
    <span class="arrow">&#9654;</span>
  </button>
  <div class="editor-section-body">
    <% if (hasCustomEjs) { %>
      <div class="ep-hint" style="margin-bottom:8px;">
        Bespoke EJS file detected for this tenant:
        <code style="display:block;margin-top:4px;padding:6px 8px;background:var(--ivory);border:1px solid var(--border);border-radius:2px;font-size:0.7rem;color:var(--on-surface);">views/tenants/<%= tenantSubdomain %>/home.ejs</code>
      </div>
      <div class="ep-hint">
        Files in the tenant namespace pull colors &amp; fonts from this design panel via the <code>design.*</code> variables and shared CSS tokens (<code>--primary</code>, <code>--accent</code>, <code>--font-heading</code>&hellip;). To switch off the bespoke layout, change <strong>Homepage Source</strong> above.
      </div>
    <% } else { %>
      <div class="ep-hint">
        No bespoke EJS file present for <code><%= tenantSubdomain || '(no subdomain)' %></code>. Custom layouts require a developer to drop a file at <code>views/tenants/&lt;sub&gt;/home.ejs</code> &mdash; until then this option falls back to the standard layout.
      </div>
    <% } %>
  </div>
</div>
<% } %>

<%# ─── 2. COLORS ─── %>
<%
  // Essentials drive 95% of the look. Everything else is derived or rarely tweaked.
  const colorEssentials = [
    { name: 'color_primary', label: 'Primary',    hint: 'Brand color &mdash; nav, headings, buttons' },
    { name: 'color_accent',  label: 'Accent',     hint: 'CTAs, highlights, badges' },
    { name: 'color_bg',      label: 'Background', hint: 'Section backgrounds, page bg' },
  ];
  const colorAdvanced = [
    { name: 'color_primary_deep', label: 'Primary Deep',  hint: 'Hero bg, footer, deepest shade' },
    { name: 'color_primary_mid',  label: 'Primary Mid',   hint: 'Borders, hover states' },
    { name: 'color_accent_light', label: 'Accent Light',  hint: 'Text on dark bg' },
    { name: 'color_accent_2',     label: 'Accent 2',      hint: 'Secondary accent (multi-brand)' },
    { name: 'color_accent_3',     label: 'Accent 3',      hint: 'Tertiary accent (multi-brand)' },
    { name: 'color_dark',         label: 'Text Dark',     hint: 'Body text on light bg' },
    { name: 'color_white',        label: 'White / Surface', hint: 'Cards, page surface' },
    { name: 'color_muted',        label: 'Muted Text',    hint: 'Secondary text (auto if empty)' },
    { name: 'color_border',       label: 'Border',        hint: 'Dividers, card borders (auto if empty)' },
    { name: 'color_success',      label: 'Success',       hint: 'Success state' },
    { name: 'color_danger',       label: 'Danger',        hint: 'Error / danger state' },
  ];
  // For palette preview we want all six "primary" tokens in one row
  const palettePreview = ['color_primary','color_primary_deep','color_primary_mid','color_accent','color_accent_light','color_bg'];
%>
<div class="editor-section" data-tab="design">
  <button type="button" class="editor-section-toggle" onclick="toggleSection(this)">
    Colors <span class="arrow">&#9654;</span>
  </button>
  <div class="editor-section-body">
    <div class="ep-hint" style="margin-bottom:10px;">Three colors set the brand. Shades and contrast colors are auto-derived &mdash; tweak them under <em>Advanced</em> only if needed.</div>

    <% colorEssentials.forEach(cf => { %>
    <div class="ep-field">
      <label><%= cf.label %></label>
      <div class="color-row">
        <input type="color" name="<%= cf.name %>" id="field-<%= cf.name %>" value="<%= design[cf.name] %>">
        <input type="text" value="<%= design[cf.name] %>" readonly>
      </div>
      <% if (cf.hint) { %><div class="ep-hint"><%- cf.hint %></div><% } %>
    </div>
    <% }) %>

    <%# Palette preview swatch row %>
    <div style="font-size:0.6rem;font-weight:600;letter-spacing:0.12em;text-transform:uppercase;color:var(--slate);margin-top:12px;margin-bottom:6px;">Palette Preview</div>
    <div class="swatch-row" id="swatch-preview">
      <% palettePreview.forEach(k => { %>
      <div class="swatch" style="background:<%= design[k] %>;<%= k === 'color_bg' ? 'border:1px solid var(--border);' : '' %>" title="<%= k %>"></div>
      <% }) %>
    </div>

    <%# Advanced collapsible %>
    <details style="margin-top:14px;border-top:1px solid var(--border);padding-top:10px;">
      <summary style="cursor:pointer;font-size:0.62rem;font-weight:600;letter-spacing:0.12em;text-transform:uppercase;color:var(--slate);user-select:none;">Advanced shades &amp; states</summary>
      <div style="margin-top:10px;">
        <div class="ep-row">
        <% colorAdvanced.forEach((cf, i) => { %>
          <div class="ep-field">
            <label><%= cf.label %></label>
            <div class="color-row">
              <input type="color" name="<%= cf.name %>" id="field-<%= cf.name %>" value="<%= design[cf.name] || (cf.name === 'color_accent_2' || cf.name === 'color_accent_3' ? design.color_accent : (cf.name === 'color_muted' ? '#6B7380' : (cf.name === 'color_border' ? '#E5E7EB' : ''))) %>">
              <input type="text" value="<%= design[cf.name] || '' %>" placeholder="<%= cf.name === 'color_muted' || cf.name === 'color_border' ? '(auto)' : (cf.name.startsWith('color_accent_') ? '(primary)' : '') %>" readonly>
            </div>
          </div>
        <% }) %>
        </div>
      </div>
    </details>
  </div>
</div>

<%# ─── 3. TYPOGRAPHY ─── %>
<div class="editor-section" data-tab="design">
  <button type="button" class="editor-section-toggle" onclick="toggleSection(this)">
    Typography <span class="arrow">&#9654;</span>
  </button>
  <div class="editor-section-body">
    <style>
      .font-picker { position: relative; }
      .fp-current { display: flex; flex-direction: column; gap: 2px; padding: 8px 10px; border: 1px solid var(--border); border-radius: 3px; background: var(--surface); margin-bottom: 6px; }
      .fp-cur-name { font-size: 0.68rem; font-weight: 700; letter-spacing: 0.06em; text-transform: uppercase; color: var(--slate); }
      .fp-cur-preview { font-size: 1.35rem; line-height: 1.25; color: var(--on-surface); }
      .fp-search { width: 100%; padding: 7px 10px; border: 1px solid var(--border); border-radius: 3px; font-size: 0.82rem; background: var(--ivory); color: var(--on-surface); }
      .fp-results { position: absolute; left: 0; right: 0; z-index: 40; margin-top: 3px; max-height: 320px; overflow-y: auto; background: var(--surface); border: 1px solid var(--border); border-radius: 3px; box-shadow: 0 8px 28px rgba(15,27,48,0.18); }
      .fp-results[hidden] { display: none; }
      .fp-row { padding: 7px 12px; cursor: pointer; border-bottom: 1px solid var(--border); display: flex; align-items: baseline; gap: 10px; }
      .fp-row:last-child { border-bottom: none; }
      .fp-row:hover, .fp-row.active { background: rgba(201,168,72,0.10); }
      .fp-row-name { font-size: 0.66rem; color: var(--slate); width: 130px; flex-shrink: 0; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
      .fp-row-sample { font-size: 1.1rem; color: var(--on-surface); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
      .fp-note { font-size: 0.66rem; color: var(--slate); margin-top: 4px; }
      .fp-note.err { color: #B91C1C; }
    </style>
    <div class="ep-field font-picker" data-target="font_heading">
      <label>Heading Font</label>
      <input type="hidden" name="font_heading" id="fp-val-font_heading" value="<%= design.font_heading || 'Cormorant Garamond' %>">
      <input type="hidden" name="font_heading_spec" id="fp-spec-font_heading" value="<%= design.font_heading_spec || '' %>">
      <div class="fp-current">
        <span class="fp-cur-name" id="fp-curname-font_heading"><%= design.font_heading || 'Cormorant Garamond' %></span>
        <span class="fp-cur-preview" id="fp-curprev-font_heading">Ag — The quick brown fox</span>
      </div>
      <input type="text" class="fp-search" data-target="font_heading" placeholder="Search all Google Fonts…" autocomplete="off">
      <div class="fp-results" data-target="font_heading" hidden></div>
      <div class="fp-note" id="fp-note-font_heading"></div>
    </div>
    <div class="ep-field font-picker" data-target="font_body">
      <label>Body Font</label>
      <input type="hidden" name="font_body" id="fp-val-font_body" value="<%= design.font_body || 'Jost' %>">
      <input type="hidden" name="font_body_spec" id="fp-spec-font_body" value="<%= design.font_body_spec || '' %>">
      <div class="fp-current">
        <span class="fp-cur-name" id="fp-curname-font_body"><%= design.font_body || 'Jost' %></span>
        <span class="fp-cur-preview" id="fp-curprev-font_body">Ag — The quick brown fox</span>
      </div>
      <input type="text" class="fp-search" data-target="font_body" placeholder="Search all Google Fonts…" autocomplete="off">
      <div class="fp-results" data-target="font_body" hidden></div>
      <div class="fp-note" id="fp-note-font_body"></div>
    </div>
  </div>
</div>

<script>
(function () {
  'use strict';
  var catalog = [];
  var loaded = new Set();       // specs already injected as <link>
  var resultsLink = {};         // per-target batch preview <link>

  function bareFamily(n) { return String(n || '').trim().replace(/\s+/g, '+'); }

  // Build a CSS2 `family=` fragment from a font's real variants (matches server).
  function buildSpec(family, variants) {
    var fam = bareFamily(family);
    var up = new Set(), it = new Set();
    (variants || []).forEach(function (v) {
      if (v === 'regular') up.add(400);
      else if (v === 'italic') it.add(400);
      else if (/^\d+$/.test(v)) up.add(parseInt(v, 10));
      else { var m = /^(\d+)italic$/.exec(v); if (m) it.add(parseInt(m[1], 10)); }
    });
    var ups = [...up].sort(function (a, b) { return a - b; });
    var its = [...it].sort(function (a, b) { return a - b; });
    if (!ups.length && !its.length) return fam;
    if (!its.length) return (ups.length === 1 && ups[0] === 400) ? fam : fam + ':wght@' + ups.join(';');
    var parts = [];
    ups.forEach(function (w) { parts.push('0,' + w); });
    its.forEach(function (w) { parts.push('1,' + w); });
    return fam + ':ital,wght@' + parts.join(';');
  }

  // Inject a stylesheet <link> for one font spec (deduped) so previews render.
  function loadFontLink(spec) {
    if (!spec || loaded.has(spec)) return;
    loaded.add(spec);
    var link = document.createElement('link');
    link.rel = 'stylesheet';
    link.href = 'https://fonts.googleapis.com/css2?family=' + spec + '&display=swap';
    document.head.appendChild(link);
  }

  // Batch-load the visible result families (regular weight) for row previews.
  function loadResultsPreview(target, families) {
    if (!families.length) return;
    var href = 'https://fonts.googleapis.com/css2?' + families.map(function (f) { return 'family=' + bareFamily(f); }).join('&') + '&display=swap';
    var link = resultsLink[target];
    if (!link) { link = document.createElement('link'); link.rel = 'stylesheet'; document.head.appendChild(link); resultsLink[target] = link; }
    link.href = href;
  }

  function setCurrent(target, family, spec) {
    document.getElementById('fp-val-' + target).value = family;
    document.getElementById('fp-spec-' + target).value = spec || '';
    document.getElementById('fp-curname-' + target).textContent = family;
    var prev = document.getElementById('fp-curprev-' + target);
    prev.style.fontFamily = "'" + family + "'";
    loadFontLink(spec || bareFamily(family));
    // Apply the new font to the live preview WITHOUT reloading (a reload would
    // snap the iframe back to the last-saved font). Marks the preview dirty so
    // the override engine injects the change.
    window._previewDirty = true;
    if (typeof window.debouncedPush === 'function') { try { window.debouncedPush(); } catch (e) {} }
  }

  function renderResults(target, query) {
    var box = document.querySelector('.fp-results[data-target="' + target + '"]');
    var q = (query || '').trim().toLowerCase();
    var list = q ? catalog.filter(function (f) { return f.family.toLowerCase().indexOf(q) !== -1; }) : catalog;
    list = list.slice(0, 60);
    if (!list.length) { box.innerHTML = '<div class="fp-row"><span class="fp-row-sample">No matches</span></div>'; box.hidden = false; return; }
    loadResultsPreview(target, list.map(function (f) { return f.family; }));
    box.innerHTML = list.map(function (f) {
      return '<div class="fp-row" data-family="' + f.family.replace(/"/g, '&quot;') + '">' +
        '<span class="fp-row-name">' + f.family + '</span>' +
        '<span class="fp-row-sample" style="font-family:\'' + f.family.replace(/'/g, '') + "'\">Ag The quick brown fox</span></div>";
    }).join('');
    box.querySelectorAll('.fp-row').forEach(function (row) {
      row.addEventListener('mousedown', function (e) {
        e.preventDefault(); // keep focus / stop blur closing before click
        var fam = row.getAttribute('data-family');
        var entry = catalog.find(function (c) { return c.family === fam; });
        setCurrent(target, fam, entry ? buildSpec(fam, entry.variants) : bareFamily(fam));
        box.hidden = true;
        var input = document.querySelector('.fp-search[data-target="' + target + '"]');
        if (input) input.value = '';
      });
    });
    box.hidden = false;
  }

  function wirePicker(target) {
    var input = document.querySelector('.fp-search[data-target="' + target + '"]');
    var box = document.querySelector('.fp-results[data-target="' + target + '"]');
    if (!input) return;
    input.addEventListener('focus', function () { renderResults(target, input.value); });
    input.addEventListener('input', function () { renderResults(target, input.value); });
    input.addEventListener('blur', function () { setTimeout(function () { box.hidden = true; }, 150); });
    // Typing an exact family + Enter works even when the catalog can't load.
    input.addEventListener('keydown', function (e) {
      if (e.key === 'Enter') {
        e.preventDefault();
        var typed = input.value.trim();
        if (!typed) return;
        var entry = catalog.find(function (c) { return c.family.toLowerCase() === typed.toLowerCase(); });
        var fam = entry ? entry.family : typed;
        setCurrent(target, fam, entry ? buildSpec(fam, entry.variants) : bareFamily(fam));
        box.hidden = true; input.value = '';
      }
    });
  }

  function init() {
    ['font_heading', 'font_body'].forEach(function (target) {
      // Seed the current preview using the already-saved spec (or bare family).
      var fam = document.getElementById('fp-val-' + target).value;
      var spec = document.getElementById('fp-spec-' + target).value;
      var prev = document.getElementById('fp-curprev-' + target);
      prev.style.fontFamily = "'" + fam + "'";
      loadFontLink(spec || bareFamily(fam));
      wirePicker(target);
    });
    fetch('/admin/design/fonts').then(function (r) { return r.json(); }).then(function (data) {
      if (data.ok && data.fonts) { catalog = data.fonts; }
      else {
        var msg = (data.error || 'Font catalog unavailable') + ' — you can still type any Google font name and press Enter.';
        ['font_heading', 'font_body'].forEach(function (t) { var n = document.getElementById('fp-note-' + t); if (n) { n.textContent = msg; n.classList.add('err'); } });
      }
    }).catch(function () {
      ['font_heading', 'font_body'].forEach(function (t) { var n = document.getElementById('fp-note-' + t); if (n) { n.textContent = 'Could not load font catalog — type a font name and press Enter.'; n.classList.add('err'); } });
    });
  }

  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', init);
  else init();
})();
</script>

<%# ─── 4. SECTION VISIBILITY ─── %>
<div class="editor-section" data-tab="sections">
  <button type="button" class="editor-section-toggle" onclick="toggleSection(this)">
    Section Visibility <span class="arrow">&#9654;</span>
  </button>
  <div class="editor-section-body">
    <%
      // vis_header + vis_footer live in the consolidated Header & Footer section
      const visSections = [
        { key: 'vis_hero',      label: 'Hero' },
        { key: 'vis_marquee',   label: 'Ticker / Marquee' },
        { key: 'vis_services',  label: 'Services' },
        { key: 'vis_portfolio', label: 'Portfolio' },
        { key: 'vis_about',     label: 'About' },
        { key: 'vis_process',   label: 'Process' },
        { key: 'vis_pricing',   label: 'Pricing' },
        { key: 'vis_reviews',   label: 'Reviews' },
        { key: 'vis_contact',   label: 'Contact' },
        { key: 'vis_blog',      label: 'Blog' },
        { key: 'vis_careers',   label: 'Careers' },
        { key: 'vis_admin_link',label: 'Admin Login Link' },
        { key: 'vis_qr',        label: 'QR Code' },
      ];
    %>
    <div class="vis-grid-compact">
      <% visSections.forEach(s => { %>
      <label class="vis-item">
        <input type="checkbox" name="<%= s.key %>" id="field-<%= s.key %>"
          <%= (s.key === 'vis_blog' || s.key === 'vis_qr' || s.key === 'vis_careers') ? (design[s.key] === 'true' ? 'checked' : '') : (design[s.key] !== 'false' ? 'checked' : '') %>>
        <%= s.label %>
      </label>
      <% }) %>
    </div>

    <% if (_validSections.length) { %>
    <div style="margin-top:16px;padding-top:12px;border-top:1px solid var(--border);">
      <div style="font-size:0.62rem;font-weight:700;letter-spacing:0.14em;text-transform:uppercase;color:var(--slate);margin-bottom:8px;">Custom Sections · Order &amp; Visibility</div>
      <div class="ep-hint" style="margin-bottom:8px;">Drag arrows to reorder. Toggle the eye to hide a section from the homepage.</div>
      <div id="cs-order-list" style="display:flex;flex-direction:column;gap:4px;">
        <% _validSections.forEach(function(sec, i) { %>
          <div class="cs-row" data-cs-id="<%= sec._id %>" style="display:flex;align-items:center;gap:6px;padding:6px 8px;border:1px solid var(--border);border-radius:2px;background:var(--surface);">
            <span style="font-size:0.7rem;color:var(--slate);min-width:16px;text-align:center;" data-cs-index="<%= i + 1 %>"><%= i + 1 %></span>
            <span style="flex:1;min-width:0;font-size:0.78rem;color:var(--on-surface);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;"><%= sec.label || 'Untitled' %></span>
            <button type="button" title="Move up" onclick="csMove('<%= sec._id %>', -1)" style="background:none;border:1px solid var(--border);width:22px;height:22px;border-radius:2px;cursor:pointer;font-size:0.7rem;color:var(--slate);"<%= i === 0 ? ' disabled' : '' %>>↑</button>
            <button type="button" title="Move down" onclick="csMove('<%= sec._id %>', 1)" style="background:none;border:1px solid var(--border);width:22px;height:22px;border-radius:2px;cursor:pointer;font-size:0.7rem;color:var(--slate);"<%= i === _validSections.length - 1 ? ' disabled' : '' %>>↓</button>
            <button type="button" class="cs-vis-toggle" data-cs-id="<%= sec._id %>" data-visible="<%= sec.visible !== false ? 'true' : 'false' %>" onclick="csToggleVisible('<%= sec._id %>', this)" title="<%= sec.visible !== false ? 'Hide section' : 'Show section' %>" style="background:none;border:1px solid var(--border);width:30px;height:22px;border-radius:2px;cursor:pointer;font-size:0.72rem;color:<%= sec.visible !== false ? 'var(--navy)' : 'var(--slate)' %>;"><%= sec.visible !== false ? '◉' : '○' %></button>
          </div>
        <% }); %>
      </div>
    </div>
    <% } %>
  </div>
</div>

<%- include('_ticker-section') %>

<%# ─── COOKIE CONSENT ─── %>
<div class="editor-section" data-tab="design">
  <button type="button" class="editor-section-toggle" onclick="toggleSection(this)">
    Cookie Consent <span class="arrow">&#9654;</span>
  </button>
  <div class="editor-section-body">
    <div class="ep-hint" style="margin-bottom:10px;">GDPR-style consent shown to visitors. Categories (Analytics, Marketing) gate any tags wrapped as <code>&lt;script type="text/plain" data-consent="analytics"&gt;</code>. Button text &amp; category descriptions are edited in the <strong>Copy</strong> editor &rsaquo; Cookie.</div>

    <div class="ep-field">
      <label style="font-size:0.75rem;text-transform:none;letter-spacing:0;display:flex;align-items:center;gap:6px;cursor:pointer;">
        <input type="checkbox" name="cookie_consent_enabled" id="field-cookie_consent_enabled" <%= design.cookie_consent_enabled === 'true' ? 'checked' : '' %> style="width:14px;height:14px;accent-color:var(--navy);"> Enable cookie consent
      </label>
    </div>

    <div class="ep-row">
      <div class="ep-field">
        <label>Style</label>
        <select name="cookie_consent_style" id="field-cookie_consent_style">
          <option value="modal"  <%= (design.cookie_consent_style || 'modal') === 'modal'  ? 'selected' : '' %>>Modal (centered dialog)</option>
          <option value="banner" <%= design.cookie_consent_style === 'banner' ? 'selected' : '' %>>Banner (docked bar)</option>
        </select>
      </div>
      <div class="ep-field">
        <label>Position</label>
        <select name="cookie_consent_position" id="field-cookie_consent_position">
          <option value="bottom"      <%= (design.cookie_consent_position || 'bottom') === 'bottom'      ? 'selected' : '' %>>Bottom</option>
          <option value="bottom-left" <%= design.cookie_consent_position === 'bottom-left' ? 'selected' : '' %>>Bottom-left</option>
          <option value="center"      <%= design.cookie_consent_position === 'center'      ? 'selected' : '' %>>Center</option>
        </select>
      </div>
    </div>

    <div class="ep-row-3">
      <div class="ep-field">
        <label>Background</label>
        <div class="color-row">
          <input type="color" value="<%= design.cookie_bg || design.color_white || '#ffffff' %>" onchange="this.nextElementSibling.value=this.value" oninput="this.nextElementSibling.value=this.value">
          <input type="text" name="cookie_bg" value="<%= design.cookie_bg || '' %>" placeholder="(surface)">
        </div>
      </div>
      <div class="ep-field">
        <label>Text Color</label>
        <div class="color-row">
          <input type="color" value="<%= design.cookie_text_color || design.color_dark || '#0f1b30' %>" onchange="this.nextElementSibling.value=this.value" oninput="this.nextElementSibling.value=this.value">
          <input type="text" name="cookie_text_color" value="<%= design.cookie_text_color || '' %>" placeholder="(on-surface)">
        </div>
      </div>
      <div class="ep-field">
        <label>Accent</label>
        <div class="color-row">
          <input type="color" value="<%= design.cookie_accent || design.color_accent || '#c9a848' %>" onchange="this.nextElementSibling.value=this.value" oninput="this.nextElementSibling.value=this.value">
          <input type="text" name="cookie_accent" value="<%= design.cookie_accent || '' %>" placeholder="(accent)">
        </div>
      </div>
    </div>
  </div>
</div>

<%# ─── 4b. HEADER & FOOTER (consolidated) ─── %>
<%# All header & footer settings live here: visibility, logo, CTA, nav colors, custom links. %>
<%# Link create/edit/delete use HTML5 form-attribute references to standalone forms declared outside designForm. %>
<% const d = design; %>
<div class="editor-section" id="links" data-tab="sections">
  <button type="button" class="editor-section-toggle" onclick="toggleSection(this)">
    Header &amp; Footer <span class="arrow">&#9654;</span>
  </button>
  <div class="editor-section-body">

    <%# Sub-tabs — Header / Footer / Colors / Links %>
    <div class="ep-hint" style="margin-bottom:6px;font-style:italic;">Switch between Header, Footer, Colors, and Links settings using the tabs below.</div>
    <div class="hf-tabs" style="display:flex;gap:4px;border-bottom:1px solid var(--border);margin-bottom:14px;flex-wrap:wrap;">
      <button type="button" class="hf-tab is-active" data-tab="header"  onclick="hfTab(this)" style="background:none;border:none;padding:10px 16px;font-size:0.78rem;letter-spacing:0.08em;text-transform:uppercase;color:var(--on-surface);font-weight:700;cursor:pointer;border-bottom:3px solid var(--gold);font-family:var(--font-body);">Header</button>
      <button type="button" class="hf-tab"           data-tab="footer"  onclick="hfTab(this)" style="background:none;border:none;padding:10px 16px;font-size:0.78rem;letter-spacing:0.08em;text-transform:uppercase;color:var(--slate);font-weight:700;cursor:pointer;border-bottom:3px solid transparent;font-family:var(--font-body);">Footer</button>
      <button type="button" class="hf-tab"           data-tab="colors"  onclick="hfTab(this)" style="background:none;border:none;padding:10px 16px;font-size:0.78rem;letter-spacing:0.08em;text-transform:uppercase;color:var(--slate);font-weight:700;cursor:pointer;border-bottom:3px solid transparent;font-family:var(--font-body);">Colors</button>
      <button type="button" class="hf-tab"           data-tab="links"   onclick="hfTab(this)" style="background:none;border:none;padding:10px 16px;font-size:0.78rem;letter-spacing:0.08em;text-transform:uppercase;color:var(--slate);font-weight:700;cursor:pointer;border-bottom:3px solid transparent;font-family:var(--font-body);">Links</button>
    </div>

    <%# ── TAB: Header (layout + typography + CTA) ── %>
    <div class="hf-pane" data-pane="header">
      <div class="ep-row">
        <label class="vis-item"><input type="checkbox" name="vis_header" <%= d.vis_header !== 'false' ? 'checked' : '' %>> Show Header</label>
        <label class="vis-item"><input type="checkbox" name="vis_footer" <%= d.vis_footer !== 'false' ? 'checked' : '' %>> Show Footer</label>
      </div>

      <div style="font-size:0.62rem;font-weight:600;letter-spacing:0.1em;color:var(--slate);margin:14px 0 6px;">BUILT-IN NAV LINKS</div>
      <div class="ep-hint" style="margin-bottom:8px;">Toggle the always-on header links. CMS pages &amp; custom links are managed in the <strong>Links</strong> tab.</div>
      <div class="vis-grid-compact">
        <label class="vis-item"><input type="checkbox" name="vis_nav_home" <%= d.vis_nav_home !== 'false' ? 'checked' : '' %>> Home link</label>
        <label class="vis-item"><input type="checkbox" name="vis_nav_blog" <%= d.vis_nav_blog !== 'false' ? 'checked' : '' %>> Blog link</label>
        <label class="vis-item"><input type="checkbox" name="vis_nav_cta" <%= d.vis_nav_cta !== 'false' ? 'checked' : '' %>> CTA button</label>
      </div>

      <div style="font-size:0.62rem;font-weight:600;letter-spacing:0.1em;color:var(--slate);margin:14px 0 6px;">STRUCTURE</div>
      <div class="ep-hint" style="margin-bottom:8px;">Picks the overall header arrangement &mdash; independent of the page&rsquo;s landing layout.</div>
      <div class="ep-row-3">
        <div class="ep-field">
          <label>Header Layout</label>
          <select name="header_layout" onchange="previewReload()">
            <option value="standard" <%= (d.header_layout || 'standard') === 'standard' ? 'selected' : '' %>>Standard &mdash; brand left, links right</option>
            <option value="compact"  <%= d.header_layout === 'compact'  ? 'selected' : '' %>>Compact &mdash; reduced padding</option>
            <option value="spacious" <%= d.header_layout === 'spacious' ? 'selected' : '' %>>Spacious &mdash; tall &amp; airy</option>
            <option value="centered" <%= d.header_layout === 'centered' ? 'selected' : '' %>>Centered &mdash; brand top, links below</option>
            <option value="split"    <%= d.header_layout === 'split'    ? 'selected' : '' %>>Split &mdash; links flank centered brand</option>
          </select>
        </div>
        <div class="ep-field">
          <label>Vertical Padding (px) <span class="ep-hint" style="display:inline;font-style:italic;"><%= d.header_padding_y || '20' %></span></label>
          <input type="range" name="header_padding_y" min="6" max="60" step="1" value="<%= d.header_padding_y || '20' %>" oninput="this.previousElementSibling.querySelector('span').textContent=this.value" onchange="previewReload()">
        </div>
        <div class="ep-field">
          <label>Horizontal Padding (px) <span class="ep-hint" style="display:inline;font-style:italic;"><%= d.header_padding_x || '52' %></span></label>
          <input type="range" name="header_padding_x" min="8" max="96" step="2" value="<%= d.header_padding_x || '52' %>" oninput="this.previousElementSibling.querySelector('span').textContent=this.value" onchange="previewReload()">
        </div>
      </div>
      <div class="ep-row-3">
        <div class="ep-field">
          <label>Sticky</label>
          <select name="header_sticky" onchange="previewReload()">
            <option value="true"  <%= d.header_sticky !== 'false' ? 'selected' : '' %>>Yes &mdash; fixed to top</option>
            <option value="false" <%= d.header_sticky === 'false' ? 'selected' : '' %>>No &mdash; scrolls with page</option>
          </select>
        </div>
        <div class="ep-field">
          <label>Backdrop Blur</label>
          <select name="header_blur" onchange="previewReload()">
            <option value="true"  <%= d.header_blur !== 'false' ? 'selected' : '' %>>Yes</option>
            <option value="false" <%= d.header_blur === 'false' ? 'selected' : '' %>>No</option>
          </select>
        </div>
        <div class="ep-field">
          <label>Shadow on Scroll</label>
          <select name="header_shadow" onchange="previewReload()">
            <option value="true"  <%= d.header_shadow !== 'false' ? 'selected' : '' %>>Yes</option>
            <option value="false" <%= d.header_shadow === 'false' ? 'selected' : '' %>>No</option>
          </select>
        </div>
      </div>

      <div style="font-size:0.62rem;font-weight:600;letter-spacing:0.1em;color:var(--slate);margin:14px 0 6px;">LOGO</div>
      <div class="ep-hint" style="margin-bottom:6px;">Upload or replace logo images on the <a href="#" onclick="dpSwitch('brand');document.getElementById('brand-images')?.scrollIntoView({behavior:'smooth'});return false;" style="color:var(--navy);font-weight:600;">Brand tab →</a></div>
      <div class="ep-row">
        <div class="ep-field">
          <label>Logo Display</label>
          <select name="nav_logo_display" onchange="previewReload()">
            <option value="text"  <%= d.nav_logo_display === 'text'  ? 'selected' : '' %>>Text Only</option>
            <option value="image" <%= d.nav_logo_display === 'image' ? 'selected' : '' %>>Image Only</option>
            <option value="both"  <%= d.nav_logo_display === 'both'  ? 'selected' : '' %>>Both</option>
          </select>
        </div>
        <div class="ep-field">
          <label>Show Tagline</label>
          <select name="nav_tagline_display" onchange="previewReload()">
            <option value="true"  <%= d.nav_tagline_display !== 'false' ? 'selected' : '' %>>Yes</option>
            <option value="false" <%= d.nav_tagline_display === 'false' ? 'selected' : '' %>>No</option>
          </select>
        </div>
      </div>
      <div class="ep-row">
        <div class="ep-field">
          <label>Logo Image Size (px) <span class="ep-hint" style="display:inline;font-style:italic;"><%= d.nav_logo_size || '48' %></span></label>
          <input type="range" name="nav_logo_size" min="24" max="180" step="2" value="<%= d.nav_logo_size || '48' %>" oninput="this.previousElementSibling.querySelector('span').textContent=this.value" onchange="previewReload()">
          <div class="ep-hint">Height of the uploaded logo. Push it past the bar height to oversize it.</div>
        </div>
        <div class="ep-field">
          <label>Logo + Text Layout</label>
          <select name="nav_logo_layout" onchange="previewReload()">
            <option value="stack"          <%= (d.nav_logo_layout || 'stack') === 'stack'          ? 'selected' : '' %>>Stacked &mdash; logo above text</option>
            <option value="inline"         <%= d.nav_logo_layout === 'inline'         ? 'selected' : '' %>>Inline &mdash; logo left of text</option>
            <option value="inline-reverse" <%= d.nav_logo_layout === 'inline-reverse' ? 'selected' : '' %>>Inline &mdash; logo right of text</option>
            <option value="overlap"        <%= d.nav_logo_layout === 'overlap'        ? 'selected' : '' %>>Overlap &mdash; text over logo</option>
          </select>
          <div class="ep-hint">Applies when Logo Display is <strong>Both</strong>.</div>
        </div>
      </div>
      <div class="ep-row" id="nav-logo-overlap-row" style="<%= d.nav_logo_layout === 'overlap' ? '' : 'display:none;' %>">
        <div class="ep-field">
          <label>Overlap Nudge X (px) <span class="ep-hint" style="display:inline;font-style:italic;"><%= d.nav_logo_overlap_x || '0' %></span></label>
          <input type="range" name="nav_logo_overlap_x" min="-80" max="80" step="1" value="<%= d.nav_logo_overlap_x || '0' %>" oninput="this.previousElementSibling.querySelector('span').textContent=this.value" onchange="previewReload()">
        </div>
        <div class="ep-field">
          <label>Overlap Nudge Y (px) <span class="ep-hint" style="display:inline;font-style:italic;"><%= d.nav_logo_overlap_y || '0' %></span></label>
          <input type="range" name="nav_logo_overlap_y" min="-60" max="60" step="1" value="<%= d.nav_logo_overlap_y || '0' %>" oninput="this.previousElementSibling.querySelector('span').textContent=this.value" onchange="previewReload()">
        </div>
      </div>
      <script>
        (function(){
          var sel = document.querySelector('select[name="nav_logo_layout"]');
          var row = document.getElementById('nav-logo-overlap-row');
          if (sel && row) sel.addEventListener('change', function(){ row.style.display = this.value === 'overlap' ? '' : 'none'; });
        })();
      </script>

      <div style="font-size:0.62rem;font-weight:600;letter-spacing:0.1em;color:var(--slate);margin:14px 0 6px;">BRAND TEXT STYLE</div>
      <div class="ep-row-3">
        <div class="ep-field">
          <label>Size (rem) <span class="ep-hint" style="display:inline;font-style:italic;"><%= d.nav_brand_size || '1.4' %></span></label>
          <input type="range" name="nav_brand_size" min="0.8" max="3" step="0.05" value="<%= d.nav_brand_size || '1.4' %>" oninput="this.previousElementSibling.querySelector('span').textContent=this.value" onchange="previewReload()">
        </div>
        <div class="ep-field">
          <label>Weight</label>
          <select name="nav_brand_weight" onchange="previewReload()">
            <% ['300','400','500','600','700','800'].forEach(w => { %>
              <option value="<%= w %>" <%= (d.nav_brand_weight || '400') === w ? 'selected' : '' %>><%= w %></option>
            <% }) %>
          </select>
        </div>
        <div class="ep-field">
          <label>Letter Spacing (em) <span class="ep-hint" style="display:inline;font-style:italic;"><%= d.nav_brand_spacing || '0.04' %></span></label>
          <input type="range" name="nav_brand_spacing" min="-0.1" max="0.5" step="0.01" value="<%= d.nav_brand_spacing || '0.04' %>" oninput="this.previousElementSibling.querySelector('span').textContent=this.value" onchange="previewReload()">
        </div>
      </div>
      <div class="ep-row-3">
        <div class="ep-field">
          <label>Transform</label>
          <select name="nav_brand_transform" onchange="previewReload()">
            <option value="none"      <%= (d.nav_brand_transform || 'none') === 'none' ? 'selected' : '' %>>As typed</option>
            <option value="uppercase" <%= d.nav_brand_transform === 'uppercase' ? 'selected' : '' %>>UPPERCASE</option>
            <option value="lowercase" <%= d.nav_brand_transform === 'lowercase' ? 'selected' : '' %>>lowercase</option>
          </select>
        </div>
        <div class="ep-field">
          <label>Italic</label>
          <select name="nav_brand_italic" onchange="previewReload()">
            <option value="false" <%= d.nav_brand_italic !== 'true' ? 'selected' : '' %>>No</option>
            <option value="true"  <%= d.nav_brand_italic === 'true' ? 'selected' : '' %>>Yes</option>
          </select>
        </div>
        <div class="ep-field">
          <label>Color</label>
          <div class="color-row">
            <input type="color" value="<%= d.nav_brand_color || d.nav_text_color || d.color_dark %>" onchange="this.nextElementSibling.value=this.value;previewReload()" oninput="this.nextElementSibling.value=this.value">
            <input type="text" name="nav_brand_color" value="<%= d.nav_brand_color || '' %>" placeholder="auto" onchange="previewReload()">
          </div>
        </div>
      </div>

      <div style="font-size:0.62rem;font-weight:600;letter-spacing:0.1em;color:var(--slate);margin:14px 0 6px;">LOGO TEXT ACCENT</div>
      <div class="ep-hint" style="margin-bottom:8px;">Color a range of characters in your brand name. Char positions are 1-based.</div>
      <div class="ep-row-3">
        <div class="ep-field">
          <label>Char Start</label>
          <input type="number" name="nav_logo_split"     value="<%= d.nav_logo_split     || '0' %>" min="0" max="50" onchange="previewReload()">
        </div>
        <div class="ep-field">
          <label>Char End</label>
          <input type="number" name="nav_logo_split_end" value="<%= d.nav_logo_split_end || '0' %>" min="0" max="50" onchange="previewReload()">
        </div>
        <div class="ep-field">
          <label>Accent Color</label>
          <div class="color-row">
            <input type="color" value="<%= d.nav_logo_accent_color || d.color_accent %>" onchange="this.nextElementSibling.value=this.value;previewReload()" oninput="this.nextElementSibling.value=this.value">
            <input type="text" name="nav_logo_accent_color" value="<%= d.nav_logo_accent_color || '' %>" placeholder="auto" onchange="previewReload()">
          </div>
        </div>
      </div>
      <div class="ep-row-3">
        <div class="ep-field">
          <label>Accent Weight</label>
          <select name="nav_accent_weight" onchange="previewReload()">
            <option value="" <%= !d.nav_accent_weight ? 'selected' : '' %>>(inherit brand)</option>
            <% ['300','400','500','600','700','800','900'].forEach(w => { %>
              <option value="<%= w %>" <%= d.nav_accent_weight === w ? 'selected' : '' %>><%= w %></option>
            <% }) %>
          </select>
        </div>
        <div class="ep-field">
          <label>Accent Italic</label>
          <select name="nav_accent_italic" onchange="previewReload()">
            <option value="false" <%= d.nav_accent_italic !== 'true' ? 'selected' : '' %>>No</option>
            <option value="true"  <%= d.nav_accent_italic === 'true' ? 'selected' : '' %>>Yes</option>
          </select>
        </div>
        <div class="ep-field">&nbsp;</div>
      </div>

      <div style="font-size:0.62rem;font-weight:600;letter-spacing:0.1em;color:var(--slate);margin:14px 0 6px;">TAGLINE / WORDMARK SUB</div>
      <div class="ep-hint" style="margin-bottom:8px;">Renders the small tagline + location line under the brand name (when &ldquo;Show Tagline&rdquo; above is Yes).</div>
      <div class="ep-row-3">
        <div class="ep-field">
          <label>Size (rem) <span class="ep-hint" style="display:inline;font-style:italic;"><%= d.nav_tagline_size || '0.58' %></span></label>
          <input type="range" name="nav_tagline_size" min="0.5" max="1.4" step="0.02" value="<%= d.nav_tagline_size || '0.58' %>" oninput="this.previousElementSibling.querySelector('span').textContent=this.value" onchange="previewReload()">
        </div>
        <div class="ep-field">
          <label>Weight</label>
          <select name="nav_tagline_weight" onchange="previewReload()">
            <% ['300','400','500','600','700','800'].forEach(w => { %>
              <option value="<%= w %>" <%= (d.nav_tagline_weight || '600') === w ? 'selected' : '' %>><%= w %></option>
            <% }) %>
          </select>
        </div>
        <div class="ep-field">
          <label>Letter Spacing (em) <span class="ep-hint" style="display:inline;font-style:italic;"><%= d.nav_tagline_spacing || '0.28' %></span></label>
          <input type="range" name="nav_tagline_spacing" min="0" max="0.6" step="0.01" value="<%= d.nav_tagline_spacing || '0.28' %>" oninput="this.previousElementSibling.querySelector('span').textContent=this.value" onchange="previewReload()">
        </div>
      </div>
      <div class="ep-row-3">
        <div class="ep-field">
          <label>Transform</label>
          <select name="nav_tagline_transform" onchange="previewReload()">
            <option value="none"      <%= d.nav_tagline_transform === 'none' ? 'selected' : '' %>>As typed</option>
            <option value="uppercase" <%= (d.nav_tagline_transform || 'uppercase') === 'uppercase' ? 'selected' : '' %>>UPPERCASE</option>
            <option value="lowercase" <%= d.nav_tagline_transform === 'lowercase' ? 'selected' : '' %>>lowercase</option>
          </select>
        </div>
        <div class="ep-field">
          <label>Italic</label>
          <select name="nav_tagline_italic" onchange="previewReload()">
            <option value="false" <%= d.nav_tagline_italic !== 'true' ? 'selected' : '' %>>No</option>
            <option value="true"  <%= d.nav_tagline_italic === 'true' ? 'selected' : '' %>>Yes</option>
          </select>
        </div>
        <div class="ep-field">
          <label>Color</label>
          <div class="color-row">
            <input type="color" value="<%= d.nav_tagline_color || d.color_primary %>" onchange="this.nextElementSibling.value=this.value;previewReload()" oninput="this.nextElementSibling.value=this.value">
            <input type="text" name="nav_tagline_color" value="<%= d.nav_tagline_color || '' %>" placeholder="auto" onchange="previewReload()">
          </div>
        </div>
      </div>

      <div style="font-size:0.62rem;font-weight:600;letter-spacing:0.1em;color:var(--slate);margin:14px 0 6px;">CTA BUTTON</div>
      <div class="ep-row">
        <div class="ep-field">
          <label>CTA Text</label>
          <input type="text" name="nav_cta_text" value="<%= d.nav_cta_text || '' %>" placeholder="Get Started" onchange="previewReload()">
        </div>
        <div class="ep-field">
          <label>CTA Link</label>
          <input type="text" name="nav_cta_link" value="<%= d.nav_cta_link || '' %>" placeholder="/#contact" onchange="previewReload()">
        </div>
      </div>
    </div>

    <%# ── TAB: Footer ── %>
    <div class="hf-pane" data-pane="footer" style="display:none;">
      <div style="font-size:0.62rem;font-weight:600;letter-spacing:0.1em;color:var(--slate);margin:0 0 6px;">STRUCTURE</div>
      <div class="ep-hint" style="margin-bottom:8px;">Pick the footer&rsquo;s overall arrangement &mdash; independent of the landing layout.</div>
      <div class="ep-row-3">
        <div class="ep-field">
          <label>Footer Layout</label>
          <select name="footer_layout" onchange="previewReload()">
            <option value="simple"   <%= (d.footer_layout || 'simple') === 'simple'   ? 'selected' : '' %>>Simple &mdash; copyright + admin</option>
            <option value="columns"  <%= d.footer_layout === 'columns'  ? 'selected' : '' %>>Columns &mdash; brand + link sets</option>
            <option value="centered" <%= d.footer_layout === 'centered' ? 'selected' : '' %>>Centered &mdash; brand + links stacked</option>
            <option value="expanded" <%= d.footer_layout === 'expanded' ? 'selected' : '' %>>Expanded &mdash; about + columns + newsletter</option>
            <option value="minimal"  <%= d.footer_layout === 'minimal'  ? 'selected' : '' %>>Minimal &mdash; copyright only</option>
            <option value="brand"    <%= d.footer_layout === 'brand'    ? 'selected' : '' %>>Brand &mdash; large brand + tagline + social</option>
          </select>
        </div>
        <div class="ep-field">
          <label>Alignment</label>
          <select name="footer_align" onchange="previewReload()">
            <option value="left"   <%= d.footer_align === 'left'   ? 'selected' : '' %>>Left</option>
            <option value="center" <%= (d.footer_align || 'center') === 'center' ? 'selected' : '' %>>Center</option>
            <option value="right"  <%= d.footer_align === 'right'  ? 'selected' : '' %>>Right</option>
          </select>
        </div>
        <div class="ep-field">
          <label>Columns</label>
          <select name="footer_columns" onchange="previewReload()">
            <% [1,2,3,4].forEach(function(n) { %>
              <option value="<%= n %>" <%= (d.footer_columns || '3') === String(n) ? 'selected' : '' %>><%= n %></option>
            <% }) %>
          </select>
        </div>
      </div>
      <div class="ep-row-3">
        <div class="ep-field">
          <label>Vertical Padding (px) <span class="ep-hint" style="display:inline;font-style:italic;"><%= d.footer_padding_y || '32' %></span></label>
          <input type="range" name="footer_padding_y" min="8" max="96" step="2" value="<%= d.footer_padding_y || '32' %>" oninput="this.previousElementSibling.querySelector('span').textContent=this.value" onchange="previewReload()">
        </div>
        <div class="ep-field">
          <label>Horizontal Padding (px) <span class="ep-hint" style="display:inline;font-style:italic;"><%= d.footer_padding_x || '24' %></span></label>
          <input type="range" name="footer_padding_x" min="8" max="96" step="2" value="<%= d.footer_padding_x || '24' %>" oninput="this.previousElementSibling.querySelector('span').textContent=this.value" onchange="previewReload()">
        </div>
        <div class="ep-field">
          <label>Brand Size (rem) <span class="ep-hint" style="display:inline;font-style:italic;"><%= d.footer_brand_size || '1.2' %></span></label>
          <input type="range" name="footer_brand_size" min="0.7" max="3" step="0.05" value="<%= d.footer_brand_size || '1.2' %>" oninput="this.previousElementSibling.querySelector('span').textContent=this.value" onchange="previewReload()">
        </div>
      </div>

      <div style="font-size:0.62rem;font-weight:600;letter-spacing:0.1em;color:var(--slate);margin:14px 0 6px;">PIECES</div>
      <div class="ep-row" style="grid-template-columns:repeat(3,1fr);gap:6px;">
        <label class="vis-item"><input type="checkbox" name="footer_show_brand"    <%= d.footer_show_brand    !== 'false' ? 'checked' : '' %>> Brand</label>
        <label class="vis-item"><input type="checkbox" name="footer_show_tagline"  <%= d.footer_show_tagline  !== 'false' ? 'checked' : '' %>> Tagline</label>
        <label class="vis-item"><input type="checkbox" name="footer_show_logo"     <%= d.footer_show_logo     === 'true'  ? 'checked' : '' %>> Logo Image</label>
        <label class="vis-item"><input type="checkbox" name="footer_show_social"   <%= d.footer_show_social   !== 'false' ? 'checked' : '' %>> Social Row</label>
        <label class="vis-item"><input type="checkbox" name="footer_show_newsletter" <%= d.footer_show_newsletter === 'true' ? 'checked' : '' %>> Newsletter</label>
        <label class="vis-item"><input type="checkbox" name="footer_show_qr"       <%= d.footer_show_qr       !== 'false' ? 'checked' : '' %>> QR Codes</label>
        <label class="vis-item"><input type="checkbox" name="vis_admin_link"       <%= d.vis_admin_link       !== 'false' ? 'checked' : '' %>> Admin Link</label>
      </div>

      <div style="font-size:0.62rem;font-weight:600;letter-spacing:0.1em;color:var(--slate);margin:14px 0 6px;">TYPOGRAPHY</div>
      <div class="ep-row-3">
        <div class="ep-field">
          <label>Body Size (rem) <span class="ep-hint" style="display:inline;font-style:italic;"><%= d.footer_text_size || '0.78' %></span></label>
          <input type="range" name="footer_text_size" min="0.5" max="1.2" step="0.02" value="<%= d.footer_text_size || '0.78' %>" oninput="this.previousElementSibling.querySelector('span').textContent=this.value" onchange="previewReload()">
        </div>
        <div class="ep-field">
          <label>Body Weight</label>
          <select name="footer_text_weight" onchange="previewReload()">
            <% ['300','400','500','600','700'].forEach(function(w) { %>
              <option value="<%= w %>" <%= (d.footer_text_weight || '400') === w ? 'selected' : '' %>><%= w %></option>
            <% }) %>
          </select>
        </div>
        <div class="ep-field">
          <label>Brand Weight</label>
          <select name="footer_brand_weight" onchange="previewReload()">
            <% ['300','400','500','600','700','800'].forEach(function(w) { %>
              <option value="<%= w %>" <%= (d.footer_brand_weight || '500') === w ? 'selected' : '' %>><%= w %></option>
            <% }) %>
          </select>
        </div>
      </div>
      <div class="ep-row-3">
        <div class="ep-field">
          <label>Heading Size (rem) <span class="ep-hint" style="display:inline;font-style:italic;"><%= d.footer_heading_size || '0.72' %></span></label>
          <input type="range" name="footer_heading_size" min="0.5" max="1.4" step="0.02" value="<%= d.footer_heading_size || '0.72' %>" oninput="this.previousElementSibling.querySelector('span').textContent=this.value" onchange="previewReload()">
        </div>
        <div class="ep-field">
          <label>Heading Weight</label>
          <select name="footer_heading_weight" onchange="previewReload()">
            <% ['300','400','500','600','700','800'].forEach(function(w) { %>
              <option value="<%= w %>" <%= (d.footer_heading_weight || '600') === w ? 'selected' : '' %>><%= w %></option>
            <% }) %>
          </select>
        </div>
        <div class="ep-field">
          <label>Heading Transform</label>
          <select name="footer_heading_transform" onchange="previewReload()">
            <option value="none"      <%= d.footer_heading_transform === 'none'      ? 'selected' : '' %>>As typed</option>
            <option value="uppercase" <%= (d.footer_heading_transform || 'uppercase') === 'uppercase' ? 'selected' : '' %>>UPPERCASE</option>
            <option value="lowercase" <%= d.footer_heading_transform === 'lowercase' ? 'selected' : '' %>>lowercase</option>
          </select>
        </div>
      </div>
      <div class="ep-row-3">
        <div class="ep-field">
          <label>Heading Letter-Spacing (em) <span class="ep-hint" style="display:inline;font-style:italic;"><%= d.footer_heading_spacing || '0.12' %></span></label>
          <input type="range" name="footer_heading_spacing" min="0" max="0.4" step="0.01" value="<%= d.footer_heading_spacing || '0.12' %>" oninput="this.previousElementSibling.querySelector('span').textContent=this.value" onchange="previewReload()">
        </div>
      </div>

      <div style="font-size:0.62rem;font-weight:600;letter-spacing:0.1em;color:var(--slate);margin:14px 0 6px;">COPY</div>
      <% const c = (typeof copy !== 'undefined' && copy) ? copy : {}; %>
      <div class="ep-row">
        <div class="ep-field">
          <label>Tagline</label>
          <input type="text" name="footer_tagline" value="<%= c.footer_tagline || '' %>" placeholder="Short line under brand" maxlength="160" onchange="previewReload()">
        </div>
        <div class="ep-field">
          <label>Copyright Override</label>
          <input type="text" name="footer_copyright" value="<%= c.footer_copyright || '' %>" placeholder="© {year} {name} (leave empty for auto)" onchange="previewReload()">
        </div>
      </div>
      <div class="ep-field">
        <label>About Paragraph <span class="ep-hint" style="display:inline;font-style:italic;">(Expanded / Brand layouts)</span></label>
        <textarea name="footer_about" rows="2" placeholder="Short description shown beside link columns" onchange="previewReload()"><%= c.footer_about || '' %></textarea>
      </div>
      <div class="ep-field">
        <label>Legal / Disclaimer <span class="ep-hint" style="display:inline;font-style:italic;">(shown in the bottom bar above Terms / Privacy)</span></label>
        <textarea name="footer_legal" rows="2" placeholder="e.g. Registered in Delaware. All prices in USD. Licensed &amp; insured." onchange="previewReload()"><%= c.footer_legal || '' %></textarea>
      </div>

      <div style="font-size:0.62rem;font-weight:600;letter-spacing:0.1em;color:var(--slate);margin:14px 0 6px;">LINK COLUMNS</div>
      <div class="ep-hint" style="margin-bottom:8px;">One link per line, format <code>Label | /url</code>. Rendered in Columns / Expanded layouts.</div>
      <% [1,2,3,4].forEach(function(n) { %>
        <div class="ep-row" style="grid-template-columns:1fr 2fr;gap:6px;">
          <div class="ep-field">
            <label>Col <%= n %> Heading</label>
            <input type="text" name="footer_col<%= n %>_heading" value="<%= c['footer_col' + n + '_heading'] || '' %>" placeholder="<%= ['Company','Services','Resources','Legal'][n-1] %>" maxlength="40" onchange="previewReload()">
          </div>
          <div class="ep-field">
            <label>Col <%= n %> Links</label>
            <textarea name="footer_col<%= n %>_links" rows="2" placeholder="About | /about&#10;Contact | /contact" onchange="previewReload()"><%= c['footer_col' + n + '_links'] || '' %></textarea>
          </div>
        </div>
      <% }) %>

      <div style="font-size:0.62rem;font-weight:600;letter-spacing:0.1em;color:var(--slate);margin:14px 0 6px;">NEWSLETTER</div>
      <div class="ep-hint" style="margin-bottom:8px;">Only renders when the Newsletter piece is enabled above.</div>
      <div class="ep-row">
        <div class="ep-field">
          <label>Heading</label>
          <input type="text" name="footer_newsletter_heading" value="<%= c.footer_newsletter_heading || '' %>" placeholder="Stay in touch" onchange="previewReload()">
        </div>
        <div class="ep-field">
          <label>Subtext</label>
          <input type="text" name="footer_newsletter_sub" value="<%= c.footer_newsletter_sub || '' %>" placeholder="Monthly updates, no spam." onchange="previewReload()">
        </div>
      </div>
      <div class="ep-row">
        <div class="ep-field">
          <label>Placeholder</label>
          <input type="text" name="footer_newsletter_placeholder" value="<%= c.footer_newsletter_placeholder || '' %>" placeholder="you@example.com" onchange="previewReload()">
        </div>
        <div class="ep-field">
          <label>Button Text</label>
          <input type="text" name="footer_newsletter_button" value="<%= c.footer_newsletter_button || '' %>" placeholder="Subscribe" onchange="previewReload()">
        </div>
      </div>

      <div style="font-size:0.62rem;font-weight:600;letter-spacing:0.1em;color:var(--slate);margin:14px 0 6px;">CAREERS CTA</div>
      <div class="ep-hint" style="margin-bottom:8px;">A &ldquo;we&rsquo;re hiring&rdquo; card linking to <code>/careers</code>. Only renders when the Careers feature is enabled for this tenant.</div>
      <div class="ep-row">
        <div class="ep-field">
          <label>Heading</label>
          <input type="text" name="footer_careers_heading" value="<%= c.footer_careers_heading || '' %>" placeholder="We're hiring" maxlength="60" onchange="previewReload()">
        </div>
        <div class="ep-field">
          <label>Button Text</label>
          <input type="text" name="footer_careers_button" value="<%= c.footer_careers_button || '' %>" placeholder="View openings" maxlength="40" onchange="previewReload()">
        </div>
      </div>
      <div class="ep-field">
        <label>Subtext</label>
        <input type="text" name="footer_careers_sub" value="<%= c.footer_careers_sub || '' %>" placeholder="Join our team — explore open roles." onchange="previewReload()">
      </div>

      <div style="font-size:0.62rem;font-weight:600;letter-spacing:0.1em;color:var(--slate);margin:14px 0 6px;">SOCIAL</div>
      <div class="ep-field">
        <label>Heading</label>
        <input type="text" name="footer_social_heading" value="<%= c.footer_social_heading || '' %>" placeholder="Follow" maxlength="40" onchange="previewReload()">
      </div>
      <div class="ep-row-3">
        <div class="ep-field"><label>Facebook</label><input type="text" name="footer_social_facebook" value="<%= d.footer_social_facebook || '' %>" placeholder="https://facebook.com/..." onchange="previewReload()"></div>
        <div class="ep-field"><label>Instagram</label><input type="text" name="footer_social_instagram" value="<%= d.footer_social_instagram || '' %>" placeholder="https://instagram.com/..." onchange="previewReload()"></div>
        <div class="ep-field"><label>Twitter / X</label><input type="text" name="footer_social_twitter" value="<%= d.footer_social_twitter || '' %>" placeholder="https://x.com/..." onchange="previewReload()"></div>
      </div>
      <div class="ep-row-3">
        <div class="ep-field"><label>LinkedIn</label><input type="text" name="footer_social_linkedin" value="<%= d.footer_social_linkedin || '' %>" placeholder="https://linkedin.com/..." onchange="previewReload()"></div>
        <div class="ep-field"><label>YouTube</label><input type="text" name="footer_social_youtube" value="<%= d.footer_social_youtube || '' %>" placeholder="https://youtube.com/@..." onchange="previewReload()"></div>
        <div class="ep-field"><label>TikTok</label><input type="text" name="footer_social_tiktok" value="<%= d.footer_social_tiktok || '' %>" placeholder="https://tiktok.com/@..." onchange="previewReload()"></div>
      </div>
      <div class="ep-row-3">
        <div class="ep-field"><label>GitHub</label><input type="text" name="footer_social_github" value="<%= d.footer_social_github || '' %>" placeholder="https://github.com/..." onchange="previewReload()"></div>
        <div class="ep-field">&nbsp;</div>
        <div class="ep-field">&nbsp;</div>
      </div>
    </div>

    <%# ── TAB: Colors ── %>
    <div class="hf-pane" data-pane="colors" style="display:none;">
      <div style="font-size:0.62rem;font-weight:600;letter-spacing:0.1em;color:var(--slate);margin:0 0 6px;">HEADER</div>
      <div class="ep-field">
        <label>Background</label>
        <div class="color-row">
          <input type="color" value="<%= d.nav_bg || d.color_bg %>" onchange="this.nextElementSibling.value=this.value;previewReload()" oninput="this.nextElementSibling.value=this.value">
          <input type="text" name="nav_bg" value="<%= d.nav_bg || '' %>" placeholder="auto" onchange="previewReload()">
        </div>
      </div>
      <div class="ep-field">
        <label>Default Text <span class="ep-hint" style="display:inline;font-style:italic;">(fallback for brand &amp; links)</span></label>
        <div class="color-row">
          <input type="color" value="<%= d.nav_text_color || d.color_dark %>" onchange="this.nextElementSibling.value=this.value;previewReload()" oninput="this.nextElementSibling.value=this.value">
          <input type="text" name="nav_text_color" value="<%= d.nav_text_color || '' %>" placeholder="auto" onchange="previewReload()">
        </div>
      </div>

      <details style="margin-top:10px;border-top:1px solid var(--border);padding-top:10px;">
        <summary style="cursor:pointer;font-size:0.62rem;font-weight:600;letter-spacing:0.12em;text-transform:uppercase;color:var(--slate);user-select:none;">Per-element overrides</summary>
        <div style="margin-top:10px;">
          <div class="ep-hint" style="margin-bottom:8px;">Brand &amp; tagline colors live with their typography in the <strong>Layout</strong> tab.</div>
          <div class="ep-row">
            <div class="ep-field">
              <label>Link Resting</label>
              <div class="color-row">
                <input type="color" value="<%= d.nav_link_color || d.nav_text_color || d.color_dark %>" onchange="this.nextElementSibling.value=this.value;previewReload()" oninput="this.nextElementSibling.value=this.value">
                <input type="text" name="nav_link_color" value="<%= d.nav_link_color || '' %>" placeholder="auto" onchange="previewReload()">
              </div>
            </div>
            <div class="ep-field">
              <label>Link Hover / Active</label>
              <div class="color-row">
                <input type="color" value="<%= d.nav_link_hover_color || d.color_primary %>" onchange="this.nextElementSibling.value=this.value;previewReload()" oninput="this.nextElementSibling.value=this.value">
                <input type="text" name="nav_link_hover_color" value="<%= d.nav_link_hover_color || '' %>" placeholder="auto" onchange="previewReload()">
              </div>
            </div>
          </div>
          <div class="ep-row">
            <div class="ep-field">
              <label>CTA Background</label>
              <div class="color-row">
                <input type="color" value="<%= d.nav_cta_bg || d.color_accent %>" onchange="this.nextElementSibling.value=this.value;previewReload()" oninput="this.nextElementSibling.value=this.value">
                <input type="text" name="nav_cta_bg" value="<%= d.nav_cta_bg || '' %>" placeholder="auto" onchange="previewReload()">
              </div>
            </div>
            <div class="ep-field">
              <label>CTA Text</label>
              <div class="color-row">
                <input type="color" value="<%= d.nav_cta_color || d.color_dark %>" onchange="this.nextElementSibling.value=this.value;previewReload()" oninput="this.nextElementSibling.value=this.value">
                <input type="text" name="nav_cta_color" value="<%= d.nav_cta_color || '' %>" placeholder="auto" onchange="previewReload()">
              </div>
            </div>
          </div>
          <div class="ep-hint">CTA: set both bg + text to render the link as a button. Empty = plain link styling.</div>
        </div>
      </details>

      <div style="font-size:0.62rem;font-weight:600;letter-spacing:0.1em;color:var(--slate);margin:18px 0 6px;border-top:1px solid var(--border);padding-top:14px;">FOOTER</div>
      <div class="ep-row">
        <div class="ep-field">
          <label>Background</label>
          <div class="color-row">
            <input type="color" value="<%= d.footer_bg || d.color_primary_deep %>" onchange="this.nextElementSibling.value=this.value;previewReload()" oninput="this.nextElementSibling.value=this.value">
            <input type="text" name="footer_bg" value="<%= d.footer_bg || '' %>" placeholder="auto" onchange="previewReload()">
          </div>
        </div>
        <div class="ep-field">
          <label>Body Text</label>
          <div class="color-row">
            <input type="color" value="<%= d.footer_text_color || d.color_white %>" onchange="this.nextElementSibling.value=this.value;previewReload()" oninput="this.nextElementSibling.value=this.value">
            <input type="text" name="footer_text_color" value="<%= d.footer_text_color || '' %>" placeholder="auto" onchange="previewReload()">
          </div>
        </div>
      </div>
      <div class="ep-row">
        <div class="ep-field">
          <label>Column Heading</label>
          <div class="color-row">
            <input type="color" value="<%= d.footer_heading_color || d.color_accent %>" onchange="this.nextElementSibling.value=this.value;previewReload()" oninput="this.nextElementSibling.value=this.value">
            <input type="text" name="footer_heading_color" value="<%= d.footer_heading_color || '' %>" placeholder="auto" onchange="previewReload()">
          </div>
        </div>
        <div class="ep-field">
          <label>Border / Divider</label>
          <div class="color-row">
            <input type="color" value="<%= d.footer_border_color || d.color_border || '#2E4270' %>" onchange="this.nextElementSibling.value=this.value;previewReload()" oninput="this.nextElementSibling.value=this.value">
            <input type="text" name="footer_border_color" value="<%= d.footer_border_color || '' %>" placeholder="auto" onchange="previewReload()">
          </div>
        </div>
      </div>
      <div class="ep-row">
        <div class="ep-field">
          <label>Link Resting</label>
          <div class="color-row">
            <input type="color" value="<%= d.footer_link_color || d.footer_text_color || d.color_white %>" onchange="this.nextElementSibling.value=this.value;previewReload()" oninput="this.nextElementSibling.value=this.value">
            <input type="text" name="footer_link_color" value="<%= d.footer_link_color || '' %>" placeholder="auto" onchange="previewReload()">
          </div>
        </div>
        <div class="ep-field">
          <label>Link Hover</label>
          <div class="color-row">
            <input type="color" value="<%= d.footer_link_hover_color || d.color_accent %>" onchange="this.nextElementSibling.value=this.value;previewReload()" oninput="this.nextElementSibling.value=this.value">
            <input type="text" name="footer_link_hover_color" value="<%= d.footer_link_hover_color || '' %>" placeholder="auto" onchange="previewReload()">
          </div>
        </div>
      </div>
      <div class="ep-field">
        <label>Accent <span class="ep-hint" style="display:inline;font-style:italic;">(social icons, dividers)</span></label>
        <div class="color-row">
          <input type="color" value="<%= d.footer_accent_color || d.color_accent %>" onchange="this.nextElementSibling.value=this.value;previewReload()" oninput="this.nextElementSibling.value=this.value">
          <input type="text" name="footer_accent_color" value="<%= d.footer_accent_color || '' %>" placeholder="auto" onchange="previewReload()">
        </div>
      </div>
    </div>

    <%# ── TAB: Links ── %>
    <div class="hf-pane" data-pane="links" style="display:none;">
      <div class="ep-hint" style="margin-bottom:10px;">CMS pages auto-populate. Add external URLs &amp; section anchors here.</div>

      <% if (navLinks && navLinks.length) { %>
        <div style="display:flex;flex-direction:column;gap:6px;margin-bottom:14px;">
          <% navLinks.forEach(function(l) { const fid = 'link-edit-' + l._id; %>
            <div style="border:1px solid var(--border);border-radius:2px;padding:8px;background:var(--surface);">
              <input type="hidden" name="_id" value="<%= l._id %>" form="<%= fid %>">
              <div class="ep-row" style="grid-template-columns:1fr 1.4fr;gap:6px;">
                <div class="ep-field" style="margin-bottom:6px;">
                  <label>Label</label>
                  <input type="text" name="label" value="<%= l.label %>" maxlength="60" form="<%= fid %>">
                </div>
                <div class="ep-field" style="margin-bottom:6px;">
                  <label>URL</label>
                  <input type="text" name="url" value="<%= l.url %>" maxlength="500" placeholder="/about or https://..." form="<%= fid %>">
                </div>
              </div>
              <div class="ep-row-3" style="gap:6px;">
                <div class="ep-field" style="margin-bottom:0;">
                  <label>Show in</label>
                  <select name="location" form="<%= fid %>">
                    <option value="header" <%= l.location === 'header' ? 'selected' : '' %>>Header only</option>
                    <option value="footer" <%= l.location === 'footer' ? 'selected' : '' %>>Footer only</option>
                    <option value="both"   <%= (l.location || 'both') === 'both' ? 'selected' : '' %>>Both</option>
                  </select>
                </div>
                <div class="ep-field" style="margin-bottom:0;">
                  <label>Open</label>
                  <select name="target" form="<%= fid %>">
                    <option value="_self"  <%= l.target !== '_blank' ? 'selected' : '' %>>Same tab</option>
                    <option value="_blank" <%= l.target === '_blank' ? 'selected' : '' %>>New tab</option>
                  </select>
                </div>
                <div class="ep-field" style="margin-bottom:0;display:flex;gap:4px;align-items:flex-end;">
                  <button type="submit" form="<%= fid %>" class="btn btn-navy" style="font-size:0.6rem;padding:6px 8px;flex:1;">Save</button>
                  <button type="submit" form="link-delete-<%= l._id %>" class="btn btn-ghost" style="font-size:0.6rem;padding:6px 8px;color:var(--danger);" onclick="return confirm('Delete this link?')">&times;</button>
                </div>
              </div>
            </div>
          <% }); %>
        </div>
      <% } %>

      <div style="border:1px dashed var(--border);border-radius:2px;padding:10px;background:var(--ivory);">
        <div style="font-size:0.62rem;font-weight:600;letter-spacing:0.12em;text-transform:uppercase;color:var(--slate);margin-bottom:8px;">+ Add new link</div>
        <div class="ep-row" style="grid-template-columns:1fr 1.4fr;gap:6px;">
          <div class="ep-field" style="margin-bottom:6px;">
            <label>Label</label>
            <input type="text" name="label" maxlength="60" placeholder="Shop" form="link-create">
          </div>
          <div class="ep-field" style="margin-bottom:6px;">
            <label>URL</label>
            <input type="text" name="url" maxlength="500" placeholder="/shop or https://..." form="link-create">
          </div>
        </div>
        <div class="ep-row-3" style="gap:6px;">
          <div class="ep-field" style="margin-bottom:0;">
            <label>Show in</label>
            <select name="location" form="link-create">
              <option value="both" selected>Both</option>
              <option value="header">Header only</option>
              <option value="footer">Footer only</option>
            </select>
          </div>
          <div class="ep-field" style="margin-bottom:0;">
            <label>Open</label>
            <select name="target" form="link-create">
              <option value="_self" selected>Same tab</option>
              <option value="_blank">New tab</option>
            </select>
          </div>
          <div class="ep-field" style="margin-bottom:0;display:flex;align-items:flex-end;">
            <button type="submit" form="link-create" class="btn btn-navy" style="font-size:0.62rem;padding:7px 10px;width:100%;">Add Link</button>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<%# ─── 5. HERO COPY ─── %>
<div class="editor-section" data-tab="copy">
  <button type="button" class="editor-section-toggle" onclick="toggleSection(this)">
    Hero Copy <span class="arrow">&#9654;</span>
  </button>
  <div class="editor-section-body">
    <div class="ep-field">
      <label>Eyebrow</label>
      <input type="text" name="hero_eyebrow" value="<%= copy.hero_eyebrow || '' %>" placeholder="e.g. Welcome to...">
    </div>
    <div class="ep-field">
      <label>Heading</label>
      <input type="text" name="hero_heading" value="<%= copy.hero_heading || '' %>" placeholder="Main headline">
    </div>
    <div class="ep-field">
      <label>Heading Emphasis</label>
      <input type="text" name="hero_heading_em" value="<%= copy.hero_heading_em || '' %>" placeholder="Italic/accent word">
    </div>
    <div class="ep-field">
      <label>Subheading</label>
      <textarea name="hero_sub" rows="2" placeholder="Supporting text"><%= copy.hero_sub || '' %></textarea>
    </div>
    <div class="ep-field">
      <label>Badge</label>
      <input type="text" name="hero_badge" value="<%= copy.hero_badge || '' %>" placeholder="e.g. Est. 2020">
    </div>
    <div class="ep-row">
      <div class="ep-field">
        <label>CTA Primary</label>
        <input type="text" name="hero_cta_primary" value="<%= copy.hero_cta_primary || '' %>" placeholder="Button text">
      </div>
      <div class="ep-field">
        <label>CTA Link</label>
        <input type="text" name="hero_cta_primary_link" value="<%= copy.hero_cta_primary_link || '' %>" placeholder="/contact">
      </div>
    </div>
    <div class="ep-row">
      <div class="ep-field">
        <label>CTA Secondary</label>
        <input type="text" name="hero_cta_secondary" value="<%= copy.hero_cta_secondary || '' %>" placeholder="Button text">
      </div>
      <div class="ep-field">
        <label>CTA Link</label>
        <input type="text" name="hero_cta_secondary_link" value="<%= copy.hero_cta_secondary_link || '' %>" placeholder="/portfolio">
      </div>
    </div>
  </div>
</div>

<%# ─── 5b. HERO SLIDES (slideshow hero style) ─── %>
<div class="editor-section" data-tab="copy">
  <button type="button" class="editor-section-toggle" onclick="toggleSection(this)">
    Hero Slides <span class="arrow">&#9654;</span>
  </button>
  <div class="editor-section-body">
    <div class="ep-hint" style="margin-bottom:10px;">
      Used when <strong>Hero Style = Slideshow</strong>. Each slide gets a background image, headline, optional tag chip,
      and a link target. Quick-anchor buttons jump straight to common public landing sections.
    </div>
    <%
      const _slideIdxAdm = new Set();
      Object.keys(copy).forEach(k => { const m = k.match(/^slide(\d+)_/); if (m) _slideIdxAdm.add(parseInt(m[1], 10)); });
      const _slideArr = [..._slideIdxAdm].sort((a, b) => a - b);
      if (_slideArr.length === 0) _slideArr.push(1, 2, 3);
    %>
    <div class="dp-repeater"
         data-prefix="slide"
         data-noun="Slide"
         data-fields='[{"key":"image","label":"Background Image","type":"text","placeholder":"/uploads/..."},{"key":"tag","label":"Tag Chip","type":"text","placeholder":"e.g. FEATURED"},{"key":"eyebrow","label":"Eyebrow","type":"text"},{"key":"heading","label":"Heading","type":"text"},{"key":"sub","label":"Subtext","type":"textarea","rows":2},{"key":"link","label":"Link","type":"text","placeholder":"#services or /pricing"},{"key":"cta","label":"CTA Button Text","type":"text","placeholder":"Learn more"},{"key":"vpos","label":"Text Position Override","type":"select","options":["inherit","top","middle","bottom"]}]'>
      <% _slideArr.forEach(function(i, iArr) { %>
      <div class="dp-rep-item" data-idx="<%= i %>">
        <div class="dp-rep-head">
          <span>Slide <%= i %></span>
          <div class="dp-rep-actions">
            <button type="button" data-dir="-1" onclick="repMove(this, -1)" title="Move up"<%= iArr === 0 ? ' disabled' : '' %>>↑</button>
            <button type="button" data-dir="1" onclick="repMove(this, 1)" title="Move down"<%= iArr === _slideArr.length - 1 ? ' disabled' : '' %>>↓</button>
            <button type="button" class="dp-rep-remove" onclick="repRemove(this)" title="Remove this slide">✕</button>
          </div>
        </div>
        <div class="ep-field">
          <label>Background Image URL</label>
          <input type="text" name="slide<%= i %>_image" value="<%= copy['slide' + i + '_image'] || '' %>" placeholder="/uploads/...">
        </div>
        <div class="ep-row">
          <div class="ep-field">
            <label>Tag Chip <span style="font-weight:400;color:var(--on-surface-muted);">(small badge)</span></label>
            <input type="text" name="slide<%= i %>_tag" value="<%= copy['slide' + i + '_tag'] || '' %>" placeholder="e.g. FEATURED, NEW">
          </div>
          <div class="ep-field">
            <label>Eyebrow</label>
            <input type="text" name="slide<%= i %>_eyebrow" value="<%= copy['slide' + i + '_eyebrow'] || '' %>" placeholder="optional small text above heading">
          </div>
        </div>
        <div class="ep-field">
          <label>Heading</label>
          <input type="text" name="slide<%= i %>_heading" value="<%= copy['slide' + i + '_heading'] || '' %>">
        </div>
        <div class="ep-field">
          <label>Subtext</label>
          <textarea name="slide<%= i %>_sub" rows="2"><%= copy['slide' + i + '_sub'] || '' %></textarea>
        </div>
        <div class="ep-field">
          <label>Link Target — wraps the whole slide, so a click anywhere navigates</label>
          <input type="text" name="slide<%= i %>_link" id="slide_<%= i %>_link" value="<%= copy['slide' + i + '_link'] || '' %>" placeholder="#services, /pricing, https://...">
          <div style="display:flex;flex-wrap:wrap;gap:4px;margin-top:6px;">
            <% ['#services','#portfolio','#about','#process','#pricing','#contact','/blog','/start'].forEach(function(anchor) { %>
              <button type="button"
                      onclick="document.getElementById('slide_<%= i %>_link').value='<%= anchor %>';document.getElementById('slide_<%= i %>_link').dispatchEvent(new Event('input',{bubbles:true}));"
                      style="font-size:0.62rem;font-weight:600;letter-spacing:0.1em;text-transform:uppercase;padding:4px 8px;border:1px solid var(--border);border-radius:2px;background:var(--ivory);color:var(--navy);cursor:pointer;font-family:var(--font-body);">→ <%= anchor %></button>
            <% }); %>
          </div>
        </div>
        <div class="ep-row">
          <div class="ep-field">
            <label>CTA Button Text <span style="font-weight:400;color:var(--on-surface-muted);">(only rendered if Link is set)</span></label>
            <input type="text" name="slide<%= i %>_cta" value="<%= copy['slide' + i + '_cta'] || '' %>" placeholder="Learn more">
          </div>
          <div class="ep-field">
            <label>Text Position Override <span style="font-weight:400;color:var(--on-surface-muted);">(overrides global)</span></label>
            <% const _sv = copy['slide' + i + '_vpos'] || 'inherit'; %>
            <select name="slide<%= i %>_vpos">
              <option value="inherit" <%= _sv === 'inherit' || _sv === '' ? 'selected' : '' %>>Inherit (global)</option>
              <option value="top"     <%= _sv === 'top'    ? 'selected' : '' %>>Top</option>
              <option value="middle"  <%= _sv === 'middle' ? 'selected' : '' %>>Middle</option>
              <option value="bottom"  <%= _sv === 'bottom' ? 'selected' : '' %>>Bottom</option>
            </select>
          </div>
        </div>
      </div>
      <% }); %>
      <button type="button" class="dp-rep-add" onclick="repAdd(this)">+ Add Slide</button>
    </div>
  </div>
</div>

<%# ─── 6. SERVICES COPY ─── %>
<div class="editor-section" data-tab="copy">
  <button type="button" class="editor-section-toggle" onclick="toggleSection(this)">
    Services <span class="arrow">&#9654;</span>
  </button>
  <div class="editor-section-body">
    <div class="ep-field">
      <label>Section Label</label>
      <input type="text" name="services_label" value="<%= copy.services_label || '' %>" placeholder="e.g. What We Do">
    </div>
    <div class="ep-field">
      <label>Heading</label>
      <input type="text" name="services_heading" value="<%= copy.services_heading || '' %>">
    </div>
    <div class="ep-row">
      <div class="ep-field">
        <label>Heading Emphasis</label>
        <input type="text" name="services_heading_em" value="<%= copy.services_heading_em || '' %>">
      </div>
      <div class="ep-field">
        <label>Subheading</label>
        <input type="text" name="services_sub" value="<%= copy.services_sub || '' %>">
      </div>
    </div>
    <%
      // Count existing services by scanning copy keys for service{N}_*
      const _svcIdx = new Set();
      Object.keys(copy).forEach(k => { const m = k.match(/^service(\d+)_/); if (m) _svcIdx.add(parseInt(m[1], 10)); });
      const _svcArr = [..._svcIdx].sort((a, b) => a - b);
      if (_svcArr.length === 0) _svcArr.push(1, 2, 3); // ensure at least 3 visible on first load
    %>
    <div class="dp-repeater"
         data-prefix="service"
         data-noun="Service"
         data-fields='[{"key":"title","label":"Title","type":"text"},{"key":"desc","label":"Description","type":"textarea","rows":2},{"key":"link","label":"Link","type":"text","placeholder":"/services"},{"key":"image","label":"Image URL","type":"text","placeholder":"/images/..."},{"key":"hidden","label":"Visibility","type":"checkbox","checkboxLabel":"Hide this card from the live page"}]'>
      <% _svcArr.forEach(function(i, iArr) { %>
      <div class="dp-rep-item" data-idx="<%= i %>">
        <div class="dp-rep-head">
          <span>Service <%= i %></span>
          <div class="dp-rep-actions">
            <button type="button" data-dir="-1" onclick="repMove(this, -1)" title="Move up"<%= iArr === 0 ? ' disabled' : '' %>>↑</button>
            <button type="button" data-dir="1" onclick="repMove(this, 1)" title="Move down"<%= iArr === _svcArr.length - 1 ? ' disabled' : '' %>>↓</button>
            <button type="button" class="dp-rep-remove" onclick="repRemove(this)" title="Remove this service">✕</button>
          </div>
        </div>
        <div class="ep-field">
          <label>Title</label>
          <input type="text" name="service<%= i %>_title" value="<%= copy['service' + i + '_title'] || '' %>">
        </div>
        <div class="ep-field">
          <label>Description</label>
          <textarea name="service<%= i %>_desc" rows="2"><%= copy['service' + i + '_desc'] || '' %></textarea>
        </div>
        <div class="ep-row">
          <div class="ep-field">
            <label>Link</label>
            <input type="text" name="service<%= i %>_link" value="<%= copy['service' + i + '_link'] || '' %>" placeholder="/services">
          </div>
          <div class="ep-field">
            <label>Image URL</label>
            <input type="text" name="service<%= i %>_image" value="<%= copy['service' + i + '_image'] || '' %>" placeholder="/images/...">
          </div>
        </div>
        <div class="ep-field">
          <label style="display:flex;align-items:center;gap:6px;font-size:0.72rem;text-transform:none;letter-spacing:0;">
            <input type="checkbox" name="service<%= i %>_hidden" value="true" <%= copy['service' + i + '_hidden'] === 'true' ? 'checked' : '' %> style="width:14px;height:14px;accent-color:var(--navy);">
            Hide this card from the live page
          </label>
        </div>
      </div>
      <% }); %>
      <button type="button" class="dp-rep-add" onclick="repAdd(this)">+ Add Service</button>
    </div>
  </div>
</div>

<%# ─── 7. ABOUT COPY ─── %>
<div class="editor-section" data-tab="copy">
  <button type="button" class="editor-section-toggle" onclick="toggleSection(this)">
    About <span class="arrow">&#9654;</span>
  </button>
  <div class="editor-section-body">
    <div class="ep-field">
      <label>Eyebrow</label>
      <input type="text" name="about_eyebrow" value="<%= copy.about_eyebrow || '' %>">
    </div>
    <div class="ep-field">
      <label>Quote</label>
      <textarea name="about_quote" rows="2"><%= copy.about_quote || '' %></textarea>
    </div>
    <div class="ep-field">
      <label>Description</label>
      <textarea name="about_desc" rows="3"><%= copy.about_desc || '' %></textarea>
    </div>
    <div class="ep-row">
      <div class="ep-field">
        <label>Signature</label>
        <input type="text" name="about_sig" value="<%= copy.about_sig || '' %>" placeholder="Founder name">
      </div>
      <div class="ep-field">
        <label>Initial</label>
        <input type="text" name="about_initial" value="<%= copy.about_initial || '' %>" placeholder="Large letter">
      </div>
    </div>
    <%
      const _statIdx = new Set();
      Object.keys(copy).forEach(k => { const m = k.match(/^about_stat(\d+)_/); if (m) _statIdx.add(parseInt(m[1], 10)); });
      const _statArr = [..._statIdx].sort((a, b) => a - b);
      if (_statArr.length === 0) _statArr.push(1, 2, 3, 4);
    %>
    <div class="dp-repeater" style="margin-top:8px;"
         data-prefix="about_stat"
         data-noun="Stat"
         data-fields='[{"key":"num","label":"Number","type":"text","placeholder":"e.g. 150+"},{"key":"label","label":"Label","type":"text","placeholder":"e.g. Projects"},{"key":"hidden","label":"Visibility","type":"checkbox","checkboxLabel":"Hide this stat from the live page"}]'>
      <% _statArr.forEach(function(i, iArr) { %>
      <div class="dp-rep-item" data-idx="<%= i %>">
        <div class="dp-rep-head">
          <span>Stat <%= i %></span>
          <div class="dp-rep-actions">
            <button type="button" data-dir="-1" onclick="repMove(this, -1)" title="Move up"<%= iArr === 0 ? ' disabled' : '' %>>↑</button>
            <button type="button" data-dir="1" onclick="repMove(this, 1)" title="Move down"<%= iArr === _statArr.length - 1 ? ' disabled' : '' %>>↓</button>
            <button type="button" class="dp-rep-remove" onclick="repRemove(this)" title="Remove this stat">✕</button>
          </div>
        </div>
        <div class="ep-row">
          <div class="ep-field">
            <label>Number</label>
            <input type="text" name="about_stat<%= i %>_num" value="<%= copy['about_stat' + i + '_num'] || '' %>" placeholder="e.g. 150+">
          </div>
          <div class="ep-field">
            <label>Label</label>
            <input type="text" name="about_stat<%= i %>_label" value="<%= copy['about_stat' + i + '_label'] || '' %>" placeholder="e.g. Projects">
          </div>
        </div>
        <div class="ep-field">
          <label style="display:flex;align-items:center;gap:6px;font-size:0.72rem;text-transform:none;letter-spacing:0;">
            <input type="checkbox" name="about_stat<%= i %>_hidden" value="true" <%= copy['about_stat' + i + '_hidden'] === 'true' ? 'checked' : '' %> style="width:14px;height:14px;accent-color:var(--navy);">
            Hide this stat from the live page
          </label>
        </div>
      </div>
      <% }); %>
      <button type="button" class="dp-rep-add" onclick="repAdd(this)">+ Add Stat</button>
    </div>
  </div>
</div>

<%# ─── 8. PROCESS COPY ─── %>
<div class="editor-section" data-tab="copy">
  <button type="button" class="editor-section-toggle" onclick="toggleSection(this)">
    Process <span class="arrow">&#9654;</span>
  </button>
  <div class="editor-section-body">
    <div class="ep-field">
      <label>Section Label</label>
      <input type="text" name="process_label" value="<%= copy.process_label || '' %>">
    </div>
    <div class="ep-row">
      <div class="ep-field">
        <label>Heading</label>
        <input type="text" name="process_heading" value="<%= copy.process_heading || '' %>">
      </div>
      <div class="ep-field">
        <label>Heading Emphasis</label>
        <input type="text" name="process_heading_em" value="<%= copy.process_heading_em || '' %>">
      </div>
    </div>
    <%
      const _procIdx = new Set();
      Object.keys(copy).forEach(k => { const m = k.match(/^process(\d+)_/); if (m) _procIdx.add(parseInt(m[1], 10)); });
      const _procArr = [..._procIdx].sort((a, b) => a - b);
      if (_procArr.length === 0) _procArr.push(1, 2, 3, 4);
    %>
    <div class="dp-repeater"
         data-prefix="process"
         data-noun="Step"
         data-fields='[{"key":"title","label":"Title","type":"text"},{"key":"desc","label":"Description","type":"textarea","rows":2},{"key":"hidden","label":"Visibility","type":"checkbox","checkboxLabel":"Hide this step from the live page"}]'>
      <% _procArr.forEach(function(i, iArr) { %>
      <div class="dp-rep-item" data-idx="<%= i %>">
        <div class="dp-rep-head">
          <span>Step <%= i %></span>
          <div class="dp-rep-actions">
            <button type="button" data-dir="-1" onclick="repMove(this, -1)" title="Move up"<%= iArr === 0 ? ' disabled' : '' %>>↑</button>
            <button type="button" data-dir="1" onclick="repMove(this, 1)" title="Move down"<%= iArr === _procArr.length - 1 ? ' disabled' : '' %>>↓</button>
            <button type="button" class="dp-rep-remove" onclick="repRemove(this)" title="Remove this step">✕</button>
          </div>
        </div>
        <div class="ep-field">
          <label>Title</label>
          <input type="text" name="process<%= i %>_title" value="<%= copy['process' + i + '_title'] || '' %>">
        </div>
        <div class="ep-field">
          <label>Description</label>
          <textarea name="process<%= i %>_desc" rows="2"><%= copy['process' + i + '_desc'] || '' %></textarea>
        </div>
        <div class="ep-field">
          <label style="display:flex;align-items:center;gap:6px;font-size:0.72rem;text-transform:none;letter-spacing:0;">
            <input type="checkbox" name="process<%= i %>_hidden" value="true" <%= copy['process' + i + '_hidden'] === 'true' ? 'checked' : '' %> style="width:14px;height:14px;accent-color:var(--navy);">
            Hide this step from the live page
          </label>
        </div>
      </div>
      <% }); %>
      <button type="button" class="dp-rep-add" onclick="repAdd(this)">+ Add Step</button>
    </div>
  </div>
</div>

<%# ─── 8b. CUSTOM SECTIONS ─── %>
<%# ─── 8b. CUSTOM SECTIONS — inline copy editors, one accordion per section ─── %>
<% _validSections.forEach(function(sec) { %>
  <% const _tmpl = _sectionTemplates[sec.type] || {};
     const _fields = _tmpl.fields || [];
     const _hidden = sec.visible === false;
  %>
  <div class="editor-section cs-edit" data-tab="copy" data-cs-id="<%= sec._id %>">
    <button type="button" class="editor-section-toggle" onclick="toggleSection(this)">
      <span><%= _tmpl.icon || '▦' %> <%= sec.label || 'Custom Section' %></span>
      <% if (_hidden) { %><span style="font-size:0.55rem;font-weight:700;letter-spacing:0.12em;background:rgba(0,0,0,0.06);color:var(--slate);padding:1px 5px;border-radius:2px;margin-left:6px;">HIDDEN</span><% } %>
      <span class="arrow">&#9654;</span>
    </button>
    <div class="editor-section-body">
      <div class="ep-hint" style="margin-bottom:8px;">
        <strong><%= _tmpl.label || sec.type %></strong> — order & visibility under Section Visibility (Sections tab).
      </div>
      <%
        // Partition template fields into singletons vs. repeatable groups
        // (cards1_*..cardN_* and q1/a1..qN/aN). Anything outside known
        // repeatable patterns renders as a regular singleton input.
        const _cardRx = /^card(\d+)_(title|body|image|link)$/;
        const _faqRx = /^(q|a)(\d+)$/;
        const _singletons = [];
        _fields.forEach(function(f) {
          if (sec.type === 'cards' && _cardRx.test(f.key)) return;
          if (sec.type === 'faq' && _faqRx.test(f.key)) return;
          _singletons.push(f);
        });
      %>
      <% _singletons.forEach(function(f) { %>
        <div class="ep-field">
          <label><%= f.label %></label>
          <% if (f.type === 'textarea') { %>
            <textarea data-cs-field="<%= f.key %>" rows="<%= f.rows || 2 %>"><%= (sec.fields && sec.fields[f.key]) || '' %></textarea>
          <% } else if (f.type === 'select') { %>
            <% const _cur = (sec.fields && sec.fields[f.key] != null) ? String(sec.fields[f.key]) : ''; %>
            <select data-cs-field="<%= f.key %>">
              <% (f.options || []).forEach(function(o) { %>
                <option value="<%= o %>" <%= _cur === String(o) ? 'selected' : '' %>><%= o %></option>
              <% }); %>
            </select>
          <% } else { %>
            <input type="text" data-cs-field="<%= f.key %>" value="<%= (sec.fields && sec.fields[f.key]) ? String(sec.fields[f.key]).replace(/"/g, '&quot;') : '' %>"<%= f.placeholder ? ' placeholder="' + f.placeholder + '"' : '' %>>
          <% } %>
        </div>
      <% }); %>

      <% if (sec.type === 'cards') { %>
        <%
          const _cardIdx = new Set();
          Object.keys(sec.fields || {}).forEach(k => { const m = k.match(_cardRx); if (m) _cardIdx.add(parseInt(m[1], 10)); });
          const _cardArr = [..._cardIdx].sort((a, b) => a - b);
          if (_cardArr.length === 0) _cardArr.push(1, 2, 3, 4);
        %>
        <div class="dp-repeater cs-repeater" style="margin-top:10px;"
             data-prefix="card"
             data-noun="Card"
             data-fields='[{"key":"title","label":"Title","type":"text"},{"key":"body","label":"Body","type":"textarea","rows":2},{"key":"image","label":"Image URL","type":"text","placeholder":"/images/..."},{"key":"link","label":"Link URL","type":"text","placeholder":"/products/sku or https://..."}]'>
          <% _cardArr.forEach(function(i, iArr) { %>
          <div class="dp-rep-item" data-idx="<%= i %>">
            <div class="dp-rep-head">
              <span>Card <%= i %></span>
              <div class="dp-rep-actions">
                <button type="button" data-dir="-1" onclick="repMove(this, -1)" title="Move up"<%= iArr === 0 ? ' disabled' : '' %>>↑</button>
                <button type="button" data-dir="1" onclick="repMove(this, 1)" title="Move down"<%= iArr === _cardArr.length - 1 ? ' disabled' : '' %>>↓</button>
                <button type="button" class="dp-rep-remove" onclick="csRepRemove(this)" title="Remove this card">✕</button>
              </div>
            </div>
            <div class="ep-field">
              <label>Title</label>
              <input type="text" data-cs-field="card<%= i %>_title" value="<%= (sec.fields && sec.fields['card' + i + '_title']) ? String(sec.fields['card' + i + '_title']).replace(/"/g, '&quot;') : '' %>">
            </div>
            <div class="ep-field">
              <label>Body</label>
              <textarea data-cs-field="card<%= i %>_body" rows="2"><%= (sec.fields && sec.fields['card' + i + '_body']) || '' %></textarea>
            </div>
            <div class="ep-field">
              <label>Image URL</label>
              <input type="text" data-cs-field="card<%= i %>_image" value="<%= (sec.fields && sec.fields['card' + i + '_image']) ? String(sec.fields['card' + i + '_image']).replace(/"/g, '&quot;') : '' %>" placeholder="/images/...">
            </div>
            <div class="ep-field">
              <label>Link URL <span style="color:var(--muted);font-weight:400;font-size:0.78em;">(makes the card clickable)</span></label>
              <input type="text" data-cs-field="card<%= i %>_link" value="<%= (sec.fields && sec.fields['card' + i + '_link']) ? String(sec.fields['card' + i + '_link']).replace(/"/g, '&quot;') : '' %>" placeholder="/products/sku or https://...">
            </div>
          </div>
          <% }); %>
          <button type="button" class="dp-rep-add" onclick="csRepAdd(this)">+ Add Card</button>
        </div>
      <% } else if (sec.type === 'faq') { %>
        <%
          const _faqIdx = new Set();
          Object.keys(sec.fields || {}).forEach(k => { const m = k.match(/^q(\d+)$/); if (m) _faqIdx.add(parseInt(m[1], 10)); });
          const _faqArr = [..._faqIdx].sort((a, b) => a - b);
          if (_faqArr.length === 0) _faqArr.push(1, 2, 3, 4, 5);
        %>
        <div class="dp-repeater cs-repeater" style="margin-top:10px;"
             data-prefix="q"
             data-noun="Q&amp;A"
             data-fields='[{"keyMode":"only","prefix":"q","labelTpl":"Question {n}","type":"text"},{"keyMode":"only","prefix":"a","labelTpl":"Answer {n}","type":"textarea","rows":2}]'>
          <% _faqArr.forEach(function(i, iArr) { %>
          <div class="dp-rep-item" data-idx="<%= i %>">
            <div class="dp-rep-head">
              <span>Q&amp;A <%= i %></span>
              <div class="dp-rep-actions">
                <button type="button" data-dir="-1" onclick="repMove(this, -1)" title="Move up"<%= iArr === 0 ? ' disabled' : '' %>>↑</button>
                <button type="button" data-dir="1" onclick="repMove(this, 1)" title="Move down"<%= iArr === _faqArr.length - 1 ? ' disabled' : '' %>>↓</button>
                <button type="button" class="dp-rep-remove" onclick="csRepRemove(this)" title="Remove this Q&amp;A">✕</button>
              </div>
            </div>
            <div class="ep-field">
              <label>Question <%= i %></label>
              <input type="text" data-cs-field="q<%= i %>" value="<%= (sec.fields && sec.fields['q' + i]) ? String(sec.fields['q' + i]).replace(/"/g, '&quot;') : '' %>">
            </div>
            <div class="ep-field">
              <label>Answer <%= i %></label>
              <textarea data-cs-field="a<%= i %>" rows="2"><%= (sec.fields && sec.fields['a' + i]) || '' %></textarea>
            </div>
          </div>
          <% }); %>
          <button type="button" class="dp-rep-add" onclick="csRepAdd(this)">+ Add Q&amp;A</button>
        </div>
      <% } %>

      <div style="display:flex;gap:6px;margin-top:10px;">
        <button type="button" class="btn btn-navy" style="font-size:0.62rem;padding:5px 12px;letter-spacing:0.08em;" onclick="csSave('<%= sec._id %>', this)">Save Section</button>
        <button type="button" class="btn btn-ghost" style="font-size:0.62rem;padding:5px 12px;letter-spacing:0.08em;color:#8B1C1C;" onclick="csDelete('<%= sec._id %>', this)">Delete</button>
      </div>
    </div>
  </div>
<% }); %>

<%# ── + Add Custom Section (always visible in Copy tab) ── %>
<div class="editor-section open" data-tab="copy" id="cs-add-section">
  <button type="button" class="editor-section-toggle" onclick="toggleSection(this)">
    + Add Custom Section <span class="arrow">&#9654;</span>
  </button>
  <div class="editor-section-body">
    <div class="ep-field">
      <label>Section Type</label>
      <select id="cs-new-type">
        <% Object.entries(_sectionTemplates).forEach(function([key, t]) { %>
          <option value="<%= key %>"><%= t.icon || '▦' %> <%= t.label || key %></option>
        <% }); %>
      </select>
    </div>
    <div class="ep-field">
      <label>Label (for admin use)</label>
      <input type="text" id="cs-new-label" placeholder="e.g. Featured Products, Why Choose Us">
    </div>
    <div id="cs-new-err" style="display:none;color:#8B1C1C;font-size:0.72rem;margin-bottom:6px;"></div>
    <button type="button" class="btn btn-navy" style="font-size:0.62rem;padding:6px 14px;letter-spacing:0.08em;" onclick="csCreate(this)">Create Section</button>
  </div>
</div>

<%# ─── 9. PRICING TIERS ─── %>
<div class="editor-section" data-tab="copy">
  <button type="button" class="editor-section-toggle" onclick="toggleSection(this)">
    Pricing Tiers <span class="arrow">&#9654;</span>
  </button>
  <div class="editor-section-body">
    <div class="ep-field">
      <label>Section Heading</label>
      <input type="text" name="startup_price_heading" value="<%= copy.startup_price_heading || '' %>" placeholder="e.g. Simple Pricing">
    </div>
    <div class="ep-field">
      <label>Section Description</label>
      <textarea name="startup_price_desc" rows="2" placeholder="Pricing section subheading"><%= copy.startup_price_desc || '' %></textarea>
    </div>
    <div class="ep-row">
      <div class="ep-field">
        <label>CTA Button Text</label>
        <input type="text" name="startup_price_cta" value="<%= copy.startup_price_cta || '' %>" placeholder="Get Started">
      </div>
      <div class="ep-field">
        <label>Footer Note</label>
        <input type="text" name="startup_price_note" value="<%= copy.startup_price_note || '' %>" placeholder="e.g. Cancel anytime">
      </div>
    </div>

    <%# ── Tier 1 (startup / primary) ── %>
    <div style="margin-top:10px;padding-top:8px;border-top:1px solid var(--border);">
      <div style="font-size:0.62rem;font-weight:600;color:var(--slate);letter-spacing:0.1em;margin-bottom:6px;">TIER 1 (PRIMARY)</div>
      <div class="ep-row">
        <div class="ep-field">
          <label>Label</label>
          <input type="text" name="startup_price_label" value="<%= copy.startup_price_label || '' %>" placeholder="e.g. Monthly">
        </div>
        <div class="ep-field">
          <label>Amount</label>
          <input type="text" name="startup_price_amount" value="<%= copy.startup_price_amount || '' %>" placeholder="$50">
        </div>
      </div>
      <div class="ep-field">
        <label>Unit</label>
        <input type="text" name="startup_price_unit" value="<%= copy.startup_price_unit || '' %>" placeholder="/mo">
      </div>
      <div class="ep-field">
        <label>Features (comma-separated)</label>
        <textarea name="startup_price_features" rows="2" placeholder="Feature 1, Feature 2, Feature 3"><%= copy.startup_price_features || '' %></textarea>
      </div>
    </div>

    <%# ── Tier 2 ── %>
    <div style="margin-top:10px;padding-top:8px;border-top:1px solid var(--border);">
      <div style="font-size:0.62rem;font-weight:600;color:var(--slate);letter-spacing:0.1em;margin-bottom:6px;">TIER 2</div>
      <div class="ep-row">
        <div class="ep-field">
          <label>Label</label>
          <input type="text" name="pricing_tier2_label" value="<%= copy.pricing_tier2_label || '' %>" placeholder="e.g. Quarterly">
        </div>
        <div class="ep-field">
          <label>Amount</label>
          <input type="text" name="pricing_tier2_amount" value="<%= copy.pricing_tier2_amount || '' %>" placeholder="$120">
        </div>
      </div>
      <div class="ep-row">
        <div class="ep-field">
          <label>Unit</label>
          <input type="text" name="pricing_tier2_unit" value="<%= copy.pricing_tier2_unit || '' %>" placeholder="/qtr">
        </div>
        <div class="ep-field">
          <label>Equiv Note</label>
          <input type="text" name="pricing_tier2_equiv" value="<%= copy.pricing_tier2_equiv || '' %>" placeholder="~$40/mo">
        </div>
      </div>
      <div class="ep-field">
        <label>CTA Link</label>
        <input type="text" name="pricing_tier2_cta_link" value="<%= copy.pricing_tier2_cta_link || '' %>" placeholder="/start or #contact">
        <div class="ep-hint">Leave blank to use default</div>
      </div>
    </div>

    <%# ── Tier 3 ── %>
    <div style="margin-top:10px;padding-top:8px;border-top:1px solid var(--border);">
      <div style="font-size:0.62rem;font-weight:600;color:var(--slate);letter-spacing:0.1em;margin-bottom:6px;">TIER 3</div>
      <div class="ep-row">
        <div class="ep-field">
          <label>Label</label>
          <input type="text" name="pricing_tier3_label" value="<%= copy.pricing_tier3_label || '' %>" placeholder="e.g. Annual">
        </div>
        <div class="ep-field">
          <label>Amount</label>
          <input type="text" name="pricing_tier3_amount" value="<%= copy.pricing_tier3_amount || '' %>" placeholder="$300">
        </div>
      </div>
      <div class="ep-row">
        <div class="ep-field">
          <label>Unit</label>
          <input type="text" name="pricing_tier3_unit" value="<%= copy.pricing_tier3_unit || '' %>" placeholder="/yr">
        </div>
        <div class="ep-field">
          <label>Equiv Note</label>
          <input type="text" name="pricing_tier3_equiv" value="<%= copy.pricing_tier3_equiv || '' %>" placeholder="$25/mo — 50% off">
        </div>
      </div>
      <div class="ep-row">
        <div class="ep-field">
          <label>CTA Link</label>
          <input type="text" name="pricing_tier3_cta_link" value="<%= copy.pricing_tier3_cta_link || '' %>" placeholder="/start or #contact">
        </div>
        <div class="ep-field">
          <label style="margin-bottom:10px;">&nbsp;</label>
          <label style="font-size:0.75rem;text-transform:none;letter-spacing:0;display:flex;align-items:center;gap:6px;cursor:pointer;">
            <input type="checkbox" name="pricing_tier3_featured" value="yes" <%= copy.pricing_tier3_featured === 'yes' ? 'checked' : '' %> style="width:14px;height:14px;accent-color:var(--navy);"> Featured
          </label>
        </div>
      </div>
    </div>

    <%# ── Tier 4 ── %>
    <div style="margin-top:10px;padding-top:8px;border-top:1px solid var(--border);">
      <div style="font-size:0.62rem;font-weight:600;color:var(--slate);letter-spacing:0.1em;margin-bottom:6px;">TIER 4</div>
      <div class="ep-row">
        <div class="ep-field">
          <label>Label</label>
          <input type="text" name="pricing_tier4_label" value="<%= copy.pricing_tier4_label || '' %>" placeholder="e.g. Lifetime">
        </div>
        <div class="ep-field">
          <label>Amount</label>
          <input type="text" name="pricing_tier4_amount" value="<%= copy.pricing_tier4_amount || '' %>" placeholder="$1,200">
        </div>
      </div>
      <div class="ep-row">
        <div class="ep-field">
          <label>Unit</label>
          <input type="text" name="pricing_tier4_unit" value="<%= copy.pricing_tier4_unit || '' %>" placeholder="one-time">
        </div>
        <div class="ep-field">
          <label>Equiv Note</label>
          <input type="text" name="pricing_tier4_equiv" value="<%= copy.pricing_tier4_equiv || '' %>" placeholder="2-year equivalent">
        </div>
      </div>
      <div class="ep-field">
        <label>CTA Link</label>
        <input type="text" name="pricing_tier4_cta_link" value="<%= copy.pricing_tier4_cta_link || '' %>" placeholder="/start or #contact">
      </div>
    </div>

    <%# ── Promo Banner ── %>
    <div style="margin-top:10px;padding-top:8px;border-top:1px solid var(--border);">
      <div style="font-size:0.62rem;font-weight:600;color:var(--slate);letter-spacing:0.1em;margin-bottom:6px;">PROMO BANNER</div>
      <div class="ep-field">
        <label style="font-size:0.75rem;text-transform:none;letter-spacing:0;display:flex;align-items:center;gap:6px;cursor:pointer;">
          <input type="checkbox" name="promo_enabled" value="yes" <%= copy.promo_enabled === 'yes' ? 'checked' : '' %> style="width:14px;height:14px;accent-color:var(--navy);"> Enable promo banner
        </label>
      </div>
      <div class="ep-row">
        <div class="ep-field">
          <label>Badge</label>
          <input type="text" name="promo_badge" value="<%= copy.promo_badge || '' %>" placeholder="NEW">
        </div>
        <div class="ep-field">
          <label>Heading</label>
          <input type="text" name="promo_heading" value="<%= copy.promo_heading || '' %>" placeholder="Limited Time Offer">
        </div>
      </div>
      <div class="ep-field">
        <label>Text</label>
        <textarea name="promo_text" rows="2" placeholder="Promo details..."><%= copy.promo_text || '' %></textarea>
      </div>
    </div>
  </div>
</div>

<%# ─── 10. CONTACT COPY ─── %>
<div class="editor-section" data-tab="copy">
  <button type="button" class="editor-section-toggle" onclick="toggleSection(this)">
    Contact <span class="arrow">&#9654;</span>
  </button>
  <div class="editor-section-body">
    <div class="ep-field">
      <label>Eyebrow</label>
      <input type="text" name="contact_eyebrow" value="<%= copy.contact_eyebrow || '' %>">
    </div>
    <div class="ep-field">
      <label>Heading</label>
      <input type="text" name="contact_heading" value="<%= copy.contact_heading || '' %>">
    </div>
    <div class="ep-row">
      <div class="ep-field">
        <label>Heading Emphasis</label>
        <input type="text" name="contact_heading_em" value="<%= copy.contact_heading_em || '' %>">
      </div>
      <div class="ep-field">
        <label>Subheading</label>
        <input type="text" name="contact_sub" value="<%= copy.contact_sub || '' %>">
      </div>
    </div>
    <div style="margin-top:8px;padding-top:6px;border-top:1px solid var(--border);">
      <div style="font-size:0.62rem;font-weight:600;color:var(--slate);letter-spacing:0.1em;margin-bottom:6px;">DETAIL ITEMS</div>
      <div class="ep-row">
        <div class="ep-field">
          <label>Location Title</label>
          <input type="text" name="contact_location_label" value="<%= copy.contact_location_label || '' %>" placeholder="Location">
        </div>
        <div class="ep-field">
          <label>Location Value</label>
          <input type="text" name="contact_location" value="<%= copy.contact_location || '' %>" placeholder="<%= brand?.location || '' %>">
        </div>
      </div>
      <div class="ep-row">
        <div class="ep-field">
          <label>Serving Title</label>
          <input type="text" name="contact_serving_label" value="<%= copy.contact_serving_label || '' %>" placeholder="Serving">
        </div>
        <div class="ep-field">
          <label>Serving Value</label>
          <input type="text" name="contact_serving" value="<%= copy.contact_serving || '' %>" placeholder="<%= brand?.serviceArea || '' %>">
        </div>
      </div>
      <div class="ep-row">
        <div class="ep-field">
          <label>Services Title</label>
          <input type="text" name="contact_services_label" value="<%= copy.contact_services_label || '' %>" placeholder="Services">
        </div>
        <div class="ep-field">
          <label>Services Value</label>
          <input type="text" name="contact_services" value="<%= copy.contact_services || '' %>">
        </div>
      </div>
    </div>
    <div class="ep-field">
      <label>Submit Button</label>
      <input type="text" name="contact_btn" value="<%= copy.contact_btn || '' %>" placeholder="Send Message">
    </div>
    <div style="margin-top:8px;padding-top:6px;border-top:1px solid var(--border);">
      <div style="font-size:0.62rem;font-weight:600;color:var(--slate);letter-spacing:0.1em;margin-bottom:6px;">SECTION COLORS</div>
      <div class="ep-row">
        <div class="ep-field">
          <label>Left Panel BG</label>
          <div class="color-row">
            <input type="color" name="contact_bg" value="<%= design.contact_bg || design.color_primary_deep %>">
            <input type="text" value="<%= design.contact_bg || '' %>" placeholder="(primary deep)" readonly>
          </div>
        </div>
        <div class="ep-field">
          <label>Form Panel BG</label>
          <div class="color-row">
            <input type="color" name="contact_form_bg" value="<%= design.contact_form_bg || design.color_bg %>">
            <input type="text" value="<%= design.contact_form_bg || '' %>" placeholder="(bg)" readonly>
          </div>
        </div>
      </div>
      <div class="ep-row">
        <div class="ep-field">
          <label>Heading</label>
          <div class="color-row">
            <input type="color" name="contact_heading_color" value="<%= design.contact_heading_color || '#FFFFFF' %>">
            <input type="text" value="<%= design.contact_heading_color || '' %>" placeholder="(white)" readonly>
          </div>
        </div>
        <div class="ep-field">
          <label>Eyebrow</label>
          <div class="color-row">
            <input type="color" name="contact_eyebrow_color" value="<%= design.contact_eyebrow_color || design.color_accent_light %>">
            <input type="text" value="<%= design.contact_eyebrow_color || '' %>" placeholder="(accent light)" readonly>
          </div>
        </div>
      </div>
      <div class="ep-row">
        <div class="ep-field">
          <label>Body Text</label>
          <div class="color-row">
            <input type="color" name="contact_text_color" value="<%= design.contact_text_color || '#FDFCFA' %>">
            <input type="text" value="<%= design.contact_text_color || '' %>" placeholder="(auto)" readonly>
          </div>
        </div>
        <div class="ep-field">
          <label>Detail Labels</label>
          <div class="color-row">
            <input type="color" name="contact_label_color" value="<%= design.contact_label_color || design.color_accent %>">
            <input type="text" value="<%= design.contact_label_color || '' %>" placeholder="(accent)" readonly>
          </div>
        </div>
      </div>
      <div class="ep-row">
        <div class="ep-field">
          <label>Detail Values</label>
          <div class="color-row">
            <input type="color" name="contact_value_color" value="<%= design.contact_value_color || '#FDFCFA' %>">
            <input type="text" value="<%= design.contact_value_color || '' %>" placeholder="(auto)" readonly>
          </div>
        </div>
        <div class="ep-field">
          <label>Form Labels</label>
          <div class="color-row">
            <input type="color" name="contact_form_label_color" value="<%= design.contact_form_label_color || '#6B7380' %>">
            <input type="text" value="<%= design.contact_form_label_color || '' %>" placeholder="(muted)" readonly>
          </div>
        </div>
      </div>
      <div class="ep-row">
        <div class="ep-field">
          <label>Button BG</label>
          <div class="color-row">
            <input type="color" name="contact_btn_bg" value="<%= design.contact_btn_bg || design.color_primary %>">
            <input type="text" value="<%= design.contact_btn_bg || '' %>" placeholder="(primary)" readonly>
          </div>
        </div>
        <div class="ep-field">
          <label>Button Text</label>
          <div class="color-row">
            <input type="color" name="contact_btn_color" value="<%= design.contact_btn_color || '#FFFFFF' %>">
            <input type="text" value="<%= design.contact_btn_color || '' %>" placeholder="(white)" readonly>
          </div>
        </div>
      </div>
    </div>
    <div style="margin-top:8px;padding-top:6px;border-top:1px solid var(--border);">
      <div style="font-size:0.62rem;font-weight:600;color:var(--slate);letter-spacing:0.1em;margin-bottom:6px;">FORM FIELDS</div>
      <div class="ep-hint" style="margin-bottom:8px;">Check <strong>Hide</strong> to remove a stock field from the public form. Tenants can also add custom fields below.</div>
      <%
        const contactFormFields = [
          { prefix: 'contact_fname',    lbl: 'First Name' },
          { prefix: 'contact_lname',    lbl: 'Last Name' },
          { prefix: 'contact_email',    lbl: 'Email' },
          { prefix: 'contact_company',  lbl: 'Company' },
          { prefix: 'contact_service',  lbl: 'Service' },
          { prefix: 'contact_message',  lbl: 'Message' },
        ];
      %>
      <% contactFormFields.forEach(cf => { %>
      <div style="margin-bottom:10px;padding:8px 10px;border:1px solid var(--border);border-radius:2px;background:var(--ivory);">
        <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:6px;">
          <strong style="font-size:0.7rem;letter-spacing:0.08em;text-transform:uppercase;color:var(--on-surface);"><%= cf.lbl %></strong>
          <label style="display:flex;align-items:center;gap:6px;font-size:0.68rem;font-weight:600;color:var(--slate);cursor:pointer;">
            <input type="checkbox" name="<%= cf.prefix %>_hidden" value="true"
              <%= copy[cf.prefix + '_hidden'] === 'true' ? 'checked' : '' %>
              style="width:14px;height:14px;accent-color:var(--navy);">
            Hide field
          </label>
        </div>
        <div class="ep-row">
          <div class="ep-field">
            <label>Label</label>
            <input type="text" name="<%= cf.prefix %>_label" value="<%= copy[cf.prefix + '_label'] || '' %>">
          </div>
          <div class="ep-field">
            <label>Placeholder</label>
            <input type="text" name="<%= cf.prefix %>_placeholder" value="<%= copy[cf.prefix + '_placeholder'] || '' %>">
          </div>
        </div>
      </div>
      <% }) %>
      <div class="ep-row" style="margin-top:6px;">
        <div class="ep-field">
          <label>Service Fallback</label>
          <input type="text" name="contact_service_fallback" value="<%= copy.contact_service_fallback || '' %>">
        </div>
        <div class="ep-field">
          <label>Service Extra</label>
          <input type="text" name="contact_service_extra" value="<%= copy.contact_service_extra || '' %>">
        </div>
      </div>
    </div>

    <%# ── Custom contact fields (repeater) ── %>
    <div style="margin-top:12px;padding-top:8px;border-top:1px solid var(--border);">
      <div style="font-size:0.62rem;font-weight:600;color:var(--slate);letter-spacing:0.1em;margin-bottom:6px;">CUSTOM FIELDS</div>
      <div class="ep-hint" style="margin-bottom:8px;">Add extra fields (phone, budget, dropdown, etc). These render between the stock fields and the submit button.</div>
      <%
        const _cfIdx = new Set();
        Object.keys(copy).forEach(k => { const m = k.match(/^contact_field(\d+)_/); if (m) _cfIdx.add(parseInt(m[1], 10)); });
        const _cfArr = [..._cfIdx].sort((a, b) => a - b);
      %>
      <div class="dp-repeater"
           data-prefix="contact_field"
           data-noun="Field"
           data-fields='[{"key":"label","label":"Label","type":"text","placeholder":"e.g. Phone Number"},{"key":"name","label":"Name attribute","type":"text","placeholder":"phone (defaults to field{n})"},{"key":"placeholder","label":"Placeholder","type":"text"},{"key":"type","label":"Type","type":"select","options":["text","email","tel","url","number","textarea","select"]},{"key":"options","label":"Select options (pipe-separated)","type":"text","placeholder":"Option A | Option B | Option C"},{"key":"required","label":"Required","type":"checkbox","checkboxLabel":"Make this field required"}]'>
        <% _cfArr.forEach(function(i, iArr) { %>
        <div class="dp-rep-item" data-idx="<%= i %>">
          <div class="dp-rep-head">
            <span>Field <%= i %></span>
            <div class="dp-rep-actions">
              <button type="button" data-dir="-1" onclick="repMove(this, -1)" title="Move up"<%= iArr === 0 ? ' disabled' : '' %>>↑</button>
              <button type="button" data-dir="1" onclick="repMove(this, 1)" title="Move down"<%= iArr === _cfArr.length - 1 ? ' disabled' : '' %>>↓</button>
              <button type="button" class="dp-rep-remove" onclick="repRemove(this)" title="Remove this field">✕</button>
            </div>
          </div>
          <div class="ep-row">
            <div class="ep-field">
              <label>Label</label>
              <input type="text" name="contact_field<%= i %>_label" value="<%= copy['contact_field' + i + '_label'] || '' %>" placeholder="e.g. Phone Number">
            </div>
            <div class="ep-field">
              <label>Name attribute</label>
              <input type="text" name="contact_field<%= i %>_name" value="<%= copy['contact_field' + i + '_name'] || '' %>" placeholder="phone (defaults to field<%= i %>)">
            </div>
          </div>
          <div class="ep-field">
            <label>Placeholder</label>
            <input type="text" name="contact_field<%= i %>_placeholder" value="<%= copy['contact_field' + i + '_placeholder'] || '' %>">
          </div>
          <div class="ep-row">
            <div class="ep-field">
              <label>Type</label>
              <select name="contact_field<%= i %>_type">
                <% ['text','email','tel','url','number','textarea','select'].forEach(function(t) { %>
                  <option value="<%= t %>" <%= (copy['contact_field' + i + '_type'] || 'text') === t ? 'selected' : '' %>><%= t %></option>
                <% }) %>
              </select>
            </div>
            <div class="ep-field">
              <label>Select options (pipe-separated)</label>
              <input type="text" name="contact_field<%= i %>_options" value="<%= copy['contact_field' + i + '_options'] || '' %>" placeholder="Option A | Option B | Option C">
            </div>
          </div>
          <div class="ep-field">
            <label style="display:flex;align-items:center;gap:6px;font-size:0.72rem;text-transform:none;letter-spacing:0;">
              <input type="checkbox" name="contact_field<%= i %>_required" value="yes"
                <%= copy['contact_field' + i + '_required'] === 'yes' ? 'checked' : '' %>
                style="width:14px;height:14px;accent-color:var(--navy);">
              Make this field required
            </label>
          </div>
        </div>
        <% }); %>
        <button type="button" class="dp-rep-add" onclick="repAdd(this)">+ Add Field</button>
      </div>
    </div>
  </div>
</div>

<%# ─── COOKIE CONSENT (copy) ─── %>
<div class="editor-section" data-tab="copy">
  <button type="button" class="editor-section-toggle" onclick="toggleSection(this)">
    Cookie Consent <span class="arrow">&#9654;</span>
  </button>
  <div class="editor-section-body">
    <div class="ep-hint" style="margin-bottom:10px;">Text for the cookie consent UI. Enable it and set colors under <strong>Design</strong> &rsaquo; Cookie Consent.</div>
    <div class="ep-field">
      <label>Title</label>
      <input type="text" name="cookie_title" value="<%= copy.cookie_title || '' %>" placeholder="We value your privacy">
    </div>
    <div class="ep-field">
      <label>Intro Message</label>
      <textarea name="cookie_message" rows="3"><%= copy.cookie_message || '' %></textarea>
    </div>
    <div class="ep-row-3">
      <div class="ep-field">
        <label>Accept All — Button</label>
        <input type="text" name="cookie_accept_label" value="<%= copy.cookie_accept_label || '' %>" placeholder="Accept all">
      </div>
      <div class="ep-field">
        <label>Reject — Button</label>
        <input type="text" name="cookie_reject_label" value="<%= copy.cookie_reject_label || '' %>" placeholder="Reject non-essential">
      </div>
      <div class="ep-field">
        <label>Save — Button</label>
        <input type="text" name="cookie_save_label" value="<%= copy.cookie_save_label || '' %>" placeholder="Save preferences">
      </div>
    </div>
    <div class="ep-row">
      <div class="ep-field">
        <label>Reopen / Settings Label</label>
        <input type="text" name="cookie_settings_label" value="<%= copy.cookie_settings_label || '' %>" placeholder="Cookie settings">
      </div>
      <div class="ep-field">
        <label>Privacy Link Text</label>
        <input type="text" name="cookie_privacy_text" value="<%= copy.cookie_privacy_text || '' %>" placeholder="Privacy Policy">
      </div>
    </div>
    <div class="ep-field">
      <label>Privacy Link URL</label>
      <input type="text" name="cookie_privacy_link" value="<%= copy.cookie_privacy_link || '' %>" placeholder="/privacy">
    </div>
    <div class="ep-row">
      <div class="ep-field">
        <label>Necessary — Title</label>
        <input type="text" name="cookie_necessary_title" value="<%= copy.cookie_necessary_title || '' %>" placeholder="Strictly necessary">
      </div>
      <div class="ep-field">
        <label>Necessary — Description</label>
        <textarea name="cookie_necessary_desc" rows="2"><%= copy.cookie_necessary_desc || '' %></textarea>
      </div>
    </div>
    <div class="ep-row">
      <div class="ep-field">
        <label>Analytics — Title</label>
        <input type="text" name="cookie_analytics_title" value="<%= copy.cookie_analytics_title || '' %>" placeholder="Analytics">
      </div>
      <div class="ep-field">
        <label>Analytics — Description</label>
        <textarea name="cookie_analytics_desc" rows="2"><%= copy.cookie_analytics_desc || '' %></textarea>
      </div>
    </div>
    <div class="ep-row">
      <div class="ep-field">
        <label>Marketing — Title</label>
        <input type="text" name="cookie_marketing_title" value="<%= copy.cookie_marketing_title || '' %>" placeholder="Marketing">
      </div>
      <div class="ep-field">
        <label>Marketing — Description</label>
        <textarea name="cookie_marketing_desc" rows="2"><%= copy.cookie_marketing_desc || '' %></textarea>
      </div>
    </div>
  </div>
</div>

<%# ─── 11. HERO & SECTION STYLING ─── %>
<div class="editor-section" data-tab="design">
  <button type="button" class="editor-section-toggle" onclick="toggleSection(this)">
    Hero & Section Styling <span class="arrow">&#9654;</span>
  </button>
  <div class="editor-section-body">

    <%# ── Hero style picker (split / slideshow / image_card) ── %>
    <div class="ep-field">
      <label>Hero Style</label>
      <select name="hero_style" id="field-hero_style" onchange="document.querySelectorAll('[data-hero-style-only]').forEach(function(el){ el.style.display = (el.getAttribute('data-hero-style-only').split(',').indexOf(this.value) >= 0) ? '' : 'none'; }.bind(this));">
        <option value="split"       <%= (design.hero_style || 'split') === 'split'       ? 'selected' : '' %>>Split — Text-Typeface Heavy (current)</option>
        <option value="slideshow"   <%= design.hero_style === 'slideshow'                ? 'selected' : '' %>>Slideshow — Multi-slide with dots + linkable</option>
        <option value="image_card"  <%= design.hero_style === 'image_card'               ? 'selected' : '' %>>Image Card — Static image with shadow card overlay</option>
        <option value="parallax"    <%= design.hero_style === 'parallax'                 ? 'selected' : '' %>>Parallax Band — Layered image + floating marquee + copy card</option>
      </select>
      <div class="ep-hint" style="margin-top:6px;">Switch how the top of the landing page renders. Slideshow uses the "Hero Slides" panel below for content. Parallax Band uses the <strong>Ticker / Marquee</strong> panel (background image, typeface size, band copy).</div>
    </div>

    <%# ── Slideshow-only controls ── %>
    <div data-hero-style-only="slideshow" style="<%= design.hero_style === 'slideshow' ? '' : 'display:none;' %>border:1px solid var(--border);border-radius:2px;padding:12px;margin:10px 0;background:var(--surface);">
      <div style="font-size:0.7rem;font-weight:700;letter-spacing:0.12em;text-transform:uppercase;margin-bottom:10px;">Slideshow Settings</div>
      <div class="ep-row">
        <div class="ep-field">
          <label>Auto-advance (seconds, 0 = off)</label>
          <input type="number" min="0" max="60" step="1" name="hero_slideshow_interval" value="<%= design.hero_slideshow_interval || '6' %>">
        </div>
        <div class="ep-field">
          <label>Active Dot Color</label>
          <input type="color" name="hero_slideshow_dot_color" value="<%= design.hero_slideshow_dot_color || design.color_accent || '#C9A848' %>">
        </div>
      </div>
      <div style="display:flex;flex-direction:column;gap:6px;margin-top:8px;">
        <label style="font-size:0.75rem;display:flex;align-items:center;gap:6px;cursor:pointer;">
          <input type="checkbox" name="hero_slideshow_dots" <%= design.hero_slideshow_dots !== 'false' ? 'checked' : '' %> style="width:14px;height:14px;accent-color:var(--navy);"> Show dot indicators
        </label>
        <label style="font-size:0.75rem;display:flex;align-items:center;gap:6px;cursor:pointer;">
          <input type="checkbox" name="hero_slideshow_arrows" <%= design.hero_slideshow_arrows !== 'false' ? 'checked' : '' %> style="width:14px;height:14px;accent-color:var(--navy);"> Show prev/next arrows
        </label>
        <label style="font-size:0.75rem;display:flex;align-items:center;gap:6px;cursor:pointer;">
          <input type="checkbox" name="hero_slideshow_static" <%= design.hero_slideshow_static === 'true' ? 'checked' : '' %> style="width:14px;height:14px;accent-color:var(--navy);"> Static headline over all slides
        </label>
      </div>
      <div class="ep-hint" style="margin-top:8px;">Add slides in the <strong>Hero Slides</strong> copy panel (under the Copy tab). Each slide can have its own image, heading, tag, and link target.<br><strong>Static headline:</strong> ignore the per-slide titles and overlay one fixed headline — your main <strong>Hero</strong> copy (eyebrow, heading, subtext, CTAs) — across every slide, using the hero typography. Ideal for image-only slideshows.</div>
    </div>

    <%# ── Image-card only controls ── %>
    <div data-hero-style-only="image_card" style="<%= design.hero_style === 'image_card' ? '' : 'display:none;' %>border:1px solid var(--border);border-radius:2px;padding:12px;margin:10px 0;background:var(--surface);">
      <div style="font-size:0.7rem;font-weight:700;letter-spacing:0.12em;text-transform:uppercase;margin-bottom:10px;">Image Card Settings</div>
      <div class="ep-row">
        <div class="ep-field">
          <label>Card Position</label>
          <select name="hero_card_position">
            <option value="left"   <%= (design.hero_card_position || 'left') === 'left'   ? 'selected' : '' %>>Left</option>
            <option value="center" <%= design.hero_card_position === 'center'             ? 'selected' : '' %>>Center</option>
            <option value="right"  <%= design.hero_card_position === 'right'              ? 'selected' : '' %>>Right</option>
          </select>
        </div>
        <div class="ep-field">
          <label>Card Blur (<%= design.hero_card_blur || '8' %>px)</label>
          <input type="range" min="0" max="24" step="1" name="hero_card_blur" value="<%= design.hero_card_blur || '8' %>" style="width:100%;">
        </div>
      </div>
      <div class="ep-row">
        <div class="ep-field">
          <label>Card Background (CSS color)</label>
          <input type="text" name="hero_card_bg" value="<%= design.hero_card_bg || '' %>" placeholder="rgba(253,252,250,0.92)">
        </div>
        <div class="ep-field">
          <label>Card Text Color</label>
          <input type="text" name="hero_card_text_color" value="<%= design.hero_card_text_color || '' %>" placeholder="auto">
        </div>
      </div>
      <label style="font-size:0.75rem;display:flex;align-items:center;gap:6px;cursor:pointer;margin-top:8px;">
        <input type="checkbox" name="hero_card_shadow" <%= design.hero_card_shadow !== 'false' ? 'checked' : '' %> style="width:14px;height:14px;accent-color:var(--navy);"> Drop shadow on card
      </label>
      <div class="ep-hint" style="margin-top:8px;">Background uses the <strong>Hero Section Image</strong> (Assets → Hero) or the full-bleed media below. The card holds your existing hero copy with the shadow-box treatment.</div>
    </div>

    <%# ── Full-bleed hero background (image or video from tenant assets) ── %>
    <div id="heroBgMediaBlock" style="border:1px solid var(--border);border-radius:2px;padding:10px 12px;margin-bottom:12px;background:var(--surface);">
      <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:8px;gap:8px;flex-wrap:wrap;">
        <label style="font-size:0.7rem;font-weight:700;letter-spacing:0.12em;text-transform:uppercase;color:var(--on-surface);margin:0;">Hero Full Background</label>
        <div style="display:flex;gap:6px;">
          <button type="button" id="heroBgPickImage" style="font-size:0.62rem;font-weight:600;letter-spacing:0.1em;text-transform:uppercase;padding:5px 10px;border:1px solid var(--border);border-radius:2px;background:var(--ivory);color:var(--navy);cursor:pointer;font-family:var(--font-body);">Pick Image</button>
          <button type="button" id="heroBgPickVideo" style="font-size:0.62rem;font-weight:600;letter-spacing:0.1em;text-transform:uppercase;padding:5px 10px;border:1px solid var(--border);border-radius:2px;background:var(--ivory);color:var(--navy);cursor:pointer;font-family:var(--font-body);">Pick Video</button>
          <button type="button" id="heroBgClear" style="font-size:0.62rem;font-weight:600;letter-spacing:0.1em;text-transform:uppercase;padding:5px 10px;border:1px solid var(--border);border-radius:2px;background:var(--ivory);color:#8B1C1C;cursor:pointer;font-family:var(--font-body);<%= design.hero_bg_media_url ? '' : 'display:none;' %>">Clear</button>
        </div>
      </div>
      <input type="hidden" name="hero_bg_media_url"    id="field-hero_bg_media_url"    value="<%= design.hero_bg_media_url || '' %>">
      <input type="hidden" name="hero_bg_media_type"   id="field-hero_bg_media_type"   value="<%= design.hero_bg_media_type || '' %>">
      <input type="hidden" name="hero_bg_media_poster" id="field-hero_bg_media_poster" value="<%= design.hero_bg_media_poster || '' %>">
      <div id="heroBgPreview" style="<%= design.hero_bg_media_url ? '' : 'display:none;' %>border:1px solid var(--border);border-radius:2px;overflow:hidden;background:#000;aspect-ratio:16/9;display:flex;align-items:center;justify-content:center;">
        <% if (design.hero_bg_media_type === 'video') { %>
          <video src="<%= design.hero_bg_media_url %>" muted playsinline autoplay loop style="width:100%;height:100%;object-fit:cover;"></video>
        <% } else if (design.hero_bg_media_url) { %>
          <img src="<%= design.hero_bg_media_url %>" alt="Hero background" style="width:100%;height:100%;object-fit:cover;">
        <% } %>
      </div>
      <div class="ep-hint" style="margin-top:8px;">Overrides the split-panel layout with a full-bleed image or looping video. Overlay color/opacity below sit on top.</div>
    </div>

    <div class="ep-row">
      <div class="ep-field">
        <label>Text Align</label>
        <select name="hero_text_align">
          <option value="left" <%= design.hero_text_align === 'left' ? 'selected' : '' %>>Left</option>
          <option value="center" <%= design.hero_text_align === 'center' ? 'selected' : '' %>>Center</option>
          <option value="right" <%= design.hero_text_align === 'right' ? 'selected' : '' %>>Right</option>
        </select>
      </div>
      <div class="ep-field">
        <label>Text Position (vertical)</label>
        <select name="hero_vpos">
          <option value="top"    <%= design.hero_vpos === 'top'    ? 'selected' : '' %>>Top</option>
          <option value="middle" <%= (design.hero_vpos || 'middle') === 'middle' ? 'selected' : '' %>>Middle</option>
          <option value="bottom" <%= design.hero_vpos === 'bottom' ? 'selected' : '' %>>Bottom</option>
        </select>
        <div class="ep-hint" style="margin-top:4px;">Where the overlaid hero text sits over the slideshow / background.</div>
      </div>
      <div class="ep-field">
        <label>Hero Height</label>
        <select name="hero_height">
          <option value="100vh" <%= design.hero_height === '100vh' ? 'selected' : '' %>>Full (100vh)</option>
          <option value="80vh" <%= design.hero_height === '80vh' ? 'selected' : '' %>>80%</option>
          <option value="60vh" <%= design.hero_height === '60vh' ? 'selected' : '' %>>60%</option>
          <option value="auto" <%= design.hero_height === 'auto' ? 'selected' : '' %>>Auto</option>
        </select>
      </div>
    </div>
    <div class="ep-field">
      <label>Heading Font Size (rem — blank = responsive default)</label>
      <input type="number" name="hero_heading_size" min="1.5" max="10" step="0.1" value="<%= design.hero_heading_size || '' %>" placeholder="auto">
      <div class="ep-hint" style="margin-top:4px;">Sets the hero headline size for this tenant (split &amp; slideshow). Leave blank to keep the responsive default.</div>
    </div>
    <div class="ep-field">
      <label>Overlay Opacity (<%= design.hero_overlay_opacity || '55' %>%)</label>
      <input type="range" name="hero_overlay_opacity" min="0" max="100" value="<%= design.hero_overlay_opacity || '55' %>" style="width:100%;">
    </div>
    <div class="ep-row">
      <div class="ep-field">
        <label>Overlay Color</label>
        <div class="color-row">
          <input type="color" name="hero_overlay_color" value="<%= design.hero_overlay_color || design.color_primary_deep %>">
        </div>
      </div>
      <div class="ep-field">
        <label>BG Pattern</label>
        <select name="hero_bg_pattern">
          <option value="none" <%= design.hero_bg_pattern === 'none' ? 'selected' : '' %>>None</option>
          <option value="grid" <%= design.hero_bg_pattern === 'grid' ? 'selected' : '' %>>Grid</option>
          <option value="diagonal" <%= design.hero_bg_pattern === 'diagonal' ? 'selected' : '' %>>Diagonal</option>
          <option value="dots" <%= design.hero_bg_pattern === 'dots' ? 'selected' : '' %>>Dots</option>
        </select>
      </div>
    </div>
    <div class="ep-field">
      <label>Card Radius (<%= design.card_border_radius || '2' %>px)</label>
      <input type="range" name="card_border_radius" min="0" max="16" value="<%= design.card_border_radius || '2' %>" style="width:100%;">
    </div>
    <div class="ep-row">
      <div class="ep-field">
        <label>Gradient Angle (<%= design.gradient_angle || '135' %>&deg;)</label>
        <input type="range" name="gradient_angle" min="0" max="360" value="<%= design.gradient_angle || '135' %>" style="width:100%;">
      </div>
    </div>
    <div style="display:flex;flex-direction:column;gap:8px;margin-top:10px;">
      <label style="font-size:0.75rem;display:flex;align-items:center;gap:6px;cursor:pointer;">
        <input type="checkbox" name="gradient_enabled" id="field-gradient_enabled" <%= design.gradient_enabled === 'true' ? 'checked' : '' %> style="width:14px;height:14px;accent-color:var(--navy);"> Gradient backgrounds
      </label>
      <label style="font-size:0.75rem;display:flex;align-items:center;gap:6px;cursor:pointer;">
        <input type="checkbox" name="card_hover_accent" id="field-card_hover_accent" <%= design.card_hover_accent === 'true' ? 'checked' : '' %> style="width:14px;height:14px;accent-color:var(--navy);"> Card hover accents
      </label>
    </div>
  </div>
</div>

<%# ─── 12. DISPLAY LAYOUTS ─── %>
<div class="editor-section" data-tab="design">
  <button type="button" class="editor-section-toggle" onclick="toggleSection(this)">
    Display Layouts <span class="arrow">&#9654;</span>
  </button>
  <div class="editor-section-body">
    <div class="ep-hint" style="margin-bottom:10px;">Logo &amp; navbar settings live in <strong>Header &amp; Footer</strong> above.</div>
    <div class="ep-field">
      <label>Hero Large Text</label>
      <input type="text" name="hero_name_large" id="field-hero_name_large" value="<%= design.hero_name_large || '' %>" placeholder="Auto">
    </div>
    <div class="ep-row">
      <div class="ep-field">
        <label>Portfolio Layout</label>
        <select name="portfolio_layout" id="field-portfolio_layout">
          <option value="grid" <%= design.portfolio_layout === 'grid' ? 'selected' : '' %>>Grid</option>
          <option value="masonry" <%= design.portfolio_layout === 'masonry' ? 'selected' : '' %>>Masonry</option>
          <option value="carousel" <%= design.portfolio_layout === 'carousel' ? 'selected' : '' %>>Carousel</option>
          <option value="list" <%= design.portfolio_layout === 'list' ? 'selected' : '' %>>List</option>
        </select>
      </div>
      <div class="ep-field">
        <label>Blog Layout</label>
        <select name="blog_layout" id="field-blog_layout">
          <option value="grid" <%= design.blog_layout === 'grid' ? 'selected' : '' %>>Grid</option>
          <option value="list" <%= design.blog_layout === 'list' ? 'selected' : '' %>>List</option>
          <option value="masonry" <%= design.blog_layout === 'masonry' ? 'selected' : '' %>>Masonry</option>
          <option value="featured" <%= design.blog_layout === 'featured' ? 'selected' : '' %>>Featured</option>
        </select>
      </div>
    </div>
  </div>
</div>

<%# ─── 13. AI ASSISTANT ─── %>
<div class="editor-section" data-tab="design">
  <button type="button" class="editor-section-toggle" onclick="toggleSection(this)">
    AI Assistant <span class="arrow">&#9654;</span>
  </button>
  <div class="editor-section-body">
    <div class="ep-field">
      <label>Agent Name</label>
      <input type="text" name="agent_name" id="field-agent_name" value="<%= design.agent_name %>" placeholder="Assistant">
    </div>
    <div class="ep-field">
      <label>Greeting</label>
      <textarea name="agent_greeting" id="field-agent_greeting" rows="2" placeholder="Hi! What would you like to create?"><%= design.agent_greeting %></textarea>
    </div>
    <div class="ep-hint">MCP endpoint: <code>/admin/master-agent/mcp</code></div>
  </div>
</div>

<!-- ── CUSTOM ELEMENTS ── -->
<div class="editor-section" data-tab="sections">
  <button type="button" class="editor-section-toggle" onclick="toggleSection(this)">
    Custom Elements <span class="arrow">&#9654;</span>
  </button>
  <div class="editor-section-body">
    <div class="ep-hint" style="margin-bottom:12px;">Platform features injected outside the template system. Toggle them on/off and see where they live.</div>

    <div style="border:1px solid var(--border);border-radius:2px;margin-bottom:8px;padding:10px 12px;">
      <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:4px;">
        <span style="font-size:0.75rem;font-weight:600;color:var(--on-surface);">Bug / Ticket Button</span>
        <select name="el_bug_button" style="width:auto;padding:3px 6px;font-size:0.68rem;" onchange="previewReload()">
          <option value="true" <%= d.el_bug_button !== 'false' ? 'selected' : '' %>>On</option>
          <option value="false" <%= d.el_bug_button === 'false' ? 'selected' : '' %>>Off</option>
        </select>
      </div>
      <div class="ep-hint">Floating report-a-bug button. Opens ticket submission modal.<br>
        <strong>Source:</strong> public/js/bugButton.js</div>
    </div>

    <div style="border:1px solid var(--border);border-radius:2px;margin-bottom:8px;padding:10px 12px;">
      <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:4px;">
        <span style="font-size:0.75rem;font-weight:600;color:var(--on-surface);">Public Design Switcher</span>
        <select name="el_design_switcher" style="width:auto;padding:3px 6px;font-size:0.68rem;" onchange="previewReload()">
          <option value="true" <%= d.el_design_switcher === 'true' ? 'selected' : '' %>>On</option>
          <option value="false" <%= d.el_design_switcher !== 'true' ? 'selected' : '' %>>Off</option>
        </select>
      </div>
      <div class="ep-hint">Fixed floating palette dropdown that lets <em>any public visitor</em> repaint the live site from preset color designs (stored per-visitor, never saved to the DB).<br>
        <strong>Source:</strong> views/partials/design-switcher.ejs</div>
    </div>

    <div style="border:1px solid var(--border);border-radius:2px;margin-bottom:8px;padding:10px 12px;">
      <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:4px;">
        <span style="font-size:0.75rem;font-weight:600;color:var(--on-surface);">Notification Bell</span>
        <select name="el_notification_bell" style="width:auto;padding:3px 6px;font-size:0.68rem;" onchange="previewReload()">
          <option value="true" <%= d.el_notification_bell !== 'false' ? 'selected' : '' %>>On</option>
          <option value="false" <%= d.el_notification_bell === 'false' ? 'selected' : '' %>>Off</option>
        </select>
      </div>
      <div class="ep-hint">Platform announcements + ticket/scan counts bell (admin only).<br>
        <strong>Source:</strong> views/admin/partials/head.ejs</div>
    </div>

    <div style="border:1px solid var(--border);border-radius:2px;margin-bottom:8px;padding:10px 12px;">
      <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:4px;">
        <span style="font-size:0.75rem;font-weight:600;color:var(--on-surface);">Admin Tour</span>
        <select name="el_admin_tour" style="width:auto;padding:3px 6px;font-size:0.68rem;" onchange="previewReload()">
          <option value="true" <%= d.el_admin_tour !== 'false' ? 'selected' : '' %>>On</option>
          <option value="false" <%= d.el_admin_tour === 'false' ? 'selected' : '' %>>Off</option>
        </select>
      </div>
      <div class="ep-hint">Driver.js guided tour for new admin users.<br>
        <strong>Source:</strong> public/js/adminTour.js · public/css/adminTour.css</div>
    </div>

    <div style="border:1px solid var(--border);border-radius:2px;margin-bottom:8px;padding:10px 12px;">
      <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:4px;">
        <span style="font-size:0.75rem;font-weight:600;color:var(--on-surface);">Preview Banner</span>
        <select name="el_preview_banner" style="width:auto;padding:3px 6px;font-size:0.68rem;" onchange="previewReload()">
          <option value="true" <%= d.el_preview_banner !== 'false' ? 'selected' : '' %>>On</option>
          <option value="false" <%= d.el_preview_banner === 'false' ? 'selected' : '' %>>Off</option>
        </select>
      </div>
      <div class="ep-hint">Top banner shown when tenant is in preview mode with "Go Live" CTA.<br>
        <strong>Source:</strong> views/partials/preview-banner.ejs</div>
    </div>

    <div style="border:1px solid var(--border);border-radius:2px;margin-bottom:8px;padding:10px 12px;">
      <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:4px;">
        <span style="font-size:0.75rem;font-weight:600;color:var(--on-surface);">Maintenance Banner</span>
        <select name="el_maintenance_banner" style="width:auto;padding:3px 6px;font-size:0.68rem;" onchange="previewReload()">
          <option value="true" <%= d.el_maintenance_banner !== 'false' ? 'selected' : '' %>>On</option>
          <option value="false" <%= d.el_maintenance_banner === 'false' ? 'selected' : '' %>>Off</option>
        </select>
      </div>
      <div class="ep-hint">Countdown banner after server restart / maintenance window.<br>
        <strong>Source:</strong> views/partials/maintenance-banner.ejs</div>
    </div>

    <div style="border:1px solid var(--border);border-radius:2px;margin-bottom:8px;padding:10px 12px;">
      <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:4px;">
        <span style="font-size:0.75rem;font-weight:600;color:var(--on-surface);">3D Model Viewer</span>
        <select name="el_gltf_viewer" style="width:auto;padding:3px 6px;font-size:0.68rem;" onchange="previewReload()">
          <option value="true" <%= d.el_gltf_viewer === 'true' ? 'selected' : '' %>>On</option>
          <option value="false" <%= d.el_gltf_viewer !== 'true' ? 'selected' : '' %>>Off</option>
        </select>
      </div>
      <div class="ep-hint">Three.js GLTF viewer for header/logo 3D models. Heavy payload.<br>
        <strong>Source:</strong> public/js/gltfViewer.js</div>
    </div>

  </div>
</div>

<div class="editor-section" data-tab="brand">
  <button type="button" class="editor-section-toggle" onclick="toggleSection(this)">
    Head, Favicon &amp; Tracking <span class="arrow">&#9654;</span>
  </button>
  <div class="editor-section-body">
    <div class="ep-hint" style="margin-bottom:10px;">Favicon, Google Tag Manager, and custom &lt;head&gt; / end-of-&lt;body&gt; tags (analytics, pixels, verification). Applied across this tenant's public site.</div>
    <label style="display:block;font-size:0.7rem;font-weight:600;margin:8px 0 3px;">Favicon Emoji</label>
    <input type="text" name="favicon_emoji" value="<%= (design.favicon_emoji || '') %>" maxlength="4" placeholder="🚀" style="width:90px;text-align:center;font-size:1.1rem;">
    <div class="ep-hint">Browser-tab icon when no Icon image is uploaded (Logos &amp; Brand Images below).</div>
    <label style="display:block;font-size:0.7rem;font-weight:600;margin:12px 0 3px;">Google Tag Manager ID</label>
    <input type="text" name="gtm_id" value="<%= (design.gtm_id || '') %>" placeholder="GTM-XXXXXXX">
    <div class="ep-hint">Injects the GTM container site-wide (head script + body noscript).</div>
    <label style="display:block;font-size:0.7rem;font-weight:600;margin:14px 0 3px;">Domain / Site Verification</label>
    <div class="ep-hint" style="margin-bottom:6px;">Paste just the <em>content</em> token from each provider's HTML-tag method (we render the meta tag). Google Search Console can also verify via the GTM ID above — no token needed.</div>
    <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:8px;margin-bottom:6px;">
      <div><label style="font-size:0.64rem;color:var(--slate);">Google (Search Console)</label><input type="text" name="verify_google" value="<%= (design.verify_google || '') %>" placeholder="google-site-verification token"></div>
      <div><label style="font-size:0.64rem;color:var(--slate);">Bing (Webmaster)</label><input type="text" name="verify_bing" value="<%= (design.verify_bing || '') %>" placeholder="msvalidate.01 token"></div>
      <div><label style="font-size:0.64rem;color:var(--slate);">Pinterest</label><input type="text" name="verify_pinterest" value="<%= (design.verify_pinterest || '') %>" placeholder="p:domain_verify token"></div>
      <div><label style="font-size:0.64rem;color:var(--slate);">Facebook (Meta)</label><input type="text" name="verify_facebook" value="<%= (design.verify_facebook || '') %>" placeholder="facebook-domain-verification token"></div>
    </div>
    <label style="display:block;font-size:0.7rem;font-weight:600;margin:12px 0 3px;">Custom &lt;head&gt; HTML</label>
    <textarea name="head_html" rows="4" style="width:100%;font-family:monospace;font-size:0.68rem;" placeholder="<!-- analytics / pixels / site-verification -->"><%= (design.head_html || '') %></textarea>
    <label style="display:block;font-size:0.7rem;font-weight:600;margin:12px 0 3px;">Custom end-of-&lt;body&gt; HTML</label>
    <textarea name="body_end_html" rows="3" style="width:100%;font-family:monospace;font-size:0.68rem;" placeholder="<!-- chat widgets / deferred scripts -->"><%= (design.body_end_html || '') %></textarea>
  </div>
</div>

</form>

<%# ─── Standalone forms for inline controls (HTML5 disallows nested <form>). ─── %>
<%# Each form is `hidden` and zero-size; its submit is triggered by buttons elsewhere via the `form` attribute. %>
<form id="link-create" action="/admin/design/links" method="POST" hidden></form>
<% (navLinks || []).forEach(function(l) { %>
  <form id="link-edit-<%= l._id %>" action="/admin/design/links" method="POST" hidden></form>
  <form id="link-delete-<%= l._id %>" action="/admin/design/links/<%= l._id %>/delete" method="POST" hidden></form>
<% }); %>
<% (templates || []).forEach(function(tpl) { %>
  <form id="tpl-act-<%= tpl._id %>"   action="/admin/templates/<%= tpl._id %>/activate"   method="POST" hidden></form>
  <form id="tpl-deact-<%= tpl._id %>" action="/admin/templates/<%= tpl._id %>/deactivate" method="POST" hidden></form>
<% }); %>

<%# ─── 14. LOGOS & BRAND IMAGES ─── %>
<div class="editor-section open" id="brand-images" data-tab="brand">
  <button type="button" class="editor-section-toggle" onclick="toggleSection(this)">
    Logos &amp; Brand Images <span class="arrow">&#9654;</span>
  </button>
  <div class="editor-section-body">
    <%
      const logoSlots = [
        { slot: 'logo_primary', label: 'Primary Logo',  hint: 'Header & default logo' },
        { slot: 'logo_white',   label: 'White Logo',    hint: 'For dark backgrounds (footer)' },
        { slot: 'logo_icon',    label: 'Icon / Favicon', hint: 'Browser tab + small contexts' },
        { slot: 'logo_social',        label: 'Social Logo',       hint: 'Used by social auto-gen on light/solid posts' },
        { slot: 'logo_social_white',  label: 'Social Logo (white)', hint: 'Used by social auto-gen on dark/solid posts' },
        { slot: 'logo_badge',         label: 'Social Badge / Mark', hint: 'Compact graphical mark for auto-gen overlays' },
        { slot: 'banner',       label: 'Banner',        hint: 'Hero / share image' },
        { slot: 'share_image',  label: 'Share Image (1.91:1)', hint: 'Social link previews — 1200×630' },
        { slot: 'share_square', label: 'Share Square (1:1)',   hint: 'Square previews — 1080×1080' },
      ];
    %>
    <div class="ep-hint" style="margin-bottom:10px;">Upload your logos and brand assets below. Each slot replaces the previous file when you upload a new one. Changes appear after a refresh.</div>

    <%# Combined preview + upload row per slot %>
    <div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:12px;">
      <% logoSlots.forEach(ls => { %>
        <% const existing = brandImages.find(i => i.slot === ls.slot); %>
        <div style="border:1px solid var(--border);border-radius:3px;padding:10px;display:flex;flex-direction:column;gap:6px;">
          <div style="font-size:0.7rem;font-weight:700;color:var(--on-surface);letter-spacing:0.04em;"><%= ls.label %></div>
          <div style="font-size:0.6rem;color:var(--slate);"><%= ls.hint %></div>
          <div style="height:70px;border:1px dashed var(--border);background:<%= (ls.slot === 'logo_white' || ls.slot === 'logo_social_white') ? 'var(--navy-deep)' : 'var(--ivory)' %>;display:flex;align-items:center;justify-content:center;overflow:hidden;">
            <% if (existing) { %>
              <img src="<%= existing.url %>" alt="<%= ls.label %>" style="max-height:60px;max-width:90%;object-fit:contain;">
            <% } else { %>
              <span style="font-size:0.62rem;color:var(--slate);">No image uploaded</span>
            <% } %>
          </div>
          <form method="POST" action="/admin/design/images" enctype="multipart/form-data" style="display:flex;gap:4px;align-items:center;flex-wrap:wrap;">
            <input type="hidden" name="slot" value="<%= ls.slot %>">
            <input type="hidden" name="label" value="<%= ls.label %>">
            <input type="file" name="image" accept="image/*" required style="font-size:0.62rem;flex:1;min-width:0;">
            <button type="submit" class="btn btn-navy btn-sm" style="font-size:0.62rem;padding:4px 10px;"><%= existing ? 'Replace' : 'Upload' %></button>
          </form>
          <% if (existing) { %>
            <form method="POST" action="/admin/design/images/<%= existing._id %>/delete" style="margin:0;" onsubmit="return confirm('Remove this <%= ls.label %>?');">
              <button type="submit" class="btn btn-ghost btn-sm" style="font-size:0.58rem;padding:2px 6px;color:var(--slate);">Remove</button>
            </form>
          <% } %>
        </div>
      <% }) %>
    </div>

    <% const supportCount = brandImages.filter(i => i.slot === 'support').length; %>
    <div class="ep-hint" style="margin-top:14px;padding-top:10px;border-top:1px solid var(--border);">
      <%= supportCount %> additional support image<%= supportCount !== 1 ? 's' : '' %> uploaded. Manage these from the <a href="/admin/assets" style="color:var(--navy);">Assets page</a>.
    </div>
  </div>
</div>

<%# ─── 15. 3D MODELS (collapsed) ─── %>
<div class="editor-section" data-tab="brand">
  <button type="button" class="editor-section-toggle" onclick="toggleSection(this)">
    3D Models <span class="arrow">&#9654;</span>
  </button>
  <div class="editor-section-body">
    <div style="display:flex;flex-direction:column;gap:8px;margin-bottom:10px;">
      <label style="font-size:0.75rem;display:flex;align-items:center;gap:6px;cursor:pointer;">
        <input type="checkbox" id="toggle_model_header" <%= design.model_header_enabled === 'true' ? 'checked' : '' %> style="width:14px;height:14px;accent-color:var(--navy);" onchange="toggleModel('model_header_enabled', this.checked)"> 3D Header
      </label>
      <label style="font-size:0.75rem;display:flex;align-items:center;gap:6px;cursor:pointer;">
        <input type="checkbox" id="toggle_model_logo" <%= design.model_logo_enabled === 'true' ? 'checked' : '' %> style="width:14px;height:14px;accent-color:var(--navy);" onchange="toggleModel('model_logo_enabled', this.checked)"> 3D Logo
      </label>
    </div>
    <%
      const modelSlots = [
        { slot: 'model_header', label: 'Header Model' },
        { slot: 'model_logo',   label: 'Logo Model' },
      ];
    %>
    <% modelSlots.forEach(ms => { %>
      <% const existing = brandModels.find(m => m.slot === ms.slot); %>
      <div style="margin-bottom:8px;">
        <div style="font-size:0.68rem;color:var(--on-surface);margin-bottom:4px;"><%= ms.label %> <% if (existing) { %><span style="color:var(--success);font-size:0.6rem;">uploaded</span><% } %></div>
        <form method="POST" action="/admin/design/models" enctype="multipart/form-data" style="display:flex;gap:4px;align-items:center;flex-wrap:wrap;">
          <input type="hidden" name="slot" value="<%= ms.slot %>">
          <input type="hidden" name="label" value="<%= ms.label %>">
          <input type="file" name="model" accept=".glb,.gltf" required style="font-size:0.65rem;max-width:160px;">
          <button type="submit" class="btn btn-ghost btn-sm" style="font-size:0.6rem;padding:3px 8px;"><%= existing ? 'Replace' : 'Upload' %></button>
        </form>
      </div>
    <% }) %>
    <div class="ep-hint">GLB format recommended. Max 20MB.</div>
  </div>
</div>

<%# ─── 16. THEMES ─── %>
<div class="editor-section" data-tab="brand">
  <button type="button" class="editor-section-toggle" onclick="toggleSection(this)">
    Themes <span class="arrow">&#9654;</span>
  </button>
  <div class="editor-section-body">
    <%# Save current as theme — separate form target %>
    <div style="margin-bottom:12px;">
      <div style="font-size:0.62rem;font-weight:600;letter-spacing:0.1em;color:var(--slate);margin-bottom:6px;">SAVE CURRENT</div>
    </div>
    <!-- This is a nested form workaround: we use formaction on the button -->
    <div style="display:flex;gap:6px;margin-bottom:14px;">
      <input type="text" id="themeNameInput" placeholder="Theme name" style="flex:1;padding:6px 8px;border:1px solid var(--border);border-radius:2px;font-size:0.75rem;font-family:var(--font-body);">
      <button type="button" class="btn btn-navy btn-sm" style="font-size:0.62rem;white-space:nowrap;" onclick="saveTheme()">Save Theme</button>
    </div>

    <% if (themes && themes.length) { %>
      <% for (const t of themes) { %>
      <div class="theme-card">
        <div class="theme-card-swatches">
          <div style="background:<%= t.settings.color_primary || '#1C2B4A' %>;"></div>
          <div style="background:<%= t.settings.color_primary_deep || '#0F1B30' %>;"></div>
          <div style="background:<%= t.settings.color_accent || '#C9A848' %>;"></div>
          <div style="background:<%= t.settings.color_bg || '#F5F3EF' %>;"></div>
        </div>
        <div class="theme-card-body">
          <div class="theme-card-name"><%= t.name %></div>
          <div class="theme-card-meta"><%= t.settings.font_heading || 'Default' %> / <%= t.settings.font_body || 'Default' %> &middot; <%= new Date(t.createdAt).toLocaleDateString() %></div>
          <div class="theme-card-actions">
            <form method="POST" action="/admin/design/themes/<%= t._id %>/apply" style="display:inline;">
              <button type="submit" class="btn btn-navy btn-sm" style="font-size:0.58rem;padding:3px 8px;" onclick="return confirm('Apply this theme?')">Apply</button>
            </form>
            <form method="POST" action="/admin/design/themes/<%= t._id %>/delete" style="display:inline;" onsubmit="return confirm('Delete?')">
              <button type="submit" class="btn btn-danger btn-sm" style="font-size:0.58rem;padding:3px 8px;">Del</button>
            </form>
          </div>
        </div>
      </div>
      <% } %>
    <% } else { %>
      <div style="font-size:0.75rem;color:var(--slate);padding:12px 0;">No saved themes yet.</div>
    <% } %>
  </div>
</div>

<%# Mobile-only: sticky preview toggle bar at bottom of editor panel %>
<div class="dp-preview-toggle" id="dpPreviewToggle">
  <button type="button" id="dpPreviewBtn" onclick="dpTogglePreview()">
    &#9654; Show Preview
  </button>
  <button type="button" class="agent-btn" id="agentToggleMobile" style="flex:none;max-width:none;">
    <span style="font-size:0.85rem;">&#9830;</span> Agent
  </button>
</div>

</div><!-- /.editor-panel -->

<%# ═══════════ RIGHT PANEL — PREVIEW ═══════════ %>
<div class="editor-preview" id="editorPreview">
  <div class="preview-bar">
    <button type="button" class="btn btn-ghost" onclick="setViewport('desktop')" id="vpDesktop" style="border-color:var(--navy);">Desktop</button>
    <button type="button" class="btn btn-ghost" onclick="setViewport('tablet')" id="vpTablet">Tablet</button>
    <button type="button" class="btn btn-ghost" onclick="setViewport('mobile')" id="vpMobile">Mobile</button>
    <button type="button" class="btn btn-ghost" onclick="previewReload()">Refresh</button>
    <button type="button" class="btn btn-ghost" id="canvasEditToggle" onclick="if(window.SlabCanvas)window.SlabCanvas.toggle()" style="border-color:var(--gold);color:var(--gold);">&#9998; Edit on canvas</button>
    <span style="margin-left:auto;font-size:0.65rem;color:var(--slate);" id="canvasStatus">Live Preview</span>
    <button type="button" class="agent-btn" id="agentToggle">
      <span style="font-size:0.85rem;">&#9830;</span> Agent
    </button>
  </div>
  <div id="previewViewport" style="flex:1;display:flex;justify-content:center;overflow:hidden;">
    <iframe id="previewFrame" src="/" style="width:100%;height:100%;border:none;background:var(--surface);"></iframe>
  </div>
</div>

</div>

<!-- AI Agent Panel -->
<div id="agentOverlay"></div>
<div id="agentPanel">
  <div class="ap-header">
    <div class="ap-title"><span class="ap-dot"></span> Design Agent</div>
    <button class="ap-close" id="agentClose">&times;</button>
  </div>
  <div class="ap-sub">
    <span class="ap-sub-dot"></span>
    AI design assistant &middot; Auto-fills fields &middot; Live preview
  </div>
  <div class="ap-msgs" id="agentMsgs">
    <div class="ap-msg assistant">
      <div class="ap-bubble">
        Hi! I can help you with colors, fonts, layouts, copy, and visibility settings.
        Try asking me to change colors, write hero copy, suggest a bold palette, or toggle sections.
        Changes preview live &mdash; save when you're happy.
      </div>
    </div>
  </div>
  <div class="ap-footer">
    <div class="ap-quick" id="apQuick"></div>
    <div class="ap-input-row">
      <textarea class="ap-input" id="agentInput" rows="1" placeholder="Ask the design agent..."></textarea>
      <button class="ap-send" id="agentSend">&uarr;</button>
    </div>
    <span id="ai-status" style="font-size:0.6rem;color:var(--slate);margin-top:4px;display:block;"></span>
  </div>
</div>

<script>
// ── Tab switching ──
function dpSwitch(tab) {
  document.querySelectorAll('#dpTabs .dp-tab').forEach(function(b) {
    b.classList.toggle('active', b.dataset.tab === tab);
  });
  document.querySelectorAll('.editor-section[data-tab]').forEach(function(s) {
    s.classList.toggle('dp-show', s.dataset.tab === tab);
  });
  // Persist tab to URL hash so refresh / deep-links survive
  try { history.replaceState(null, '', '#tab=' + tab); } catch (e) {}
  // Bring the newly-active tab fully into view within the scroll strip
  var act = document.querySelector('#dpTabs .dp-tab.active');
  if (act && act.scrollIntoView) act.scrollIntoView({ inline: 'center', block: 'nearest', behavior: 'smooth' });
  dpUpdateTabScroll();
}
// Nudge the tab strip left (-1) or right (+1)
function dpScrollTabs(dir) {
  var el = document.getElementById('dpTabs');
  if (el) el.scrollBy({ left: dir * Math.max(120, el.clientWidth * 0.6), behavior: 'smooth' });
}
// Show/hide the arrows + edge fades based on how far the strip can scroll
function dpUpdateTabScroll() {
  var el = document.getElementById('dpTabs');
  var wrap = document.getElementById('dpTabsWrap');
  if (!el || !wrap) return;
  var maxScroll = el.scrollWidth - el.clientWidth;
  wrap.classList.toggle('dp-scroll-l', el.scrollLeft > 2);
  wrap.classList.toggle('dp-scroll-r', el.scrollLeft < maxScroll - 2);
}
(function() {
  var initial = 'design';
  var m = location.hash.match(/tab=([a-z]+)/);
  if (m && ['source','design','copy','sections','templates','brand'].indexOf(m[1]) !== -1) initial = m[1];
  dpSwitch(initial);
  var tabsEl = document.getElementById('dpTabs');
  if (tabsEl) tabsEl.addEventListener('scroll', dpUpdateTabScroll, { passive: true });
  window.addEventListener('resize', dpUpdateTabScroll);
  dpUpdateTabScroll();
})();

// ── Accordion toggle ──
function toggleSection(btn) {
  var section = btn.closest('.editor-section');
  section.classList.toggle('open');
}

// ── Repeater (add/remove items in copy lists; data-attrs drive everything) ──
// Wrapper config (set as JSON on the .dp-repeater element):
//   data-prefix     — key prefix (e.g. "service", "process", "pricing_tier")
//   data-fields     — JSON array [{key:"title",label:"Title",type:"text"},...]
//   data-noun       — singular label for the +Add button ("Service")
//   data-startidx   — first index used by template (default 1)
function repAdd(btn) {
  var wrap = btn.closest('.dp-repeater');
  var prefix = wrap.dataset.prefix;
  var fields;
  try { fields = JSON.parse(wrap.dataset.fields); } catch (e) { fields = []; }
  var items = wrap.querySelectorAll('.dp-rep-item');
  // Find next free index — scan existing items and use max+1.
  var nextIdx = 1;
  items.forEach(function(it) { var n = parseInt(it.dataset.idx, 10); if (n >= nextIdx) nextIdx = n + 1; });
  var item = document.createElement('div');
  item.className = 'dp-rep-item';
  item.dataset.idx = nextIdx;
  var nounCap = (wrap.dataset.noun || 'Item') + ' ' + nextIdx;
  var html = '<div class="dp-rep-head"><span>' + nounCap + '</span>' +
    '<div class="dp-rep-actions">' +
      '<button type="button" data-dir="-1" onclick="repMove(this, -1)" title="Move up">↑</button>' +
      '<button type="button" data-dir="1" onclick="repMove(this, 1)" title="Move down">↓</button>' +
      '<button type="button" class="dp-rep-remove" onclick="repRemove(this)" title="Remove this ' + (wrap.dataset.noun || 'item').toLowerCase() + '">✕</button>' +
    '</div></div>';
  fields.forEach(function(f) {
    var name = prefix + nextIdx + '_' + f.key;
    html += '<div class="ep-field"><label>' + (f.label || f.key) + '</label>';
    if (f.type === 'textarea') html += '<textarea name="' + name + '" rows="' + (f.rows || 2) + '"></textarea>';
    else if (f.type === 'checkbox') html += '<label style="display:flex;align-items:center;gap:6px;font-size:0.72rem;text-transform:none;letter-spacing:0;"><input type="checkbox" name="' + name + '" value="yes" style="width:14px;height:14px;accent-color:var(--navy);"> ' + (f.checkboxLabel || 'Yes') + '</label>';
    else if (f.type === 'select') {
      var opts = Array.isArray(f.options) ? f.options : [];
      html += '<select name="' + name + '">' +
        opts.map(function(o) { return '<option value="' + o + '">' + o + '</option>'; }).join('') +
      '</select>';
    }
    else html += '<input type="text" name="' + name + '"' + (f.placeholder ? ' placeholder="' + f.placeholder + '"' : '') + '>';
    html += '</div>';
  });
  item.innerHTML = html;
  // Insert before the +Add button
  btn.parentNode.insertBefore(item, btn);
  repRefreshBoundary(wrap);
  // Focus first input
  var first = item.querySelector('input,textarea');
  if (first) first.focus();
}
function repRemove(btn) {
  var item = btn.closest('.dp-rep-item');
  if (!item) return;
  if (!confirm('Remove this item? Saved data for it will be cleared on next Save All.')) return;
  var wrap = item.closest('.dp-repeater');
  // Mark every keyed input on this row for removal so the server deletes them.
  item.querySelectorAll('[name]').forEach(function(el) {
    var hidden = document.createElement('input');
    hidden.type = 'hidden';
    hidden.name = '_keys_to_remove';
    hidden.value = el.name;
    document.getElementById('designForm').appendChild(hidden);
  });
  item.remove();
  if (wrap) repRefreshBoundary(wrap);
}

// ── Custom-section repeater (cards / FAQ pairs inside per-section editors) ──
// Uses data-cs-field instead of name; saves happen via csSave AJAX. No
// _keys_to_remove tracking needed since server overwrites sec.fields entirely.
function csRepAdd(btn) {
  var wrap = btn.closest('.cs-repeater');
  var prefix = wrap.dataset.prefix;
  var fields;
  try { fields = JSON.parse(wrap.dataset.fields); } catch (e) { fields = []; }
  var nextIdx = 1;
  wrap.querySelectorAll('.dp-rep-item').forEach(function(it) {
    var n = parseInt(it.dataset.idx, 10); if (n >= nextIdx) nextIdx = n + 1;
  });
  var item = document.createElement('div');
  item.className = 'dp-rep-item';
  item.dataset.idx = nextIdx;
  var noun = (wrap.dataset.noun || 'Item') + ' ' + nextIdx;
  var html = '<div class="dp-rep-head"><span>' + noun + '</span>' +
    '<div class="dp-rep-actions">' +
      '<button type="button" data-dir="-1" onclick="repMove(this, -1)" title="Move up">↑</button>' +
      '<button type="button" data-dir="1" onclick="repMove(this, 1)" title="Move down">↓</button>' +
      '<button type="button" class="dp-rep-remove" onclick="csRepRemove(this)" title="Remove">✕</button>' +
    '</div></div>';
  fields.forEach(function(f) {
    // keyMode "only" → field name = f.prefix + idx (e.g. q5, a5).
    // keyMode "suffix" (default) → field name = wrap.prefix + idx + '_' + f.key (e.g. card5_title).
    var key = f.keyMode === 'only'
      ? (f.prefix || prefix) + nextIdx
      : prefix + nextIdx + '_' + f.key;
    var label = typeof f.labelTpl === 'string' ? f.labelTpl.replace('{n}', nextIdx) : (f.label || f.key);
    html += '<div class="ep-field"><label>' + label + '</label>';
    if (f.type === 'textarea') html += '<textarea data-cs-field="' + key + '" rows="' + (f.rows || 2) + '"></textarea>';
    else html += '<input type="text" data-cs-field="' + key + '"' + (f.placeholder ? ' placeholder="' + f.placeholder + '"' : '') + '>';
    html += '</div>';
  });
  item.innerHTML = html;
  btn.parentNode.insertBefore(item, btn);
  repRefreshBoundary(wrap);
  var first = item.querySelector('input,textarea');
  if (first) first.focus();
}
function csRepRemove(btn) {
  var item = btn.closest('.dp-rep-item');
  if (!item) return;
  if (!confirm('Remove this item? Click Save Section to commit.')) return;
  var wrap = item.closest('.dp-repeater');
  item.remove();
  if (wrap) repRefreshBoundary(wrap);
}

// ── Reorder repeater rows (works for both main-form and cs repeaters) ──
// Strategy: swap rows in DOM, then renumber ALL rows 1..N so input
// names/data-cs-fields match the new visual order. Save persists the new
// numbering. The keys-to-remove channel handles any indexes that fall off
// when count drops (only via × Remove, not via reorder).
function repMove(btn, dir) {
  var item = btn.closest('.dp-rep-item');
  if (!item) return;
  var sibling = dir < 0 ? item.previousElementSibling : item.nextElementSibling;
  while (sibling && !sibling.classList.contains('dp-rep-item')) {
    sibling = dir < 0 ? sibling.previousElementSibling : sibling.nextElementSibling;
  }
  if (!sibling) return;
  var parent = item.parentNode;
  if (dir < 0) parent.insertBefore(item, sibling);
  else parent.insertBefore(sibling, item);
  repRenumber(item.closest('.dp-repeater'));
}
function repRenumber(wrap) {
  if (!wrap) return;
  var isCs = wrap.classList.contains('cs-repeater');
  var attrName = isCs ? 'data-cs-field' : 'name';
  var noun = wrap.dataset.noun || 'Item';
  var items = wrap.querySelectorAll('.dp-rep-item');
  items.forEach(function(item, i) {
    var newIdx = i + 1;
    item.dataset.idx = newIdx;
    var head = item.querySelector('.dp-rep-head > span');
    if (head) head.textContent = noun + ' ' + newIdx;
    item.querySelectorAll('[' + attrName + ']').forEach(function(el) {
      var oldKey = el.getAttribute(attrName);
      // Replace the FIRST digit-run in the key with newIdx — covers
      // service{N}_title, card{N}_body, q{N}, a{N}, pricing_tier{N}_cta, etc.
      var newKey = oldKey.replace(/(\d+)/, newIdx);
      if (newKey !== oldKey) el.setAttribute(attrName, newKey);
    });
  });
  repRefreshBoundary(wrap);
}
function repRefreshBoundary(wrap) {
  var items = wrap.querySelectorAll('.dp-rep-item');
  items.forEach(function(item, i) {
    item.querySelectorAll('.dp-rep-actions button[data-dir]').forEach(function(b) {
      var d = parseInt(b.dataset.dir, 10);
      b.disabled = (d < 0 && i === 0) || (d > 0 && i === items.length - 1);
    });
  });
}

// ── Custom sections: save / delete / create / reorder / visibility ──
async function csSave(id, btn) {
  var card = btn.closest('.cs-edit');
  var fd = new URLSearchParams();
  card.querySelectorAll('[data-cs-field]').forEach(function(el) { fd.append(el.dataset.csField, el.value); });
  // Preserve current visibility (server overwrites if missing)
  var visBtn = document.querySelector('.cs-vis-toggle[data-cs-id="' + id + '"]');
  if (visBtn && visBtn.dataset.visible === 'true') fd.append('visible', 'on');
  btn.disabled = true; var prev = btn.textContent; btn.textContent = 'Saving…';
  try {
    var r = await fetch('/admin/sections/custom/' + id + '/copy', { method: 'POST', body: fd });
    if (!r.ok) throw new Error('HTTP ' + r.status);
    btn.textContent = '✓ Saved'; setTimeout(function() { btn.textContent = prev; btn.disabled = false; }, 1200);
    // Re-render the live preview so the new section content is visible
    previewReload();
  } catch (e) { btn.textContent = '✕ Failed'; btn.disabled = false; alert('Save failed: ' + e.message); }
}
async function csDelete(id, btn) {
  if (!confirm('Delete this section permanently?')) return;
  btn.disabled = true;
  try {
    var r = await fetch('/admin/sections/custom/' + id, { method: 'DELETE' });
    if (!r.ok) throw new Error('HTTP ' + r.status);
    location.reload();
  } catch (e) { btn.disabled = false; alert('Delete failed: ' + e.message); }
}
async function csCreate(btn) {
  var type = document.getElementById('cs-new-type').value;
  var label = document.getElementById('cs-new-label').value.trim();
  var err = document.getElementById('cs-new-err');
  err.style.display = 'none';
  if (!label) { err.textContent = 'Label is required.'; err.style.display = 'block'; return; }
  btn.disabled = true;
  try {
    var fd = new URLSearchParams(); fd.append('type', type); fd.append('label', label);
    var r = await fetch('/admin/sections/custom/new', { method: 'POST', body: fd });
    var data = await r.json();
    if (!r.ok || !data.ok) throw new Error(data.error || ('HTTP ' + r.status));
    location.reload();
  } catch (e) { btn.disabled = false; err.textContent = e.message; err.style.display = 'block'; }
}
async function csMove(id, dir) {
  var list = document.getElementById('cs-order-list');
  var rows = Array.from(list.querySelectorAll('.cs-row'));
  var idx = rows.findIndex(function(r) { return r.dataset.csId === id; });
  var to = idx + dir;
  if (to < 0 || to >= rows.length) return;
  list.insertBefore(rows[idx], dir < 0 ? rows[to] : rows[to].nextSibling);
  // Renumber labels
  list.querySelectorAll('.cs-row').forEach(function(row, i) {
    var lbl = row.querySelector('[data-cs-index]'); if (lbl) lbl.textContent = (i + 1);
  });
  // Persist
  var orderJson = JSON.stringify(Array.from(list.querySelectorAll('.cs-row')).map(function(r) { return r.dataset.csId; }));
  var fd = new URLSearchParams(); fd.append('orderJson', orderJson);
  try {
    await fetch('/admin/sections/custom/reorder', { method: 'POST', body: fd });
    previewReload();
  } catch (e) { alert('Reorder save failed: ' + e.message); }
}
async function csToggleVisible(id, btn) {
  var nowVisible = btn.dataset.visible !== 'true'; // flip
  // Pull current copy fields so we don't accidentally wipe them
  var editCard = document.querySelector('.cs-edit[data-cs-id="' + id + '"]');
  var fd = new URLSearchParams();
  if (editCard) editCard.querySelectorAll('[data-cs-field]').forEach(function(el) { fd.append(el.dataset.csField, el.value); });
  if (nowVisible) fd.append('visible', 'on');
  try {
    var r = await fetch('/admin/sections/custom/' + id + '/copy', { method: 'POST', body: fd });
    if (!r.ok) throw new Error('HTTP ' + r.status);
    btn.dataset.visible = nowVisible ? 'true' : 'false';
    btn.textContent = nowVisible ? '◉' : '○';
    btn.style.color = nowVisible ? 'var(--navy)' : 'var(--slate)';
    btn.title = nowVisible ? 'Hide section' : 'Show section';
    previewReload();
  } catch (e) { alert('Visibility update failed: ' + e.message); }
}

// ── Header & Footer sub-tabs ──
function hfTab(btn) {
  var section = btn.closest('.editor-section');
  var target = btn.getAttribute('data-tab');
  section.querySelectorAll('.hf-tab').forEach(function(t) {
    var active = t === btn;
    t.classList.toggle('is-active', active);
    t.style.color = active ? 'var(--on-surface)' : 'var(--slate)';
    t.style.borderBottom = active ? '3px solid var(--gold)' : '3px solid transparent';
  });
  section.querySelectorAll('.hf-pane').forEach(function(p) {
    p.style.display = (p.getAttribute('data-pane') === target) ? '' : 'none';
  });
}

// ── Preview ──
function previewReload() {
  var f = document.getElementById('previewFrame');
  f.src = f.src;
}

function setViewport(mode) {
  var vp = document.getElementById('previewViewport');
  var frame = document.getElementById('previewFrame');
  frame.style.maxWidth = mode === 'tablet' ? '768px' : mode === 'mobile' ? '375px' : '100%';
  ['Desktop','Tablet','Mobile'].forEach(function(m) {
    var btn = document.getElementById('vp' + m);
    if (btn) btn.style.borderColor = m.toLowerCase() === mode ? 'var(--navy)' : '';
  });
}

// ── Mobile preview toggle ──
// On mobile (≤820px) the preview pane is hidden by default.
// dpTogglePreview() shows/hides it and scrolls it into view.
var _dpPreviewOpen = false;
function dpTogglePreview() {
  var pane = document.getElementById('editorPreview');
  var btn  = document.getElementById('dpPreviewBtn');
  if (!pane) return;
  _dpPreviewOpen = !_dpPreviewOpen;
  pane.classList.toggle('dp-preview-open', _dpPreviewOpen);
  if (btn) {
    btn.textContent = _dpPreviewOpen ? '▼ Hide Preview' : '▶ Show Preview';
    btn.classList.toggle('active', _dpPreviewOpen);
  }
  if (_dpPreviewOpen) {
    // Lazy-load the iframe on first open so it doesn't block form load
    var frame = document.getElementById('previewFrame');
    if (frame && frame.src === '' || (frame && frame.getAttribute('src') === 'about:blank')) {
      frame.src = '/';
    }
    // Scroll the preview pane into view
    setTimeout(function() { pane.scrollIntoView({ behavior: 'smooth', block: 'start' }); }, 50);
  }
}

// ── Reload preview after save ──
<% if (saved) { %>
(function() {
  var f = document.getElementById('previewFrame');
  f.onload = function() {}; // already loaded, just ensure fresh
  f.src = '/?t=' + Date.now();
})();
<% } %>

// ── Live preview engine ──
// Maps design field → CSS variable names (covers both index.ejs and landing.ejs)
//
// IMPORTANT: Only map raw user-controlled color inputs here.
// Do NOT map to server-computed vars (--surface, --on-surface, --on-surface-muted,
// --border, --on-ivory, --on-ivory-muted, --on-navy, etc.) — those are derived by
// enrichDesignContrast() on the server and their live-preview equivalents are
// injected separately below via computedVarsFromInputs().
// Overwriting computed vars with raw color inputs causes the header and nav to
// render stripped of their contrast-aware styles, producing the "preview looks
// unstyled" bug (surface bleeds through as page-bg instead of white/card bg).
var cssVarMap = {
  color_primary:      ['--navy', '--primary'],
  color_primary_deep: ['--navy-deep', '--primary-deep', '--bg'],
  color_primary_mid:  ['--navy-mid', '--primary-mid', '--card'],
  color_accent:       ['--gold', '--accent'],
  color_accent_light: ['--gold-light', '--accent-light', '--accent-hover'],
  color_bg:           ['--ivory', '--bg-page'],  // --surface intentionally excluded (computed)
  color_accent_2:     ['--accent-2'],
  color_accent_3:     ['--accent-3'],
  color_dark:         ['--dark'],   // --on-surface intentionally excluded (computed)
  color_white:        ['--white'],
  color_muted:        ['--muted'], // --on-surface-muted intentionally excluded (computed)
  color_border:       ['--ivory-mid'], // --border intentionally excluded (computed)
  color_success:      ['--success'],
  color_danger:       ['--crimson'],
  hero_overlay_color:      ['--hero-overlay'],
  contact_bg:              ['--contact-bg'],
  contact_heading_color:   ['--contact-heading'],
  contact_eyebrow_color:   ['--contact-eyebrow'],
  contact_text_color:      ['--contact-text'],
  contact_label_color:     ['--contact-label'],
  contact_value_color:     ['--contact-value'],
  contact_form_bg:         ['--contact-form-bg'],
  contact_form_label_color:['--contact-form-label'],
  contact_btn_bg:          ['--contact-btn-bg'],
  contact_btn_color:       ['--contact-btn-color'],
};

// ── Client-side approximation of enrichDesignContrast() ──
// Mirrors the server-side logic so computed CSS vars (--surface, --on-surface,
// --border, --on-ivory, etc.) stay accurate in the live preview as the user
// adjusts colors. Without this the header renders with wrong surface/text colors.
function _hexToRgb(hex) {
  hex = (hex || '').replace('#', '');
  if (hex.length === 3) hex = hex[0]+hex[0]+hex[1]+hex[1]+hex[2]+hex[2];
  if (hex.length !== 6) return null;
  var n = parseInt(hex, 16);
  return { r: (n >> 16) & 255, g: (n >> 8) & 255, b: n & 255 };
}
function _relativeLuminance(hex) {
  var c = _hexToRgb(hex);
  if (!c) return 0.5;
  return [c.r, c.g, c.b].reduce(function(acc, ch, i) {
    ch /= 255;
    ch = ch <= 0.04045 ? ch / 12.92 : Math.pow((ch + 0.055) / 1.055, 2.4);
    return acc + ch * [0.2126, 0.7152, 0.0722][i];
  }, 0);
}
function _readableText(bgHex) {
  return _relativeLuminance(bgHex) > 0.179 ? '#0F1B30' : '#FDFCFA';
}
function _mixHex(hex1, hex2, w) {
  var a = _hexToRgb(hex1) || {r:0,g:0,b:0};
  var b = _hexToRgb(hex2) || {r:255,g:255,b:255};
  var mix = function(c1, c2) { return Math.round(c1 * w + c2 * (1 - w)); };
  var r = mix(a.r, b.r), g = mix(a.g, b.g), bl = mix(a.b, b.b);
  return '#' + ((1 << 24) + (r << 16) + (g << 8) + bl).toString(16).slice(1).toUpperCase();
}
function computedVarsFromInputs() {
  // Read current raw color values from the form
  function inp(name) { var el = document.querySelector('input[name="' + name + '"]'); return el ? (el.value || '') : ''; }
  var colorBg      = inp('color_bg');
  var colorPrimary = inp('color_primary');
  var colorAccent  = inp('color_accent');

  if (!colorBg || !colorPrimary) return {};

  var bgLum = _relativeLuminance(colorBg);
  var onBg  = _readableText(colorBg);

  // --surface: white on light bg, slightly lifted on dark bg
  var surface = bgLum > 0.4 ? '#FFFFFF' : _mixHex('#FFFFFF', colorBg, 0.12);
  var onSurface = _readableText(surface);

  // --border: mix of onBg with bg
  var border = bgLum > 0.4
    ? _mixHex(onBg, colorBg, 0.22)
    : _mixHex(onBg, colorBg, 0.30);

  return {
    '--surface':          surface,
    '--on-surface':       onSurface,
    '--on-surface-muted': _mixHex(onSurface, surface, 0.65),
    '--border':           border,
    '--on-ivory':         onBg,
    '--on-ivory-muted':   _mixHex(onBg, colorBg, 0.6),
    '--on-navy':          _readableText(inp('color_primary') || colorPrimary),
    '--on-navy-deep':     _readableText(inp('color_primary_deep') || colorPrimary),
    '--on-navy-mid':      _readableText(inp('color_primary_mid') || colorPrimary),
    '--on-gold':          _readableText(inp('color_accent') || colorAccent),
    '--on-gold-light':    _readableText(inp('color_accent_light') || colorAccent),
  };
}

// Range/select inputs that map to CSS vars but need special transforms
var cssVarTransform = {
  hero_overlay_opacity: { vars: ['--hero-overlay-alpha'], transform: function(v) { return (parseInt(v,10)||55)/100; } },
  card_border_radius:   { vars: ['--card-radius'],        transform: function(v) { return v + 'px'; } },
  gradient_angle:       { vars: ['--gradient-angle'],     transform: function(v) { return v + 'deg'; } },
  hero_text_align:      { vars: ['--hero-text-align'],    transform: function(v) { return v; } },
  hero_vpos:            { vars: ['--hero-vpos'],          transform: function(v) { return ({ top: 'flex-start', middle: 'center', bottom: 'flex-end' })[v] || 'center'; } },
  hero_height:          { vars: ['--hero-min-height'],    transform: function(v) { return v; } },
  hero_heading_size:    { vars: ['--hero-heading-size'],  transform: function(v) { return v ? v + 'rem' : ''; } },
  // Wordmark / tagline typography
  nav_brand_size:       { vars: ['--nav-brand-size'],      transform: function(v) { return v + 'rem'; } },
  nav_brand_weight:     { vars: ['--nav-brand-weight'],    transform: function(v) { return v; } },
  nav_brand_spacing:    { vars: ['--nav-brand-spacing'],   transform: function(v) { return v + 'em'; } },
  nav_brand_transform:  { vars: ['--nav-brand-transform'], transform: function(v) { return v; } },
  nav_brand_italic:     { vars: ['--nav-brand-style'],     transform: function(v) { return v === 'true' ? 'italic' : 'normal'; } },
  nav_accent_weight:    { vars: ['--nav-accent-weight'],   transform: function(v) { return v || 'inherit'; } },
  nav_accent_italic:    { vars: ['--nav-accent-style'],    transform: function(v) { return v === 'true' ? 'italic' : 'inherit'; } },
  nav_tagline_size:     { vars: ['--nav-tagline-size'],      transform: function(v) { return v + 'rem'; } },
  nav_tagline_weight:   { vars: ['--nav-tagline-weight'],    transform: function(v) { return v; } },
  nav_tagline_spacing:  { vars: ['--nav-tagline-spacing'],   transform: function(v) { return v + 'em'; } },
  nav_tagline_transform:{ vars: ['--nav-tagline-transform'], transform: function(v) { return v; } },
  nav_tagline_italic:   { vars: ['--nav-tagline-style'],     transform: function(v) { return v === 'true' ? 'italic' : 'normal'; } },
  // Logo image sizing + overlap composition
  nav_logo_size:        { vars: ['--nav-logo-size'],         transform: function(v) { return (parseInt(v,10)||48) + 'px'; } },
  nav_logo_overlap_x:   { vars: ['--nav-logo-overlap-x'],    transform: function(v) { return (parseInt(v,10)||0) + 'px'; } },
  nav_logo_overlap_y:   { vars: ['--nav-logo-overlap-y'],    transform: function(v) { return (parseInt(v,10)||0) + 'px'; } },
  // Plain color overrides (text fields) — also push as CSS vars.
  // Empty ⇒ return '' so the push loop SKIPS it and the server-rendered
  // fallback chain stands (nav_text_color → var(--on-ivory,var(--dark))).
  // Returning 'currentColor' here made the wordmark inherit the <a> link
  // colour (purple, once visited) instead of the real brand colour.
  nav_brand_color:      { vars: ['--nav-brand-color'],     transform: function(v) { return v || ''; } },
  nav_tagline_color:    { vars: ['--nav-tagline-color'],   transform: function(v) { return v || ''; } },
};
// Body data-attributes to push into the iframe body element
var bodyDataMap = {
  gradient_enabled:    'gradient',
  card_hover_accent:   'card-hover-accent',
  hero_bg_pattern:     'hero-pattern',
  portfolio_layout:    'portfolio-layout',
  blog_layout:         'blog-layout',
};
var fontMap = {
  font_heading: '--font-heading', font_body: '--font-body',
};
// Google Fonts URL map (for loading fonts in iframe)
var _GF = {
  // Heading / serif / display
  'Cormorant Garamond': 'Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400',
  'Playfair Display': 'Playfair+Display:ital,wght@0,400;0,600;1,400',
  'Lora': 'Lora:ital,wght@0,400;0,600;1,400',
  'Merriweather': 'Merriweather:ital,wght@0,300;0,400;0,700;1,300',
  'Libre Baskerville': 'Libre+Baskerville:ital,wght@0,400;0,700;1,400',
  'DM Serif Display': 'DM+Serif+Display:ital@0;1',
  'Fraunces': 'Fraunces:ital,opsz,wght@0,9..144,300;0,9..144,400;0,9..144,600;1,9..144,400',
  'Sora': 'Sora:wght@300;400;500;600;700',
  'Space Grotesk': 'Space+Grotesk:wght@300;400;500;600;700',
  'Outfit': 'Outfit:wght@300;400;500;600;700',
  'Bebas Neue': 'Bebas+Neue&display=swap',
  'Oswald': 'Oswald:wght@300;400;500;600;700',
  // Body / sans-serif
  'Jost': 'Jost:wght@300;400;500;600',
  'Inter': 'Inter:wght@300;400;500;600',
  'Poppins': 'Poppins:wght@300;400;500;600',
  'Raleway': 'Raleway:wght@300;400;500;600',
  'Nunito': 'Nunito:wght@300;400;500;600',
  'DM Sans': 'DM+Sans:wght@300;400;500;600',
  'Work Sans': 'Work+Sans:wght@300;400;500;600',
  'Source Sans 3': 'Source+Sans+3:wght@300;400;500;600',
  'IBM Plex Sans': 'IBM+Plex+Sans:wght@300;400;500;600',
  'Manrope': 'Manrope:wght@300;400;500;600;700',
  'Figtree': 'Figtree:wght@300;400;500;600;700',
  'Karla': 'Karla:wght@300;400;500;600;700',
  'Rubik': 'Rubik:wght@300;400;500;600',
  'Cabin': 'Cabin:wght@400;500;600;700',
  'Barlow': 'Barlow:wght@300;400;500;600',
  'Lexend': 'Lexend:wght@300;400;500;600;700',
  'Geist': 'Geist:wght@300;400;500;600;700',
};
function loadFontInIframe(doc, fontName, spec) {
  if (!doc || !doc.head || !fontName) return;
  // Prefer the saved CSS2 spec (any catalog font, real weights), then the curated
  // map, then a bare family — mirrors the server so the preview matches the site.
  var frag = (spec && spec.trim()) || _GF[fontName] || fontName.trim().replace(/\s+/g, '+');
  var id = 'slab-font-' + fontName.replace(/\s+/g, '-').toLowerCase();
  var href = 'https://fonts.googleapis.com/css2?family=' + frag + '&display=swap';
  var existing = doc.getElementById(id);
  if (existing) { if (existing.getAttribute('href') !== href) existing.setAttribute('href', href); return; }
  var link = doc.createElement('link');
  link.id = id;
  link.rel = 'stylesheet';
  link.href = href;
  doc.head.appendChild(link);
}

// Map form field names → iframe CSS selectors + property to set
var copyMap = {
  // ── Hero ──
  // index.ejs: .hero-eyebrow, .hero-heading, .hero-btns, .hero-badge-text
  // landing.ejs: .hero-badge, .hero-content h1, .hero-content > p, .hero-actions
  hero_eyebrow:          { sel: '.hero-eyebrow', prop: 'textContent' },
  hero_heading:          { sel: '.hero-heading,.hero-content h1', prop: 'heroHeading' },
  hero_heading_em:       { sel: '.hero-heading,.hero-content h1', prop: 'heroHeading' },
  hero_sub:              { sel: '.hero-content > p', prop: 'textContent' },
  hero_badge:            { sel: '.hero-badge-text,.hero-content .hero-badge', prop: 'textContent' },
  hero_cta_primary:      { sel: '.hero-btns .btn-primary,.hero-actions .btn-primary', prop: 'textContent' },
  hero_cta_secondary:    { sel: '.hero-btns .btn-ghost,.hero-actions .btn-outline', prop: 'textContent' },
  hero_cta_primary_link: { sel: '.hero-btns .btn-primary,.hero-actions .btn-primary', prop: 'href' },
  hero_cta_secondary_link:{ sel: '.hero-btns .btn-ghost,.hero-actions .btn-outline', prop: 'href' },

  // ── Services ──
  // index.ejs: #services .section-eyebrow, .section-heading, .service-card
  // landing.ejs: #features .section-label, .section-title, .feature-card
  services_label:        { sel: '#services .section-eyebrow,#features .section-label', prop: 'textContent' },
  services_heading:      { sel: '#services .section-heading,#features .section-title', prop: 'childText' },
  services_heading_em:   { sel: '#services .section-heading em,#features .section-title em', prop: 'textContent' },
  services_sub:          { sel: '#services .section-sub', prop: 'textContent' },
  service1_title:        { sel: '.service-card:nth-child(1) .service-title,.feature-card:nth-child(1) h3', prop: 'textContent' },
  service1_desc:         { sel: '.service-card:nth-child(1) .service-desc,.feature-card:nth-child(1) p', prop: 'textContent' },
  service2_title:        { sel: '.service-card:nth-child(2) .service-title,.feature-card:nth-child(2) h3', prop: 'textContent' },
  service2_desc:         { sel: '.service-card:nth-child(2) .service-desc,.feature-card:nth-child(2) p', prop: 'textContent' },
  service3_title:        { sel: '.service-card:nth-child(3) .service-title,.feature-card:nth-child(3) h3', prop: 'textContent' },
  service3_desc:         { sel: '.service-card:nth-child(3) .service-desc,.feature-card:nth-child(3) p', prop: 'textContent' },

  // ── About ──
  about_eyebrow:         { sel: '#about .section-eyebrow', prop: 'textContent' },
  about_quote:           { sel: '.advisor-quote', prop: 'textContent' },
  about_desc:            { sel: '.advisor-desc', prop: 'textContent' },
  about_sig:             { sel: '.advisor-sig', prop: 'textContent' },
  about_initial:         { sel: '.advisor-initial', prop: 'textContent' },
  about_stat1_num:       { sel: '.stat-box:nth-child(1) .stat-num', prop: 'textContent' },
  about_stat1_label:     { sel: '.stat-box:nth-child(1) .stat-label', prop: 'textContent' },
  about_stat2_num:       { sel: '.stat-box:nth-child(2) .stat-num', prop: 'textContent' },
  about_stat2_label:     { sel: '.stat-box:nth-child(2) .stat-label', prop: 'textContent' },
  about_stat3_num:       { sel: '.stat-box:nth-child(3) .stat-num', prop: 'textContent' },
  about_stat3_label:     { sel: '.stat-box:nth-child(3) .stat-label', prop: 'textContent' },
  about_stat4_num:       { sel: '.stat-box:nth-child(4) .stat-num', prop: 'textContent' },
  about_stat4_label:     { sel: '.stat-box:nth-child(4) .stat-label', prop: 'textContent' },

  // ── Process ──
  // index.ejs: #process .section-eyebrow, .process-item .process-title/.process-desc
  // landing.ejs: #how .section-label, .step .step-text h3/p
  process_label:         { sel: '#process .section-eyebrow,#how .section-label', prop: 'textContent' },
  process_heading:       { sel: '#process .section-heading,#how .section-title', prop: 'childText' },
  process_heading_em:    { sel: '#process .section-heading em,#how .section-title em', prop: 'textContent' },
  process1_title:        { sel: '.process-list .process-item:nth-child(1) .process-title,.steps .step:nth-child(1) .step-text h3', prop: 'innerHTML' },
  process1_desc:         { sel: '.process-list .process-item:nth-child(1) .process-desc,.steps .step:nth-child(1) .step-text p', prop: 'innerHTML' },
  process2_title:        { sel: '.process-list .process-item:nth-child(2) .process-title,.steps .step:nth-child(2) .step-text h3', prop: 'innerHTML' },
  process2_desc:         { sel: '.process-list .process-item:nth-child(2) .process-desc,.steps .step:nth-child(2) .step-text p', prop: 'innerHTML' },
  process3_title:        { sel: '.process-list .process-item:nth-child(3) .process-title,.steps .step:nth-child(3) .step-text h3', prop: 'innerHTML' },
  process3_desc:         { sel: '.process-list .process-item:nth-child(3) .process-desc,.steps .step:nth-child(3) .step-text p', prop: 'innerHTML' },
  process4_title:        { sel: '.process-list .process-item:nth-child(4) .process-title,.steps .step:nth-child(4) .step-text h3', prop: 'innerHTML' },
  process4_desc:         { sel: '.process-list .process-item:nth-child(4) .process-desc,.steps .step:nth-child(4) .step-text p', prop: 'innerHTML' },

  // ── Contact ──
  // index.ejs: #contact .section-eyebrow, .section-heading, .contact-sub, .contact-detail-item, .contact-form, .btn-submit
  // landing.ejs: #contact .section-label, .section-title, inline form (no .contact-form)
  contact_eyebrow:       { sel: '#contact .section-eyebrow,#contact .section-label', prop: 'textContent' },
  contact_heading:       { sel: '#contact .section-heading,#contact .section-title', prop: 'headingBrEm', pair: 'contact_heading_em' },
  contact_heading_em:    { sel: '#contact .section-heading,#contact .section-title', prop: 'headingBrEm', pair: 'contact_heading' },
  contact_sub:           { sel: '.contact-sub,#contact .section-title + p', prop: 'textContent' },
  contact_location_label:{ sel: '.contact-detail-item:nth-child(1) .detail-label', prop: 'textContent' },
  contact_location:      { sel: '.contact-detail-item:nth-child(1) .detail-value', prop: 'textContent' },
  contact_serving_label: { sel: '.contact-detail-item:nth-child(2) .detail-label', prop: 'textContent' },
  contact_serving:       { sel: '.contact-detail-item:nth-child(2) .detail-value', prop: 'textContent' },
  contact_services_label:{ sel: '.contact-detail-item:nth-child(3) .detail-label', prop: 'textContent' },
  contact_services:      { sel: '.contact-detail-item:nth-child(3) .detail-value', prop: 'textContent' },
  contact_btn:           { sel: '.btn-submit,#contact .btn-lg', prop: 'textContent' },
  // Contact form placeholders (index.ejs uses .contact-form, landing.ejs uses #contact form)
  contact_fname_placeholder: { sel: '.contact-form input[name="firstName"],#contact input[name="name"]', prop: 'placeholder' },
  contact_lname_placeholder: { sel: '.contact-form input[name="lastName"]', prop: 'placeholder' },
  contact_email_placeholder: { sel: '.contact-form input[name="email"],#contact input[name="email"]', prop: 'placeholder' },
  contact_company_placeholder:{ sel: '.contact-form input[name="company"]', prop: 'placeholder' },
  contact_message_placeholder:{ sel: '.contact-form textarea[name="message"],#contact textarea[name="message"]', prop: 'placeholder' },
  // Contact form labels (formLabel — finds label in parent .form-field)
  contact_fname_label:       { sel: '.contact-form input[name="firstName"]', prop: 'formLabel' },
  contact_lname_label:       { sel: '.contact-form input[name="lastName"]', prop: 'formLabel' },
  contact_email_label:       { sel: '.contact-form input[name="email"]', prop: 'formLabel' },
  contact_company_label:     { sel: '.contact-form input[name="company"]', prop: 'formLabel' },
  contact_service_label:       { sel: '.contact-form select[name="service"]', prop: 'formLabel' },
  contact_service_placeholder: { sel: '.contact-form select[name="service"]', prop: 'placeholder' },
  contact_message_label:       { sel: '.contact-form textarea[name="message"]', prop: 'formLabel' },
};

// Dynamic copy selector — returns {sel, prop} for repeater keys (service4,
// process5, about_stat6, etc.) that aren't enumerated in copyMap. Used by the
// preview pipeline so newly-added rows show up immediately in the iframe.
function dynamicCopySelector(name) {
  var m;
  if ((m = name.match(/^service(\d+)_(title|desc)$/))) {
    var n = m[1], k = m[2];
    if (k === 'title') return { sel: '.service-card:nth-child(' + n + ') .service-title,.feature-card:nth-child(' + n + ') h3', prop: 'textContent' };
    if (k === 'desc')  return { sel: '.service-card:nth-child(' + n + ') .service-desc,.feature-card:nth-child(' + n + ') p', prop: 'textContent' };
  }
  if ((m = name.match(/^process(\d+)_(title|desc)$/))) {
    var n2 = m[1], k2 = m[2];
    if (k2 === 'title') return { sel: '.process-list .process-item:nth-child(' + n2 + ') .process-title,.steps .step:nth-child(' + n2 + ') .step-text h3', prop: 'innerHTML' };
    if (k2 === 'desc')  return { sel: '.process-list .process-item:nth-child(' + n2 + ') .process-desc,.steps .step:nth-child(' + n2 + ') .step-text p', prop: 'innerHTML' };
  }
  if ((m = name.match(/^about_stat(\d+)_(num|label)$/))) {
    var n3 = m[1], k3 = m[2];
    if (k3 === 'num')   return { sel: '.stat-box:nth-child(' + n3 + ') .stat-num', prop: 'textContent' };
    if (k3 === 'label') return { sel: '.stat-box:nth-child(' + n3 + ') .stat-label', prop: 'textContent' };
  }
  return null;
}

// Map vis_ checkboxes → iframe section selectors
var visMap = {
  vis_header:    '#main-nav,.nav,.tl-nav',
  vis_hero:      '.hero,.lb-hero',
  vis_marquee:   '.marquee-bar',
  vis_services:  '#services,.services,#features,.features,.lb-cards',
  vis_portfolio: '#portfolio',
  vis_about:     '#about,.advisor-section',
  vis_process:   '#process,.process,#how,.how',
  vis_pricing:   '#pricing,.pricing,.lb-pricing',
  vis_reviews:   '[id*="review"],.lb-testimonials',
  vis_contact:   '#contact,.contact,.lb-cta',
  vis_blog:      '[id*="blog"],[class*="blog-section"]',
  vis_footer:    'footer,.tl-footer',
  vis_admin_link:'.footer-copy span:last-child,.tl-footer-admin,[style*="margin-top:12px"] a[href*="auth"]',
  vis_qr:        '#qr-share-widget',
};

// Expose the DOM↔data binding to the in-preview canvas editor (public/js/canvasEditor.js).
// The editor inverts these maps to tag editable nodes inside the same-origin iframe.
window.SlabPreviewMaps = { copyMap: copyMap, visMap: visMap, dynamicCopySelector: dynamicCopySelector };
// Active template id (empty on standard-layout tenants) — lets the canvas editor
// save per-block field overrides for template-rendered pages.
window.SLAB_ACTIVE_TEMPLATE_ID = '<%= activeTemplateId || "" %>';
// Section types offered in the canvas "+" insert dropdown — driven by the live
// CUSTOM_TEMPLATES catalog so new types appear automatically.
window.SLAB_SECTION_TYPES = <%- JSON.stringify(Object.entries(sectionTemplates || {}).map(function(e){ return [e[0], e[1].label || e[0]]; })) %>;

var _previewReady = false;
function pushToPreview() {
  var frame = document.getElementById('previewFrame');
  if (!frame) return;

  // Try both: direct DOM access AND postMessage
  var doc = null;
  try { doc = frame.contentDocument || frame.contentWindow.document; } catch(x) {}

  // ── 1) CSS variables + fonts ──
  var fontBodyVal = '', fontHeadVal = '';
  var css = ':root{';
  Object.keys(cssVarMap).forEach(function(name) {
    var inp = document.querySelector('input[name="' + name + '"]');
    if (!inp) return;
    var vars = cssVarMap[name];
    if (!inp.value) return; // skip empty — prevents invalid CSS like "--var: !important;"
    for (var vi = 0; vi < vars.length; vi++) css += vars[vi] + ':' + inp.value + ' !important;';
  });
  // Range/select inputs with transforms
  Object.keys(cssVarTransform).forEach(function(name) {
    var inp = document.querySelector('[name="' + name + '"]');
    if (!inp) return;
    var t = cssVarTransform[name];
    var val = t.transform(inp.value);
    // Skip empty results — never inject invalid CSS ("--var: !important;")
    // and, for the color fields, leave the server-rendered fallback in place.
    if (val === '' || val == null) return;
    for (var vi = 0; vi < t.vars.length; vi++) css += t.vars[vi] + ':' + val + ' !important;';
  });
  Object.keys(fontMap).forEach(function(name) {
    // The font picker stores the family in a hidden input (not a <select>).
    var sel = document.querySelector('[name="' + name + '"]');
    if (!sel) return;
    var fallback = name === 'font_heading' ? 'serif' : 'sans-serif';
    css += fontMap[name] + ":'" + sel.value + "'," + fallback + " !important;";
    if (name === 'font_body') fontBodyVal = sel.value;
    if (name === 'font_heading') fontHeadVal = sel.value;
  });
  var fontHeadSpec = (document.querySelector('[name="font_heading_spec"]') || {}).value || '';
  var fontBodySpec = (document.querySelector('[name="font_body_spec"]') || {}).value || '';
  // Append server-side-computed contrast vars (surface, border, on-* text colors).
  // These are derived from the raw colors so must be recalculated client-side
  // whenever the user changes a color input, rather than being taken from cssVarMap.
  var computed = computedVarsFromInputs();
  Object.keys(computed).forEach(function(v) { css += v + ':' + computed[v] + ' !important;'; });

  css += '}';
  // Force font on body + headings for templates that hardcode font-family (landing.ejs)
  if (fontBodyVal) css += " body,p,a,span,div,li,input,textarea,select,button{font-family:'" + fontBodyVal + "',sans-serif !important;}";
  // Headings AND the nav wordmark use the heading font on the live site — the
  // broad span/a rule above would otherwise force the wordmark to the body font,
  // making it render smaller/different (a serif vs sans at the same size). Higher
  // specificity here (class beats bare `span`) wins among the !important rules.
  if (fontHeadVal) css += " h1,h2,h3,h4,h5,h6,.nav-wordmark-top,.nav-wordmark-top .nav-wordmark-accent{font-family:'" + fontHeadVal + "',serif !important;}";

  // ── 2) Layout ──
  var layoutSel = document.querySelector('select[name="landing_layout"]');
  var layout = layoutSel ? layoutSel.value : '';

  // ── Direct DOM approach ──
  if (doc && doc.body) {
    // CSS vars
    var style = doc.getElementById('slab-live-vars');
    if (!style) { style = doc.createElement('style'); style.id = 'slab-live-vars'; doc.head.appendChild(style); }
    style.textContent = css;

    // Load Google Fonts in iframe so font-family changes are visible
    if (fontBodyVal) loadFontInIframe(doc, fontBodyVal, fontBodySpec);
    if (fontHeadVal) loadFontInIframe(doc, fontHeadVal, fontHeadSpec);

    // Layout
    if (layout) doc.body.setAttribute('data-layout', layout);

    // Body data-attributes (gradient, card-hover-accent, hero-pattern)
    Object.keys(bodyDataMap).forEach(function(name) {
      var el = document.querySelector('[name="' + name + '"]');
      if (!el) return;
      var val = el.type === 'checkbox' ? (el.checked ? 'true' : 'false') : el.value;
      doc.body.setAttribute('data-' + bodyDataMap[name], val);
    });

    // Copy fields — skip empty values to preserve server-rendered defaults
    var _doneHeroHeading = false, _doneContactHeading = false;
    Object.keys(copyMap).forEach(function(name) {
      var field = document.querySelector('[name="' + name + '"]');
      if (!field) return;
      var val = field.value;
      var m = copyMap[name];

      // Deduplicate: heroHeading/headingBrEm process both fields in one pass
      if (m.prop === 'heroHeading') {
        if (_doneHeroHeading) return;
        _doneHeroHeading = true;
        var hVal = (document.querySelector('[name="hero_heading"]') || {}).value || '';
        var emVal = (document.querySelector('[name="hero_heading_em"]') || {}).value || '';
        if (!hVal && !emVal) return; // no data — keep server defaults
        var el = doc.querySelector(m.sel);
        if (el) el.innerHTML = hVal + (emVal ? ' <em>' + emVal + '</em>' : '');
        return;
      }
      if (m.prop === 'headingBrEm') {
        if (_doneContactHeading) return;
        _doneContactHeading = true;
        var mf = document.querySelector('[name="contact_heading"]');
        var ef = document.querySelector('[name="contact_heading_em"]');
        var mVal = mf ? mf.value : '', eVal = ef ? ef.value : '';
        if (!mVal && !eVal) return;
        var el = doc.querySelector(m.sel);
        if (el) el.innerHTML = mVal + (eVal ? '<br><em>' + eVal + '</em>' : '');
        return;
      }

      // Skip empty values — don't wipe server-rendered content
      if (!val && m.prop !== 'href' && m.prop !== 'placeholder' && m.prop !== 'formLabel') return;

      var el = doc.querySelector(m.sel);
      if (!el) return;

      if (m.prop === 'childText') {
        var emInput = document.querySelector('[name="' + name + '_em"]');
        if (emInput && emInput.value) { el.innerHTML = val + ' <em>' + emInput.value + '</em>'; }
        else if (val) { el.textContent = val; }
      } else if (m.prop === 'textContent') { el.textContent = val; }
      else if (m.prop === 'innerHTML') { el.innerHTML = val; }
      else if (m.prop === 'href') { el.setAttribute('href', val || '#'); }
      else if (m.prop === 'placeholder') { el.setAttribute('placeholder', val); }
      else if (m.prop === 'formLabel') {
        var ff = el.closest ? el.closest('.form-field') : null;
        var lbl = ff ? ff.querySelector('label') : null;
        if (lbl) lbl.textContent = val;
      }
    });

    // Dynamic repeater fields (service4, process5, about_stat6, …)
    document.querySelectorAll('.dp-repeater[data-prefix] input[name], .dp-repeater[data-prefix] textarea[name]').forEach(function(field) {
      var name = field.getAttribute('name');
      if (copyMap[name]) return; // already handled above
      var dyn = dynamicCopySelector(name);
      if (!dyn) return;
      var val = field.value;
      if (!val && dyn.prop !== 'href') return;
      var el = doc.querySelector(dyn.sel);
      if (!el) return;
      if (dyn.prop === 'textContent') el.textContent = val;
      else if (dyn.prop === 'innerHTML') el.innerHTML = val;
    });

    // Visibility
    Object.keys(visMap).forEach(function(key) {
      var cb = document.querySelector('input[name="' + key + '"]');
      if (!cb) return;
      visMap[key].split(',').forEach(function(s) {
        var els = doc.querySelectorAll(s.trim());
        for (var i = 0; i < els.length; i++) els[i].style.display = cb.checked ? '' : 'none';
      });
    });

    _previewReady = true;
    return;
  }

  // ── Fallback: postMessage ──
  if (frame.contentWindow) {
    var msgCopy = [];
    var _pmDoneHero = false, _pmDoneContact = false;
    Object.keys(copyMap).forEach(function(name) {
      var field = document.querySelector('[name="' + name + '"]');
      if (!field) return;
      var val = field.value, m = copyMap[name];
      if (m.prop === 'heroHeading') {
        if (_pmDoneHero) return; _pmDoneHero = true;
        var hVal = (document.querySelector('[name="hero_heading"]') || {}).value || '';
        var emVal = (document.querySelector('[name="hero_heading_em"]') || {}).value || '';
        if (hVal || emVal) msgCopy.push({ sel: m.sel, prop: 'innerHTML', val: hVal + (emVal ? ' <em>' + emVal + '</em>' : '') });
        return;
      }
      if (m.prop === 'headingBrEm') {
        if (_pmDoneContact) return; _pmDoneContact = true;
        var mf = document.querySelector('[name="contact_heading"]');
        var ef = document.querySelector('[name="contact_heading_em"]');
        var mVal = mf ? mf.value : '', eVal = ef ? ef.value : '';
        if (mVal || eVal) msgCopy.push({ sel: m.sel, prop: 'innerHTML', val: mVal + (eVal ? '<br><em>' + eVal + '</em>' : '') });
        return;
      }
      if (!val && m.prop !== 'href' && m.prop !== 'placeholder' && m.prop !== 'formLabel') return;
      if (m.prop === 'childText') {
        var ei = document.querySelector('[name="' + name + '_em"]');
        if (ei && ei.value) msgCopy.push({ sel: m.sel, prop: 'innerHTML', val: val + ' <em>' + ei.value + '</em>' });
        else if (val) msgCopy.push({ sel: m.sel, prop: 'textContent', val: val });
      } else if (m.prop === 'formLabel') {
        msgCopy.push({ sel: m.sel, prop: 'formLabel', val: val });
      } else {
        msgCopy.push({ sel: m.sel, prop: m.prop, val: val });
      }
    });
    // Dynamic repeater fields (postMessage path)
    document.querySelectorAll('.dp-repeater[data-prefix] input[name], .dp-repeater[data-prefix] textarea[name]').forEach(function(field) {
      var nm = field.getAttribute('name');
      if (copyMap[nm]) return;
      var dyn = dynamicCopySelector(nm);
      if (!dyn) return;
      var v = field.value;
      if (!v) return;
      msgCopy.push({ sel: dyn.sel, prop: dyn.prop, val: v });
    });
    var vis = {};
    Object.keys(visMap).forEach(function(key) {
      var cb = document.querySelector('input[name="' + key + '"]');
      if (cb) vis[visMap[key]] = cb.checked;
    });
    var cssVarsObj = {};
    Object.keys(cssVarMap).forEach(function(n) { var inp = document.querySelector('input[name="' + n + '"]'); if (inp) { var vars = cssVarMap[n]; for (var vi = 0; vi < vars.length; vi++) cssVarsObj[vars[vi]] = inp.value; } });
    Object.keys(cssVarTransform).forEach(function(n) { var inp = document.querySelector('[name="' + n + '"]'); if (inp) { var t = cssVarTransform[n]; var val = t.transform(inp.value); for (var vi = 0; vi < t.vars.length; vi++) cssVarsObj[t.vars[vi]] = val; } });
    Object.keys(fontMap).forEach(function(n) { var el = document.querySelector('[name="' + n + '"]'); if (el) cssVarsObj[fontMap[n]] = "'" + el.value + "'," + (n === 'font_heading' ? 'serif' : 'sans-serif'); });
    // Merge computed contrast vars into the postMessage payload
    var _cmp = computedVarsFromInputs();
    Object.keys(_cmp).forEach(function(v) { cssVarsObj[v] = _cmp[v]; });
    var extraCss = '';
    if (fontBodyVal) extraCss += " body,p,a,span,div,li,input,textarea,select,button{font-family:'" + fontBodyVal + "',sans-serif !important;}";
    if (fontHeadVal) extraCss += " h1,h2,h3,h4,h5,h6{font-family:'" + fontHeadVal + "',serif !important;}";
    // Build font stylesheet URLs from spec → curated map → bare family (mirrors server).
    var _fontFrag = function(name, spec) { return (spec && spec.trim()) || _GF[name] || (name || '').trim().replace(/\s+/g, '+'); };
    var fonts = [];
    if (fontBodyVal) fonts.push('https://fonts.googleapis.com/css2?family=' + _fontFrag(fontBodyVal, fontBodySpec) + '&display=swap');
    if (fontHeadVal) fonts.push('https://fonts.googleapis.com/css2?family=' + _fontFrag(fontHeadVal, fontHeadSpec) + '&display=swap');
    var bodyAttrs = {};
    Object.keys(bodyDataMap).forEach(function(name) {
      var el = document.querySelector('[name="' + name + '"]');
      if (!el) return;
      bodyAttrs[bodyDataMap[name]] = el.type === 'checkbox' ? (el.checked ? 'true' : 'false') : el.value;
    });
    frame.contentWindow.postMessage({ type: 'slab-live-preview', cssVars: cssVarsObj, extraCss: extraCss, layout: layout, copy: msgCopy, visibility: vis, fonts: fonts, bodyAttrs: bodyAttrs }, '*');
  }
}

// Debounced push
// _previewDirty gates the live-preview overrides: they only apply once the admin
// has actually changed something. On a clean load (or right after save) the
// iframe shows the TRUE server render, so the preview matches the live site
// exactly — no JS-approximated contrast vars / fonts injected over the real page.
var _previewDirty = false;
var pushTimer;
function debouncedPush() {
  _previewDirty = true;
  clearTimeout(pushTimer);
  pushTimer = setTimeout(pushToPreview, 80);
}

// ── Layout change → preview-only reload (no save until "Save All") ──
var _layoutSelect = document.querySelector('select[name="landing_layout"]');
if (_layoutSelect) {
  _layoutSelect.addEventListener('change', function() {
    var val = _layoutSelect.value;
    var f = document.getElementById('previewFrame');
    f.src = '/?preview_layout=' + val + '&t=' + Date.now();
  });
}

// ── Server-rendered fields: section_animation & snap_enabled need iframe reload to take effect ──
// (These drive EJS conditionals that inject JS/CSS blocks — cannot be patched via DOM/postMessage)
// NOTE: ticker_* fields are wired in _ticker-section.ejs (self-contained save + reload).
var _reloadFields = ['section_animation', 'snap_enabled', 'snap_strictness', 'hero_style', 'hero_slideshow_static',
  'cookie_consent_enabled', 'cookie_consent_style', 'cookie_consent_position',
  'cookie_bg', 'cookie_text_color', 'cookie_accent'];
_reloadFields.forEach(function(name) {
  var el = document.querySelector('[name="' + name + '"]');
  if (!el) return;
  el.addEventListener('change', function() {
    // Save this key first via the /key API, then reload preview
    fetch('/admin/design/key', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ key: name, value: el.type === 'checkbox' ? (el.checked ? 'true' : 'false') : el.value })
    }).then(function() {
      var f = document.getElementById('previewFrame');
      f.src = '/?t=' + Date.now();
    });
  });
});

// Delegate input/change listeners on the editor panel so rows added later
// (repeater + Add) automatically get live-preview without re-attaching.
(function() {
  var panel = document.querySelector('.editor-panel');
  if (!panel) return;
  panel.addEventListener('input', function(e) {
    if (e.target.matches('input, select, textarea')) debouncedPush();
  });
  panel.addEventListener('change', function(e) {
    if (e.target.matches('input, select, textarea')) debouncedPush();
  });
})();

// Push once iframe loads — retry to handle slow renders
var _pvFrame = document.getElementById('previewFrame');
var _pvStatus = document.querySelector('.preview-bar span[style*="auto"]');
_pvFrame.addEventListener('load', function() {
  setTimeout(function() {
    // Only overlay edits when the admin has made unsaved changes; otherwise the
    // iframe already IS the true server render and must be left untouched.
    if (_previewDirty) pushToPreview();
    if (_pvStatus) _pvStatus.textContent = _previewReady ? 'Live Preview' : 'Preview (no DOM access — using postMessage)';
  }, 300);
  setTimeout(function() { if (_previewDirty) pushToPreview(); }, 1000);
});
// Initial push after page load — only if there are pending edits (there won't be
// on a fresh load, so the preview reflects exactly what's saved/rendered).
setTimeout(function() {
  if (_previewDirty) pushToPreview();
  if (_pvStatus) _pvStatus.textContent = _previewReady ? 'Live Preview' : 'Preview (connecting...)';
}, 500);

// ── Sync color pickers with hex display + swatches ──
document.querySelectorAll('input[type=color]').forEach(function(picker) {
  var textInput = picker.parentElement.querySelector('input[type=text]');
  picker.addEventListener('input', function() {
    if (textInput) textInput.value = picker.value;
    // Update palette swatches (the 6 primary ones)
    var swatches = document.querySelectorAll('#swatch-preview .swatch');
    var names = ['color_primary','color_primary_deep','color_primary_mid','color_accent','color_accent_light','color_bg'];
    names.forEach(function(n, i) {
      var inp = document.querySelector('input[name="' + n + '"]');
      if (inp && swatches[i]) swatches[i].style.background = inp.value;
    });
  });
});
// Also sync the range-slider label displays
document.querySelectorAll('input[type=range]').forEach(function(range) {
  range.addEventListener('input', function() {
    // Update the label that shows the current value (contains the value in its text)
    var label = range.closest('.ep-field') && range.closest('.ep-field').querySelector('label');
    if (!label) return;
    var text = label.textContent;
    // Replace trailing (Npx) or (N%) or (Ndeg) pattern
    label.textContent = text.replace(/\(\d+(?:px|%|deg)?\)/, '(' + range.value + (range.name === 'hero_overlay_opacity' ? '%' : range.name === 'card_border_radius' ? 'px' : range.name === 'gradient_angle' ? '\u00b0' : '') + ')');
  });
});

// ── 3D model toggle ──
function toggleModel(key, enabled) {
  fetch('/admin/design/toggle-model', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ key: key, value: enabled ? 'true' : 'false' })
  }).then(function(r) { return r.json(); });
}

// ── Save theme via fetch (avoids nested form issue) ──
function saveTheme() {
  var name = document.getElementById('themeNameInput').value.trim();
  if (!name) { alert('Enter a theme name'); return; }
  var form = document.createElement('form');
  form.method = 'POST';
  form.action = '/admin/design/themes';
  var inp = document.createElement('input');
  inp.type = 'hidden'; inp.name = 'theme_name'; inp.value = name;
  form.appendChild(inp);
  document.body.appendChild(form);
  form.submit();
}

// ══════════════════════════════════════════════════════════════════════════════
// DESIGN AGENT — chat panel (matches blog/pages pattern)
// ══════════════════════════════════════════════════════════════════════════════
(function() {
  var panel    = document.getElementById('agentPanel');
  var overlay  = document.getElementById('agentOverlay');
  var toggle   = document.getElementById('agentToggle');
  var closeBtn = document.getElementById('agentClose');
  var msgs     = document.getElementById('agentMsgs');
  var input    = document.getElementById('agentInput');
  var sendBtn  = document.getElementById('agentSend');
  var agentMessages = [];

  // ── Open / close ──
  function openPanel()  { panel.classList.add('open'); overlay.classList.add('active'); input.focus(); }
  function closePanel() { panel.classList.remove('open'); overlay.classList.remove('active'); }
  if (toggle)  toggle.addEventListener('click', openPanel);
  // Mobile duplicate agent toggle (in preview-toggle bar)
  var toggleMobile = document.getElementById('agentToggleMobile');
  if (toggleMobile) toggleMobile.addEventListener('click', openPanel);
  if (closeBtn) closeBtn.addEventListener('click', closePanel);
  if (overlay) overlay.addEventListener('click', closePanel);

  // ── Message display ──
  function appendMsg(role, text, extra) {
    var el = document.createElement('div');
    el.className = 'ap-msg ' + role;
    el.innerHTML = '<div class="ap-bubble">' + text + '</div>';
    if (extra) el.innerHTML += extra;
    msgs.appendChild(el);
    msgs.scrollTop = msgs.scrollHeight;
  }

  function showTyping() {
    var el = document.createElement('div');
    el.id = 'apTyping'; el.className = 'ap-msg assistant';
    el.innerHTML = '<div class="ap-typing"><div class="ap-typing-dot"></div><div class="ap-typing-dot"></div><div class="ap-typing-dot"></div></div>';
    msgs.appendChild(el);
    msgs.scrollTop = msgs.scrollHeight;
  }
  function hideTyping() { var t = document.getElementById('apTyping'); if (t) t.remove(); }

  // ── Collect current design state ──
  function getCurrentDesign() {
    var d = {};
    document.querySelectorAll('.editor-panel input, .editor-panel select, .editor-panel textarea').forEach(function(el) {
      if (!el.name) return;
      if (el.type === 'checkbox') d[el.name] = el.checked ? 'true' : 'false';
      else d[el.name] = el.value;
    });
    return d;
  }

  // ── Fill form fields + trigger live preview ──
  function fillField(key, val) {
    var el = document.querySelector('[name="' + key + '"]');
    if (!el) return;
    if (el.type === 'checkbox') {
      el.checked = val === 'true';
    } else {
      el.value = val;
      if (el.type === 'color') {
        var textInput = el.parentElement && el.parentElement.querySelector('input[type=text]');
        if (textInput) textInput.value = val;
      }
    }
    el.classList.add('ai-filled');
    el.dispatchEvent(new Event('input', { bubbles: true }));
    el.dispatchEvent(new Event('change', { bubbles: true }));
  }

  // ── Send to agent ──
  async function sendAgent() {
    var text = input.value.trim();
    if (!text) return;
    input.value = '';
    input.style.height = 'auto';
    sendBtn.disabled = true;

    agentMessages.push({ role: 'user', content: text });
    appendMsg('user', text.replace(/&/g,'&amp;').replace(/</g,'&lt;'));
    showTyping();

    try {
      var res = await fetch('/admin/design/agent', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          messages: agentMessages,
          currentDesign: getCurrentDesign()
        })
      });
      var data = await res.json();
      hideTyping();

      if (data.error) {
        appendMsg('assistant', 'Error: ' + data.error);
        return;
      }

      var fills = Object.entries(data.fill || {});
      fills.forEach(function(pair) { fillField(pair[0], pair[1]); });
      agentMessages.push({ role: 'assistant', content: data.message });

      var extra = '';
      if (fills.length) {
        var details = fills.map(function(pair) {
          var k = pair[0], v = pair[1];
          var isColor = k.indexOf('color_') === 0;
          var swatch = isColor
            ? '<span style="display:inline-block;width:10px;height:10px;border-radius:2px;background:' + v + ';vertical-align:middle;margin-right:3px;border:1px solid rgba(0,0,0,0.1);"></span>'
            : '';
          return '<span style="font-size:0.65rem;color:var(--slate);">' + k + ': ' + swatch + v + '</span>';
        }).join('<br>');
        extra = '<div class="ap-fill-notice" style="margin-top:4px;">&#10003; ' +
          fills.length + ' field' + (fills.length > 1 ? 's' : '') + ' updated</div>' +
          '<div style="margin-top:2px;">' + details + '</div>';
      }
      appendMsg('assistant', data.message, extra);
    } catch(e) {
      hideTyping();
      appendMsg('assistant', 'Connection error. Please try again.');
    } finally {
      sendBtn.disabled = false;
      input.focus();
    }
  }

  // ── Input handlers ──
  sendBtn.addEventListener('click', sendAgent);
  input.addEventListener('keydown', function(e) {
    if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); sendAgent(); }
  });
  input.addEventListener('input', function() {
    this.style.height = 'auto';
    this.style.height = Math.min(this.scrollHeight, 120) + 'px';
  });

  // ── Quick prompts ──
  var quickPrompts = [
    'Bold dark palette',
    'Warm earthy tones',
    'Write hero copy',
    'Modern minimal look',
    'Toggle off blog & QR',
    'Suggest all service copy'
  ];
  var quickContainer = document.getElementById('apQuick');
  if (quickContainer) {
    // Shuffle and pick 4
    var shuffled = quickPrompts.sort(function() { return 0.5 - Math.random(); }).slice(0, 4);
    shuffled.forEach(function(q) {
      var btn = document.createElement('button');
      btn.type = 'button';
      btn.className = 'ap-quick-btn';
      btn.textContent = q;
      btn.addEventListener('click', function() {
        input.value = q;
        sendAgent();
      });
      quickContainer.appendChild(btn);
    });
  }
})();
</script>
<script src="/js/agentHealth.js"></script>
<script src="/js/tours/design.js"></script>
<script src="/js/assetPicker.js?v=<%= Date.now() %>"></script>
<script src="/js/canvasEditor.js?v=<%= Date.now() %>"></script>
<script>
// ── Hero full-bg media picker (image or video from tenant assets) ──
(function () {
  var urlField    = document.getElementById('field-hero_bg_media_url');
  var typeField   = document.getElementById('field-hero_bg_media_type');
  var posterField = document.getElementById('field-hero_bg_media_poster');
  var preview     = document.getElementById('heroBgPreview');
  var pickImg     = document.getElementById('heroBgPickImage');
  var pickVid     = document.getElementById('heroBgPickVideo');
  var clearBtn    = document.getElementById('heroBgClear');
  if (!urlField || !pickImg || !pickVid) return;

  function renderPreview() {
    if (!urlField.value) {
      preview.style.display = 'none';
      preview.innerHTML = '';
      clearBtn.style.display = 'none';
      return;
    }
    preview.style.display = '';
    clearBtn.style.display = '';
    if (typeField.value === 'video') {
      preview.innerHTML = '<video src="' + urlField.value + '" muted playsinline autoplay loop style="width:100%;height:100%;object-fit:cover;"></video>';
    } else {
      preview.innerHTML = '<img src="' + urlField.value + '" alt="Hero background" style="width:100%;height:100%;object-fit:cover;">';
    }
  }

  function saveKey(key, value) {
    return fetch('/admin/design/key', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ key: key, value: value })
    });
  }
  function persistHeroBg() {
    return Promise.all([
      saveKey('hero_bg_media_url',    urlField.value),
      saveKey('hero_bg_media_type',   typeField.value),
      saveKey('hero_bg_media_poster', posterField.value),
    ]);
  }

  function pick(type) {
    if (typeof window.openAssetPicker !== 'function') {
      alert('Asset picker not loaded');
      return;
    }
    window.openAssetPicker({
      type: type,
      onSelect: function (asset) {
        urlField.value = asset.publicUrl || '';
        typeField.value = type;
        if (type === 'video' && asset.posterUrl) posterField.value = asset.posterUrl;
        else if (type === 'image') posterField.value = '';
        renderPreview();
        persistHeroBg().then(function () {
          if (typeof previewReload === 'function') previewReload();
        });
      }
    });
  }

  pickImg.addEventListener('click', function () { pick('image'); });
  pickVid.addEventListener('click', function () { pick('video'); });
  clearBtn.addEventListener('click', function () {
    urlField.value = '';
    typeField.value = '';
    posterField.value = '';
    renderPreview();
    persistHeroBg().then(function () {
      if (typeof previewReload === 'function') previewReload();
    });
  });
})();
</script>

</body>
</html>

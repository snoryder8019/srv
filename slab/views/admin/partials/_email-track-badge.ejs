<%# Open/click state pill for one outbound client email. `mt` comes from
    emailTrack() in the Emails tab; null means the message isn't tracked. %>
<% if (mt) { %><span class="trk <%= mt.cls %>" title="<%= TRK_HINT %>"><%= mt.label %></span><% } %>
